# LP-MSPM0G3507 中文精讲与学习路线

对应资料：[LP-MSPM0G3507.pdf](./LP-MSPM0G3507.pdf)（TI 文档号 SLAU873D，2024-10，Rev. D）

## 0. 先把这本手册的定位弄清楚

这份 PDF 是 **LaunchPad 开发板用户指南**，重点是板上有什么、跳线怎么接、信号怎样走、如何供电和调试。它不是完整的芯片手册，因此只靠这 24 页还不能“学透 MSPM0G3507”。完整学习要同时看四类资料：

1. **开发板用户指南**：板载电路、跳线、接口和原理图，也就是当前这份 PDF。
2. **MSPM0G350x 数据手册**：引脚复用、电气极限、模拟指标、封装和订货信息。
3. **MSPM0 G-Series TRM**：寄存器、时钟、ADC、OPA、GPAMP、定时器、DMA、CAN 等模块的工作原理。
4. **MSPM0 SDK 示例**：能导入 CCS/SysConfig 的可运行代码。学习外设时应优先从对应示例改起。

官方入口：

- [LP-MSPM0G3507 开发板页面](https://www.ti.com/tool/LP-MSPM0G3507)
- [MSPM0G3507 芯片页面与数据手册](https://www.ti.com/product/MSPM0G3507)
- [MSPM0 G-Series TRM](https://www.ti.com/lit/pdf/slau846)
- [MSPM0 SDK](https://www.ti.com/tool/MSPM0-SDK)
- [MSPM0 SDK 示例索引](https://software-dl.ti.com/msp430/esd/MSPM0-SDK/latest/docs/english/sdk_users_guide/doc_guide/doc_guide-srcs/examples_guide.html)

一句话理解这块板：**上半部分是 XDS110 调试、USB 串口、供电和能耗测量工具；下半部分才是 MSPM0G3507 目标系统、传感器、LED、模拟前端和扩展接口。**

---

## 1. 芯片本体能做什么

板上目标芯片是 LQFP64 封装的 MSPM0G3507，手册列出的核心资源如下：

| 类别 | 资源 | 你应该如何理解 |
|---|---|---|
| CPU | Arm Cortex-M0+，最高 80 MHz，带 MPU | 适合实时控制、采集、通信，不是跑 Linux 的处理器 |
| 存储 | 128 KB Flash，32 KB SRAM | Flash 放程序和常量，SRAM 放变量、栈和 ADC/DMA 缓冲区 |
| DMA | 7 通道 | ADC、UART、SPI 等可不经过 CPU 搬数据 |
| ADC | 2 个 12 位 SAR ADC，最高 4 MSPS | 适合高速采样；“12 位”和“4 MSPS”分别表示幅度分辨率与最快采样速度 |
| DAC | 1 个 12 位 DAC | 可输出可编程模拟电压，适合偏置、波形或闭环控制 |
| 模拟比较器 | 3 个高速 COMP，带参考 DAC | 不必启动 ADC 就能快速判断电压越界 |
| 片内运放 | 2 个零漂、斩波、可编程增益 OPA | 用于缓冲、放大、跨阻等模拟信号链 |
| 通用放大器 | 1 个 GPAMP | 板上专门留了热敏电阻/电机电流检测实验网络 |
| 定时器 | 2 个高级 16 位、3 个通用 16 位、1 个 32 位高分辨率定时器 | PWM、电机控制、输入捕获、编码器、精确定时 |
| 通信 | 4 UART、2 I2C、2 SPI、1 CAN 2.0/FD | 注意：板上没有 CAN 总线收发器，CAN_TX/RX 不能直接接 CANH/CANL |
| 安全/计算 | AES、CRC、TRNG、数学加速器 | 加解密、校验、随机数以及控制算法加速 |
| GPIO | 此封装最多 60 个 | 每个脚通常有多种复用功能，不能只看“PAxx/PBxx”就接线 |
| 工作电压 | 1.62 V～3.6 V | 这是 MCU 范围；本板多数板载模拟电路按 3.3 V 设计 |

常用换算：12 位 ADC 满量程共有 4096 个码。单端、无符号采样的近似关系为：

```text
Vin ≈ ADC_code / 4095 × Vref
```

实际测量还会受到参考电压误差、ADC 增益/失调误差、采样时间、源阻抗、噪声和 PCB 布局影响。

---

## 2. 开发板各区域

从 USB 口朝上放置开发板，可以把它分为下面几块：

1. **顶部 XDS110-ET 区域**：下载程序、单步调试、SWD、USB 转串口、EnergyTrace 能耗测量，并可给目标板供电。
2. **J101 隔离跳线排**：连接或隔离 XDS110 与目标 MCU 的 GND、5V、3V3、UART、复位、SWD 和 BSL。
3. **中部目标 MCU**：MSPM0G3507 及其复位、时钟、电源和 BoosterPack 排针。
4. **底部模拟实验区**：TMP6131 温度传感器、OPA2365 外置双运放、片内 OPA 光传感器电路、RGB LED、普通 LED、QEI 接口和底部引脚扩展排针。

板上实际有三个可按按键：复位键、S1/BSL 键、S2 用户键。手册说“2 buttons”时通常只把后两者算作用户交互按键。

---

## 3. 第一次上电与开发流程

### 3.1 出厂演示

用 Micro-USB 连接电脑，绿色电源灯点亮。出厂程序可配合 TI 的 OoBE GUI 演示：

- 改变 LED 闪烁速度；
- 读取光传感器并画曲线，同时改变 RGB LED 亮度；
- 读取 TMP6131 电压并换算温度，以 RGB 颜色显示温度区间；
- 产生正弦、方波、锯齿波，再由 ADC 采样并画图。

手册原文说“three main examples”却列了四项，这是文字错误，按四项理解即可。

### 3.2 推荐的软件结构

学习阶段建议使用 CCS + MSPM0 SDK + SysConfig：

```text
system.syscfg / 工程名.syscfg
        ↓ 配置引脚、时钟、外设、DMA、中断
SysConfig 自动生成 ti_msp_dl_config.c/.h
        ↓
应用代码调用 SYSCFG_DL_init()
        ↓
应用代码使用 DriverLib 完成采集、控制和通信
```

`.syscfg` 是引脚和外设配置的源文件。不要手工修改自动生成的 `ti_msp_dl_config.c/.h`，因为重新生成时会被覆盖。也不要猜 SysConfig 生成的宏名，应从当前工程的 `ti_msp_dl_config.h` 中读取。

---

## 4. 指示灯、按键和基本 GPIO

| 板载资源 | MCU 引脚 | 默认连接 | 有效电平/注意事项 |
|---|---:|---|---|
| 红色 LED1 | PA0 | J4 安装 | LED 接到 3.3 V，PA0 输出低电平时点亮，即低有效 |
| RGB 蓝色 | PB22 | J5 安装 | 输出高电平点亮 |
| RGB 红色 | PB26 | J6 安装 | 输出高电平点亮 |
| RGB 绿色 | PB27 | J7 安装 | 输出高电平点亮 |
| S1/BSL 按键 | PA18 | J8 安装 | 47 kΩ 下拉，按下接 3.3 V，通常高有效；同时与 BSL/模拟功能复用 |
| S2 用户按键 | PB21 | 直接连接 | 按下接地，GPIO 通常配置内部上拉，低有效 |
| S3 复位键 | NRST | 直接连接 | 按下拉低复位 |

如果要把这些脚用于别的功能，可拔掉 J4～J8 对应跳帽，但软件仍必须重新做引脚复用。PA18 同时参与 BSL、OPA1/ADC1.3/GPAMP 负输入和 BoosterPack 选择，冲突尤其多。

J19、J20 给 PA0、PA1 提供开漏输出上拉选择，可选择上拉到 3.3 V 或 5 V。使用 5 V 上拉前必须再核对数据手册中对应引脚的 5 V 容限和当前引脚模式。

---

## 5. XDS110、J101 与 USB 串口

### 5.1 J101 每一项的意义

| J101 信号 | 含义 |
|---|---|
| GND | 调试器区与目标区共地 |
| 5V | USB VBUS 5 V 送往目标侧/扩展口 |
| 3V3 | XDS110 侧稳压出的 3.3 V 送往目标侧 |
| RXD<< | 电脑/XDS110 向目标 MCU 发送的数据 |
| TXD>> | 目标 MCU 向电脑/XDS110 发送的数据 |
| NRST | 目标复位 |
| SWDIO | SWD 双向数据 |
| SWCLK | SWD 时钟 |
| BSL | 引导加载相关信号 |

拔掉 J101 跳帽的用途不是“板坏了才拔”，而是：精确测功耗、隔离供电、释放 UART/SWD 引脚、换用外部调试器，或把板载 XDS110 借给另一块目标板。

### 5.2 后台串口

板载 USB 会枚举出 `XDS110 Class Application/User UART` 虚拟串口。目标侧默认使用 UART0：

- PA10：UART0_TX；
- PA11：UART0_RX；
- J21/J22 的 1-2 位：接 XDS110 USB 串口；
- J21/J22 的 2-3 位：接 BoosterPack 方向；
- 电脑串口工具的波特率、数据位、校验和停止位必须与 MCU 一致。

PA10/PA11 是这块板的默认调试串口脚。把它们改作 GPIO、PWM 或其他外设前，要先处理 J21/J22，否则会与 XDS110 或 BoosterPack 线路冲突。

### 5.3 外部调试器和“借出”XDS110

- **J103 是 External Debug IN**：外部调试器接板上 MSPM0G3507。先拔 J101 的 NRST、SWDIO、SWCLK，避免两个调试器同时驱动。
- **J102 是 XDS110 Debug OUT**：把板载 XDS110 用于外部 Cortex-M 目标。输出逻辑电平只有 3.3 V；手册明确说对外部目标不提供 EnergyTrace。

调试接口的 VCC 脚常用于电压检测，不应想当然地当成电源输出。接外部目标前要再次确认 J102/JP102 配置、共地和目标电压。

---

## 6. 供电与低功耗测量

### 6.1 供电路径

- 最常用：USB → XDS110 → 5 V 与 3.3 V → J101 → 目标板。
- J10：5 V 与 GND 电源排针。
- J11：3.3 V 与 GND 电源排针，可给目标侧外部供电。
- J13：`Analog Power`，给 TMP6131 和外置 OPA2365 等模拟区供电。

外部给 J11 供电时，电压必须符合器件要求，并避免与 USB 侧 3.3 V 电源互相顶电。最稳妥的做法是先理清 J101 的 3V3/5V 跳帽、电源方向和共地关系，再上电。MCU 允许 1.62～3.6 V，但板载传感器、LED 亮度和模拟前端的标称工作点是按 3.3 V 设计的，低压实验不能只看 MCU 范围。

### 6.2 串联电流表测目标电流

1. 断电，拔掉 J101 的 3V3 跳帽。
2. 把电流表串接在这两个 3V3 端点之间，先用较大量程。
3. 不用模拟区时拔掉 J13；不用 LED、光传感器、热敏电阻时也隔离相应跳线。
4. 处理 UART、SWD 和外部模块可能产生的灌电流/倒灌电流。
5. 确保所有输入脚不悬空，再让程序进入稳定的 RUN、SLEEP 或 STOP 状态测量。
6. 也可用 EnergyTrace 比较不同程序阶段的能耗波形。

警告：电流表应串联，不能像电压表一样直接跨接 3V3 与 GND。手册第 14 页把被测 MCU 误写成 `MSP430FR2355`，这里实际指的是本板 MSPM0G3507。

---

## 7. 时钟系统

板上提供：

- Y1：32.768 kHz 低频晶振，用于 RTC、日历和低功耗计时；
- Y2：40 MHz 高频晶振，用于高精度高速系统时钟；
- 片内 SYSOSC：上电默认 32 MHz，手册给出的默认精度为 2.5%；
- PA2/ROSC：可接高精度电阻提高 SYSOSC 精度，原理图给出 R8 = 100 kΩ、0.1%。

默认 MCLK 来自 32 MHz SYSOSC，RUN 模式下 CPUCLK 由 MCLK 提供。ULPCLK 可配置在 RUN/SLEEP 中工作。CPU 标称最高 80 MHz 不等于上电就运行在 80 MHz；需要在时钟配置中选择相应时钟源、PLL/分频，并遵守 Flash 等待周期和电压条件。

Rev. D 把 40 MHz 晶振 Y2 的 C10、C12 从 22 pF 改为 18 pF。若你手里的板子版本不同，应以实物丝印、BOM 和对应版本原理图为准。

---

## 8. 三条最重要的模拟信号链

### 8.1 先区分三种“运放”

| 名称 | 在哪里 | 数量 | 本板用途 |
|---|---|---:|---|
| OPA2365 | MCU 外部、板子底部 | 芯片内含 2 个运放 | 驱动高速 ADC0.4，默认两级电压跟随器 |
| OPA0/OPA1 | MSPM0G3507 芯片内部 | 2 个 | 光传感器跨阻放大、缓冲、可编程增益等 |
| GPAMP | MSPM0G3507 芯片内部 | 1 个 | 热敏电阻缓冲/放大，也可探索电机电流检测 |

把这三者混在一起，是读这块板最容易犯的错误。

### 8.2 外置 OPA2365 → ADC0.4：高速 ADC 实验

默认信号路径为：

```text
P1（未焊同轴座）或底部模拟输入
    → OPA2365 U2A 电压跟随器
    → OPA2365 U2B 电压跟随器
    → R72 100 Ω
    → PB25 / ADC0.4
    → C47 82 pF 到地
```

两级默认增益都是 1，目的不是“把电压放大”，而是降低源阻抗、隔离信号源与 ADC 的开关电容采样网络，并给 4 MSPS 采样提供较强驱动。R72 与 C47 的理论极点约为：

```text
fc = 1 / (2πRC) ≈ 1 / (2π × 100 Ω × 82 pF) ≈ 19.4 MHz
```

它更像 ADC 驱动隔离/稳定网络，不是很低截止频率的抗混叠滤波器。真正抑制混叠时还要根据目标带宽重新设计前端滤波。

R68、R70 等为 DNC（默认不装）配置位，可改变反馈网络实现放大。若完全不用 OPA2365，原理图说明要去掉 R72、装上 R74 的 0 Ω旁路；这属于焊接改板，不是只改软件。

实验注意：

- J13 必须给模拟区供电；
- P1 同轴连接器默认未焊，可从底部引脚接入；
- 输入先从 0～3.3 V 范围内、低频、小幅度开始，禁止直接输入负压或超过 MCU/OPA 允许范围的信号；
- 想证明 4 MSPS，不能逐点 `printf`，应使用 ADC 定时触发 + DMA 缓冲，再分批传输数据；
- 采样正弦波时必须满足奈奎斯特条件，并给模拟抗混叠留余量。

推荐 SDK 示例起点：`adc12_single_conversion` → `adc12_max_freq_dma` → `adc12_simultaneous_trigger_event`。

### 8.3 TMP6131 → ADC0.5 或 GPAMP：温度实验

TMP6131 是 10 kΩ 线性 PTC，位于分压器低端；上端是 R56 = 10 kΩ、0.1%、10 ppm/°C，接 3.3VM：

```text
3.3VM ─ R56 10k ─┬─ V_Temperature ─ TMP6131 ─ GND
                  └─ J9
```

分压关系：

```text
Vtemp = VDD × RT / (R56 + RT)
RT = R56 × Vtemp / (VDD - Vtemp)
```

室温时输出约 1.6 V。最终温度不能只用一个固定线性系数猜，应使用 TMP6131 数据手册给出的阻值/电压与温度关系或多项式。

J9 的选择：

- J9 1-2：温度节点接 PB24 / ADC0.5，最适合第一次实验；
- J9 2-3：温度节点经 R57 接 PA26 / GPAMP 正输入，用于片内放大器实验；
- 不用时拔掉 J9，可释放相关引脚。

GPAMP 的负输入是 PA18，输出是 PA22。软件可以配置内部反馈做缓冲；R61、R66、R67、C46 等默认未装焊盘可用于外部偏置、增益和补偿网络。非反相放大器理想增益为：

```text
Av = 1 + Rf / Rg
```

单电源系统放大小信号时要给信号合适偏置，确保输入共模范围和输出摆幅没有撞到 0 V/3.3 V 电源轨。

手册正文和跳线表写的是 J9，原理图粉色注释误写成 J27；按板上实际的 J9 操作。

### 8.4 光电二极管 → 片内 OPA0 → ADC0.7：跨阻放大器实验

板上的 VEMD4110X01 光电二极管输出的是很小的电流，不能把它当普通电压传感器。片内 OPA0 组成跨阻放大器（TIA）：

- PA26：OPA0 正输入，通过 J18 接地；
- PA27：OPA0 负输入，通过 J17 接光电二极管和反馈网络；
- PA22：OPA0 输出，通过 J16 接 R1/C5 反馈，并可送 ADC0.7；
- R1：2.37 MΩ，决定光电流到输出电压的主要转换增益；
- C5：1 pF，改善反馈稳定性并限制高频噪声。

理想跨阻关系按光电二极管方向可写成：

```text
Vout ≈ Vbias ± Iphoto × Rf
```

这里正输入接近 0 V，实际输出方向与可用范围应以电路极性和实测为准。R1 很大，因此微小漏电、手触、板面污染和环境电磁干扰都会显著影响结果。

J16/J17/J18 默认安装。拔掉三者即可把光传感器电路从 PA22/PA27/PA26 隔离，释放这些引脚做其他 OPA、ADC、COMP 或 GPAMP 实验。

这是本板最完整的“传感器电流 → 片内模拟调理 → ADC 数字量”实验。建议依次观察暗处、室内灯、手机手电照射时 ADC 码的变化，再研究饱和、反馈电阻、带宽和噪声。

---

## 9. 其他模拟资源

### 9.1 DAC 输出

PA15 是 DAC_OUT，并连接到 BoosterPack 第 30 脚。板上预留 R55/C45 作为 DAC 输出 RC 滤波，默认不装。DAC 可以用于：

- 产生直流偏置；
- 通过定时器/DMA 更新产生低频波形；
- 给 OPA/PGA 提供虚拟地；
- 与 ADC 做回环线性度实验。

RC 截止频率仍按 `fc = 1/(2πRC)` 计算。电容越大不一定越好，它会降低波形带宽并增加 DAC 驱动负担。

### 9.2 ADC 输入 RC 焊盘

原理图给 PB18/ADC1.5、PB19/ADC1.6、PA24/ADC0.3、PA17/ADC1.2、PA22/ADC0.7 预留 RC 网络，默认不完整装配。它们适合做噪声、采样保持和抗混叠实验，但改值前要同时考虑：

- 信号源阻抗；
- ADC 采样时间；
- 目标带宽；
- ADC 内部采样电容的充电时间；
- OPA 稳定性和负载能力。

### 9.3 参考电压脚

PA23/VREF+、PA21/VREF- 在原理图中有隔离电阻焊盘。参考电压直接决定 ADC/DAC 标度与噪声，不能把它们当普通 GPIO 随便使用。做高精度实验时，应先看数据手册/TRM 的参考源选择、去耦和允许范围。

---

## 10. BoosterPack 40 针接口

手册第 15 页给出 40 针标准接口。下面按接口脚号整理主要功能：

| 脚号 | 默认信号 | 脚号 | 默认信号 |
|---:|---|---:|---|
| 1 | 3V3 | 21 | 5V |
| 2 | PA25 / ADC0.2 | 22 | GND |
| 3 | PB23 或 PA9 / UART_RX，由 J14 选择 | 23 | PB19 / ADC1.6 |
| 4 | PA8 / UART_TX | 24 | PA22 / ADC0.7 |
| 5 | PA26 / 模拟输入 | 25 | PB18 / ADC1.5 |
| 6 | PB24 / ADC0.5 | 26 | PA18 / ADC1.3 |
| 7 | PB9 / SPI_CLK | 27 | PA24 / ADC0.3 |
| 8 | PA27 / 模拟输入 | 28 | PA17 / ADC1.2 |
| 9 | PB2 / I2C_SCL | 29 | PA16 或 PA18，由 J15 选择 |
| 10 | PB3 / I2C_SDA | 30 | PA15 / DAC_OUT |
| 11 | PB16 | 31 | PA13 / CAN_RX |
| 12 | PB0 / SPI_CS | 32 | PA12 / CAN_TX |
| 13 | PB6 / SPI_CS | 33 | PA11 / LIN_RX（UART 复用） |
| 14 | PB7 / SPI_POCI（MISO） | 34 | PA10 / LIN_TX（UART 复用） |
| 15 | PB8 / SPI_PICO（MOSI） | 35 | PB13 / PWM |
| 16 | NRST | 36 | PB20 / PWM |
| 17 | PB15 | 37 | PA31 / PWM |
| 18 | PB17 / SPI_CS | 38 | PA28 / PWM |
| 19 | PB18 | 39 | PB1 / PWM |
| 20 | GND | 40 | PB4 / PWM |

`PICO/POCI` 是 TI 对主从命名的替代：PICO 相当于控制器输出/外设输入，POCI 相当于外设输出/控制器输入。

接 BoosterPack 前必须核对：电源电压、每个引脚的复用、J14/J15/J21/J22 位置，以及是否和 LED、按键、传感器、SWD 或底部跳线冲突。所谓“40 针兼容”只保证机械和推荐接口标准，不能保证任意模块插上就没有资源冲突。

特别注意：PA12/PA13 是 MCU 的 CAN 逻辑 TX/RX，不是 CANH/CANL。要接实际 CAN 总线还需要 3.3 V 逻辑兼容的 CAN/CAN-FD 收发器、正确终端电阻和共地/隔离设计。

---

## 11. QEI、电机控制与底部扩展

J12 QEI 接口包含：

- PA29 / TIMG10_C0：编码器 A 相候选；
- PA30 / TIMG10_C1：编码器 B 相候选；
- PB14 / TIMG10_IDX：索引 Z 相候选；
- 3V3、GND。

QEI 用于正交编码器位置、速度和方向检测。接编码器前确认输出是推挽、开漏还是差分；差分 RS-422 编码器不能直接接 MCU，需要接收器。长线也需要考虑滤波、施密特触发、屏蔽和地线。

BoosterPack 还引出了 6 路 PWM 候选、三组模拟量和 CAN/UART/SPI，原理图注释明确考虑了电机相电压、相电流、霍尔和 1/2/3 电阻电流采样方案。不过这只是接口规划，不代表板上已有栅极驱动器、功率 MOSFET、电流采样电阻或隔离。驱动电机必须再接合适的功率板。

底部 J23～J28 把部分不方便从标准 40 针取得的信号引出，做实验前以第 19 页原理图的实际网络名为准。

---

## 12. 读懂跳线表

| 跳线 | 默认作用 | 什么时候拔/改 |
|---|---|---|
| J4 | PA0 接红色 LED | 释放 PA0 或降低功耗 |
| J5/J6/J7 | PB22/PB26/PB27 接 RGB 蓝/红/绿 | 释放引脚或降低功耗 |
| J8 | PA18 接 S1/BSL | 使用 PA18 模拟功能、降低下拉电流影响 |
| J9 | TMP6131 默认接 PB24/ADC0.5 | 改接 GPAMP 或隔离温度电路 |
| J13 | 给温度传感器与 OPA2365 模拟区供电 | 测 MCU 低功耗且不用模拟区时拔掉 |
| J14 | BoosterPack 第 3 脚在 PB23/PA9 间选择 | 适配 UART_RX 或其他复用 |
| J15 | BoosterPack 第 29 脚在 PA16/PA18 间选择 | 适配 OPA1/ADC/电机电流采样 |
| J16/J17/J18 | PA22/PA27/PA26 接光传感器 TIA | 不用光传感器或重用 OPA0 引脚时拔掉 |
| J19/J20 | PA0/PA1 开漏上拉电压选择 | 更换 3.3/5 V 上拉或完全隔离 |
| J21/J22 | PA10/PA11 UART0 连接 XDS 或 BoosterPack | USB 串口与扩展口切换 |

容易被板载电路占用的特殊脚包括 PA2/ROSC、PA18、PA21/VREF-、PA23/VREF+，以及默认 USB 串口 PA10/PA11。做普通 GPIO 分配时应优先避开；确实要用时先检查原理图和跳线。

---

## 13. 按这个顺序做 12 个实验

| 阶段 | 实验 | 目标 |
|---:|---|---|
| 1 | 出厂 OoBE | 确认 USB、驱动、下载器、板载传感器都正常 |
| 2 | PA0 红灯 + PB21 按键中断 | 学 GPIO、上下拉、中断、消抖和有效电平 |
| 3 | UART0 echo | 学 J21/J22、虚拟 COM 口、波特率和串口调试 |
| 4 | RGB 三路 PWM | 学定时器、占空比、颜色混合和渐变 |
| 5 | TMP6131 直连 ADC0.5 | 学参考电压、采样时间、平均、分压与温度换算 |
| 6 | TMP6131 经 GPAMP | 比较直接采样、缓冲和增益模式的噪声/源阻抗影响 |
| 7 | 光传感器 OPA0 TIA | 学跨阻、反馈电阻/电容、饱和、暗电流和 ADC 内部路由 |
| 8 | 外置 OPA2365 + ADC0.4 | 学高速 ADC 驱动、定时触发、DMA、频谱和混叠 |
| 9 | DAC → ADC 回环 | 学 DAC 标度、波形表、定时/DMA 更新和 ADC 校验 |
| 10 | QEI 编码器 | 学捕获、正交计数、方向、速度和溢出处理 |
| 11 | I2C/SPI/CAN | 先传感器总线，再配 CAN 收发器做 CAN-FD |
| 12 | SLEEP/STOP + EnergyTrace | 对比时钟门控、外设、跳线和浮空脚对功耗的影响 |

每个实验都应留下四样东西：`接线/跳线表`、`.syscfg`、`核心代码`、`实测波形或串口数据`。这样你学到的是可复用能力，而不是只把例程跑亮。

---

## 14. 片内 OPA/ADC 的推荐例程路线

在 MSPM0 SDK 示例索引中优先找这些名称：

### ADC

1. `adc12_single_conversion`
2. `adc12_sequence_conversion`
3. `adc12_max_freq_dma`
4. `adc12_14bit_resolution`（硬件平均提升有效分辨率，不等于 ADC 物理上变成理想 14 位）
5. `adc12_simultaneous_trigger_event`

### OPA

1. `opa_general_purpose_rri`
2. `opa_non_inverting_pga`
3. `opa_inverting_pga_with_dac`
4. `opa_dac8_output_buffer`
5. `opa_signal_chain_to_adc`

把例程导入后，先读 `.syscfg` 中的模块、引脚、内部模拟连接和时钟，再读 `ti_msp_dl_config.h` 中实际生成的宏，最后才看 `main.c`。对模拟外设来说，很多关键连接发生在芯片内部模拟开关矩阵里，仅看 C 代码往往看不全。

---

## 15. 原理图中的几个坑

1. 第 14 页测电流段落把 MCU 写成 MSP430FR2355，应为 MSPM0G3507。
2. 第 12/19 页温度电路注释写 J27，但跳线表和实际板上选择器是 J9。
3. 第 17 页表头出现 “Document For MSPM0L1306”，内容实际在讲 G 系列/本芯片资料层级。
4. 第 4 页说三个主示例，却列出四项功能。
5. 原理图红叉通常表示 DNC/未装元件，不代表短路或损坏；“0 Ω”是已设计的跳接/配置电阻，仍要结合 BOM 判断是否实装。
6. `ADC0.4` 是 ADC0 的通道 4，不是 0.4 V；`4 MSPS` 是每秒最多四百万次采样，不是 4 MHz 输入信号一定能无失真采集。

---

## 16. 最终应达到的“学透”标准

你真正掌握这块板，不是记住所有引脚，而是能做到：

- 看原理图判断一个板载资源占用了哪些引脚和跳线；
- 用 SysConfig 正确配置时钟、引脚、外设、DMA 和中断；
- 能解释 OPA 缓冲、PGA、TIA、GPAMP 与 ADC 之间的信号路径；
- 能算 ADC 标度、分压、运放增益、跨阻增益和 RC 截止频率；
- 能用示波器/逻辑分析仪/串口数据证明系统真的按预期工作；
- 遇到异常时能区分软件配置、引脚冲突、供电、参考电压、模拟饱和、带宽、噪声和硬件接线问题；
- 能从 SDK 示例出发独立做出温度采集、光照采集、高速 ADC、PWM、编码器和 CAN-FD 小项目。

这份板卡手册解决的是“板上资源与连接关系”。下一步最值得深入的章节是 TRM 的 Clock、GPIO/IOMUX、ADC12、OPA、GPAMP、VREF、DMA、Timer 和 Event Fabric，并配合数据手册检查每个模拟模块的绝对最大值和电气指标。
