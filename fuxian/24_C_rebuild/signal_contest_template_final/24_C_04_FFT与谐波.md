# 24_C-04 FFT 与谐波

FFT 使用 CMSIS-DSP Q15 CFFT 示例；谐波和多 bin 能量分别复制 `harmonic`、`multi_bin_energy` README 的 Process 调用。屏幕保持 H1-H5 的频率和峰值幅度。

自写代码逐行解释：Hann 窗行降低频谱泄漏；幅值数组行保存 0 到 Nyquist；峰值搜索行跳过 DC；抛物线插值行修正整数 bin；`AnalyzeHarmonics` 行把基波频率交给模块；最后循环仅把 1-5 次结果复制到显示结构体。
