# 24_C-07 验证与赛场复现

赛场顺序：先按 01 文档复制模块并配置 SysConfig；只接 ADC 验证双路 DMA；再接 PB2 capture；加入 FFT/谐波；最后接 ST7789。每完成一步都 Refresh、Clean/Build。屏幕应保持原来的 TYPE、F、D、Fs、MAX/MIN/VPP/RMS、H1-H5 和 BURST 内容。

源码检查要求：工程中不得出现 ILI9341 API；`modules` 下每个 `.c` 都参与编译；生成文件不手改；库模块与复制件校验一致。当前实板验证为 `BOARD NOT_RUN`，需要开发板、信号源和 ST7789 完成最终确认。
