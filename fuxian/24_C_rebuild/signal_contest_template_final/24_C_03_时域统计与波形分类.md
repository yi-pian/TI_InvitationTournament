# 24_C-03 时域统计与波形分类

ADC code 转电压、CMSIS 去直流、min/max/Vpp/RMS 的调用从集成库通用示例复制。main 只保留题目的分类门限。

自写分类代码逐行解释：先用 `MIN_SIGNAL_VPP_V` 判断无信号；再比较谐波和波形斜率判断正弦、锯齿、脉冲或复杂波；每个分支只返回枚举；`WaveformName` 再把枚举映射成屏幕字符串。这些行只定义赛题规则，不重写采样和 FFT。
