# 22_X：工程模块与函数参数详解

> 工程定位：PLL/Y 通道倍频控制、双 ADC 相位测量和 ILI9341 李萨如显示。
> 本文由当前源码静态生成，行号均指向函数定义或调用的起始行；修改源码后请重新运行生成器。

## 1. 工程功能与主数据流

GPIO 控制 PLL 倍频、Y 通道增益和波形模式；双 ADC 同步采集后计算相位，并在 ILI9341 上绘制数值与李萨如轨迹。

### 统计与阅读边界

- 工程内函数定义：**91** 个，其中 `main.c` 15 个、`modules` 76 个。
- 软件模块文件族：**7** 个；SysConfig 实例/单例记录：**12** 条。
- 函数目录覆盖 `main.c`、`modules/*.c` 和 `modules/*.h` 中带函数体的内联函数；不重复收录纯声明、宏、`Debug/Release` 生成副本和 TI/CMSIS 外部库函数体。
- 参数说明优先引用中文 `@param`；无注释时根据声明类型、命名、所属模块及实现语义整理，并在“说明依据”列标出。
- `[in/out]` 表示指针指向的对象可能被读取并更新。实际数组长度、单位和合法范围仍应以相邻参数、配置宏和函数体检查为准。

## 2. SysConfig 硬件/SDK 模块

这些名称来自 `signal_contest_template.syscfg`。迁移硬件函数时，必须同步迁移实例名、物理外设、DMA 通道、事件、IRQ 和引脚。

| DriverLib 模块 | SysConfig 变量 / 逻辑实例名 | 物理外设 | 关键配置摘要 |
|---|---|---|---|
| `ADC12` | `ADC121` / `SIGNAL_ADC_A` | `ADC0` | adcMem0chansel="DL_ADC12_INPUT_CHAN_2"；trigSrc="DL_ADC12_TRIG_SRC_EVENT"；configureDMA=true；enabledInterrupts=["DL_ADC12_INTERRUPT_DMA_DONE"]；sampClkSrc="DL_ADC12_CLOCK_ULPCLK"；DMA_CHANNEL.$name="SIGNAL_ADC_A_DMA"；DMA_CHANNEL.peripheral.$assign="DMA_CH0"；引脚：peripheral.adcPin2.$assign=PA25 |
| `ADC12` | `ADC122` / `SIGNAL_ADC_B` | `ADC1` | adcMem0chansel="DL_ADC12_INPUT_CHAN_2"；trigSrc="DL_ADC12_TRIG_SRC_EVENT"；configureDMA=true；enabledInterrupts=["DL_ADC12_INTERRUPT_DMA_DONE"]；sampClkSrc="DL_ADC12_CLOCK_ULPCLK"；DMA_CHANNEL.$name="SIGNAL_ADC_B_DMA"；DMA_CHANNEL.peripheral.$assign="DMA_CH1"；引脚：peripheral.adcPin2.$assign=PA17 |
| `Board` | `Board` / `Board` | `—` | 使用 SysConfig/SDK 默认值或仅参与全局配置 |
| `GPIO` | `GPIO2` / `GPIO_KEYPAD` | `—` | 引脚：KEYPAD_R1=PORTB16，KEYPAD_R2=PORTB0，KEYPAD_R3=PORTB7，KEYPAD_R4=PORTB17，KEYPAD_C1=PORTB18(INPUT/PULL_UP)，KEYPAD_C2=PORTB13(INPUT/PULL_UP)，KEYPAD_C3=PORTB20(INPUT/PULL_UP)，KEYPAD_C4=PORTB4(INPUT/PULL_UP) |
| `GPIO` | `GPIO3` / `GPIO_PLL_CTRL` | `—` | 引脚：PLL_A=PORTA10，PLL_B=PORTA11，PLL_C=PORTA12，PLL_D=PORTA13 |
| `GPIO` | `GPIO1` / `GPIO_TFT_CTRL` | `—` | 引脚：TFT_DC=PORTB15，TFT_BLK=PORTB12 |
| `GPIO` | `GPIO5` / `GPIO_WAVE_CTRL` | `—` | 引脚：WAVE_SELECT=PORTA15 |
| `GPIO` | `GPIO4` / `GPIO_YV_CTRL` | `—` | 引脚：YV_A=PORTA24，YV_B=PORTA18 |
| `ProjectConfig` | `ProjectConfig` / `ProjectConfig` | `—` | 使用 SysConfig/SDK 默认值或仅参与全局配置 |
| `SPI` | `SPI1` / `SPI_TFT` | `SPI1` | 引脚：peripheral.sclkPin.$assign=PB9，peripheral.mosiPin.$assign=PB8，peripheral.cs0Pin.$assign=PB6 |
| `SYSCTL` | `SYSCTL` / `SYSCTL` | `—` | 使用 SysConfig/SDK 默认值或仅参与全局配置 |
| `TIMER` | `TIMER1` / `SIGNAL_DUAL_ADC_TIMER` | `TIMG0` | timerPeriod="10us"；timerMode="PERIODIC" |

## 3. 软件模块清单

“main 直接调用”只统计 `main.c` 的显式调用；0 次不等于无用，它可能由其他模块调用、作为 ISR/回调入口，或仅提供类型与配置。

| 模块文件族 | 功能分类 | 模块职责 | 函数定义数 | main 直接调用的接口 | 构建状态 |
|---|---|---|---:|---|---|
| `modules/signal_algorithm_status.h` | 数学、状态与内部公共支持 | 算法状态码公共定义 | 0 | — | 随工程编译/头文件依赖 |
| `modules/signal_dual_adc_mspm0g3507.c`、`modules/signal_dual_adc_mspm0g3507.h` | ADC、DMA、连续采集与缓冲 | MSPM0G3507 双 ADC 同定时器事件同步 DMA 采集 | 20 | `SignalDualADC_Init`、`SignalDualADC_IsFinished`、`SignalDualADC_Start` | 随工程编译/头文件依赖 |
| `modules/signal_dual_adc_phase.c`、`modules/signal_dual_adc_phase.h` | 频率、相位与同步检波 | 双路原始 ADC 动态中点、滞回过零及平均相位差 | 5 | `SignalDualADCPhase_Process` | 随工程编译/头文件依赖 |
| `modules/signal_matrix_keypad_4x4.c`、`modules/signal_matrix_keypad_4x4.h` | 矩阵键盘与交互输入 | 4×4 矩阵键盘行列扫描、消抖、鬼键过滤和符号映射 | 11 | `SignalMatrixKeypad4x4_ReadNewSymbol` | 随工程编译/头文件依赖 |
| `modules/signal_status.h` | 数学、状态与内部公共支持 | 驱动/模块统一状态码 | 0 | — | 随工程编译/头文件依赖 |
| `modules/signal_tft_ili9341.c`、`modules/signal_tft_ili9341.h` | TFT、字库、波形与界面绘制 | ILI9341 SPI 屏驱动、图元和文字输出 | 35 | `TFT_ILI9341_DrawInt32`、`TFT_ILI9341_DrawLine`、`TFT_ILI9341_DrawRect`、`TFT_ILI9341_DrawString`、`TFT_ILI9341_FillRect`、`TFT_ILI9341_FillScreen` | 随工程编译/头文件依赖 |
| `modules/signal_tft_ili9341_mspm0g3507.c`、`modules/signal_tft_ili9341_mspm0g3507.h` | TFT、字库、波形与界面绘制 | ILI9341 到 MSPM0 SPI/GPIO 的平台适配 | 5 | `SignalTFTILI9341_MSPM0_Init` | 随工程编译/头文件依赖 |

## 4. main.c 实际使用的模块接口

下表回答“这个工程实际调用了模块里的哪些函数”。调用位置给出当前实参；完整形参、方向和意义见后面的函数目录。

| 模块接口 | 定义文件 | 作用 | main.c 调用位置与当前实参 |
|---|---|---|---|
| `SignalDualADCPhase_Process` | `modules/signal_dual_adc_phase.c` | 从一帧双路同步 ADC 原始码计算 Y 相对 X 的相位差。 | [main.c:L420](main.c#L420)：`SignalDualADCPhase_Process(g_raw_a, g_raw_b, SIGNAL_SAMPLE_COUNT, SIGNAL_SAMPLE_RATE_HZ, &g_phase_config, &phase_result)` |
| `SignalDualADC_Init` | `modules/signal_dual_adc_mspm0g3507.c` | 公开接口（MSPM0G3507 双 ADC 同定时器事件同步 DMA 采集）：初始化模块对象、硬件绑定和运行状态。 | [main.c:L353](main.c#L353)：`SignalDualADC_Init(&config)` |
| `SignalDualADC_IsFinished` | `modules/signal_dual_adc_mspm0g3507.c` | 公开接口（MSPM0G3507 双 ADC 同定时器事件同步 DMA 采集）：查询本次 DMA/算法处理是否完成。 | [main.c:L410](main.c#L410)：`SignalDualADC_IsFinished()` |
| `SignalDualADC_Start` | `modules/signal_dual_adc_mspm0g3507.c` | 公开接口（MSPM0G3507 双 ADC 同定时器事件同步 DMA 采集）：启动一次硬件搬运、采集或输出操作。 | [main.c:L404](main.c#L404)：`SignalDualADC_Start(g_raw_a, g_raw_b, SIGNAL_SAMPLE_COUNT)` |
| `SignalMatrixKeypad4x4_ReadNewSymbol` | `modules/signal_matrix_keypad_4x4.c` | 从键盘事件队列读取一次新按下事件并映射为字符；没有新事件时返回 false。 | [main.c:L198](main.c#L198)：`SignalMatrixKeypad4x4_ReadNewSymbol(&symbol)` |
| `SignalTFTILI9341_MSPM0_Init` | `modules/signal_tft_ili9341_mspm0g3507.c` | 公开接口（ILI9341 到 MSPM0 SPI/GPIO 的平台适配）：初始化模块对象、硬件绑定和运行状态。 | [main.c:L361](main.c#L361)：`SignalTFTILI9341_MSPM0_Init(&g_tft, TFT_ILI9341_ROTATION_270)` |
| `TFT_ILI9341_DrawInt32` | `modules/signal_tft_ili9341.c` | 作为模块公开接口，绘制整数；具体输入、输出和量纲见下方参数及返回值说明。 | [main.c:L214](main.c#L214)：`TFT_ILI9341_DrawInt32(&g_tft, 258, 30, multiplier, TFT_ILI9341_FONT_8X16, TFT_ILI9341_WHITE, TFT_ILI9341_BLACK, false)`<br>[main.c:L228](main.c#L228)：`TFT_ILI9341_DrawInt32(&g_tft, 258, 60, multiplier, TFT_ILI9341_FONT_8X16, TFT_ILI9341_WHITE, TFT_ILI9341_BLACK, false)`<br>[main.c:L265](main.c#L265)：`TFT_ILI9341_DrawInt32(&g_tft, 234, 120, phase_degrees, TFT_ILI9341_FONT_8X16, TFT_ILI9341_WHITE, TFT_ILI9341_BLACK, false)` |
| `TFT_ILI9341_DrawLine` | `modules/signal_tft_ili9341.c` | 作为模块公开接口，绘制线段/曲线；具体输入、输出和量纲见下方参数及返回值说明。 | [main.c:L322](main.c#L322)：`TFT_ILI9341_DrawLine(&g_tft, Lissajous_MapX(g_raw_a[index0]), Lissajous_MapY(g_raw_b[index0]), Lissajous_MapX(g_raw_a[index1]), Lissajous_MapY(g_raw_b[index1]), TFT_ILI9341_YELLOW)` |
| `TFT_ILI9341_DrawRect` | `modules/signal_tft_ili9341.c` | 作为模块公开接口，绘制模块专用状态和数据；具体输入、输出和量纲见下方参数及返回值说明。 | [main.c:L297](main.c#L297)：`TFT_ILI9341_DrawRect(&g_tft, LISSAJOUS_PLOT_X, LISSAJOUS_PLOT_Y, LISSAJOUS_PLOT_WIDTH, LISSAJOUS_PLOT_HEIGHT, TFT_ILI9341_BLUE)` |
| `TFT_ILI9341_DrawString` | `modules/signal_tft_ili9341.c` | 作为模块公开接口，绘制模块专用状态和数据；具体输入、输出和量纲见下方参数及返回值说明。 | [main.c:L243](main.c#L243)：`TFT_ILI9341_DrawString(&g_tft, 234, 90, (mode == 0U) ? "SIN" : "TRI", TFT_ILI9341_FONT_8X16, TFT_ILI9341_WHITE, TFT_ILI9341_BLACK, false, false)`<br>[main.c:L260](main.c#L260)：`TFT_ILI9341_DrawString(&g_tft, 234, 120, "----", TFT_ILI9341_FONT_8X16, TFT_ILI9341_WHITE, TFT_ILI9341_BLACK, false, false)`<br>[main.c:L366](main.c#L366)：`TFT_ILI9341_DrawString(&g_tft, 8, 8, "LiSaRu", TFT_ILI9341_FONT_8X16, TFT_ILI9341_WHITE, TFT_ILI9341_BLACK, false, false)`<br>[main.c:L370](main.c#L370)：`TFT_ILI9341_DrawString(&g_tft, 210, 30, "PLL:", TFT_ILI9341_FONT_8X16, TFT_ILI9341_WHITE, TFT_ILI9341_BLACK, false, false)`<br>[main.c:L374](main.c#L374)：`TFT_ILI9341_DrawString(&g_tft, 210, 60, "YV:", TFT_ILI9341_FONT_8X16, TFT_ILI9341_WHITE, TFT_ILI9341_BLACK, false, false)`<br>[main.c:L378](main.c#L378)：`TFT_ILI9341_DrawString(&g_tft, 210, 90, "WV:", TFT_ILI9341_FONT_8X16, TFT_ILI9341_WHITE, TFT_ILI9341_BLACK, false, false)`<br>[main.c:L382](main.c#L382)：`TFT_ILI9341_DrawString(&g_tft, 210, 120, "PH:", TFT_ILI9341_FONT_8X16, TFT_ILI9341_WHITE, TFT_ILI9341_BLACK, false, false)` |
| `TFT_ILI9341_FillRect` | `modules/signal_tft_ili9341.c` | 公开接口（ILI9341 SPI 屏驱动、图元和文字输出）：连续产生样本并填满调用者提供的输出缓冲区。 | [main.c:L211](main.c#L211)：`TFT_ILI9341_FillRect(&g_tft, 258, 30, 8, 16, TFT_ILI9341_BLACK)`<br>[main.c:L225](main.c#L225)：`TFT_ILI9341_FillRect(&g_tft, 258, 60, 8, 16, TFT_ILI9341_BLACK)`<br>[main.c:L239](main.c#L239)：`TFT_ILI9341_FillRect(&g_tft, 234, 90, 48, 16, TFT_ILI9341_BLACK)`<br>[main.c:L255](main.c#L255)：`TFT_ILI9341_FillRect(&g_tft, 234, 120, 48, 16, TFT_ILI9341_BLACK)`<br>[main.c:L291](main.c#L291)：`TFT_ILI9341_FillRect(&g_tft, LISSAJOUS_PLOT_X, LISSAJOUS_PLOT_Y, LISSAJOUS_PLOT_WIDTH, LISSAJOUS_PLOT_HEIGHT, TFT_ILI9341_BLACK)`<br>[main.c:L311](main.c#L311)：`TFT_ILI9341_FillRect(&g_tft, LISSAJOUS_INNER_X, LISSAJOUS_INNER_Y, LISSAJOUS_INNER_WIDTH, LISSAJOUS_INNER_HEIGHT, TFT_ILI9341_BLACK)` |
| `TFT_ILI9341_FillScreen` | `modules/signal_tft_ili9341.c` | 公开接口（ILI9341 SPI 屏驱动、图元和文字输出）：连续产生样本并填满调用者提供的输出缓冲区。 | [main.c:L364](main.c#L364)：`TFT_ILI9341_FillScreen(&g_tft, TFT_ILI9341_BLACK)` |

## 5. main.c 直接调用的 SDK / CMSIS-DSP / C 数学接口

这里只记录工程源码直接调用的外部库函数；其函数体属于 TI SDK、CMSIS 或 C 运行库，不计入本工程函数定义总数。实参按当前源码原样列出，迁移时要同时核对目标 SDK 版本的原型。

### `DL_GPIO_clearPins`

- 作用：把 GPIO 端口中掩码指定的引脚置低。
- 形参与意义：

| 形参 | 方向 | 意义 |
|---|---|---|
| `port` | [in] | GPIO 端口基地址。 |
| `pins` | [in] | 要置低的引脚位掩码。 |
- 返回：无返回值。
- 当前工程调用：[main.c:L111](main.c#L111)：`DL_GPIO_clearPins(GPIO_PLL_CTRL_PORT, all_pins & ~set_pins)`；[main.c:L136](main.c#L136)：`DL_GPIO_clearPins(GPIO_YV_CTRL_PORT, all_pins & ~set_pins)`；[main.c:L148](main.c#L148)：`DL_GPIO_clearPins(GPIO_WAVE_CTRL_PORT, GPIO_WAVE_CTRL_WAVE_SELECT_PIN)`

### `DL_GPIO_setPins`

- 作用：把 GPIO 端口中掩码指定的引脚置高。
- 形参与意义：

| 形参 | 方向 | 意义 |
|---|---|---|
| `port` | [in] | GPIO 端口基地址。 |
| `pins` | [in] | 要置高的引脚位掩码，可按位或组合。 |
- 返回：无返回值。
- 当前工程调用：[main.c:L112](main.c#L112)：`DL_GPIO_setPins(GPIO_PLL_CTRL_PORT, set_pins)`；[main.c:L137](main.c#L137)：`DL_GPIO_setPins(GPIO_YV_CTRL_PORT, set_pins)`；[main.c:L152](main.c#L152)：`DL_GPIO_setPins(GPIO_WAVE_CTRL_PORT, GPIO_WAVE_CTRL_WAVE_SELECT_PIN)`

### `SYSCFG_DL_init`

- 作用：执行 SysConfig 生成的整板初始化，配置时钟、引脚和外设实例。
- 参数：无。
- 返回：无返回值。
- 当前工程调用：[main.c:L348](main.c#L348)：`SYSCFG_DL_init()`

### `SysTick_Config`

- 作用：设置 Cortex-M SysTick 重装值并开启 SysTick 中断。
- 形参与意义：

| 形参 | 方向 | 意义 |
|---|---|---|
| `ticks` | [in] | 每次中断对应的内核时钟 tick 数；通常传 SystemCoreClock/1000 得到 1 ms。 |
- 返回：0 表示配置成功，非 0 表示重装值超出 24 位范围。
- 当前工程调用：[main.c:L399](main.c#L399)：`SysTick_Config(CPUCLK_FREQ / 1000U)`

### `__WFI`

- 作用：执行 Wait For Interrupt，使内核休眠到中断到来。
- 参数：无。
- 返回：无返回值。
- 当前工程调用：[main.c:L410](main.c#L410)：`__WFI()`

## 6. 全部函数定义、作用、返回值与参数

本节逐个列出本工程源码中的所有函数定义。公开接口适合跨文件调用；`static` 函数通常依赖同一 `.c` 的静态状态，复用时优先整体复制所属模块。

### 文件导航

- [`main.c`：15 个](#file-main-c)
- [`modules/signal_dual_adc_mspm0g3507.c`：20 个](#file-modules-signal_dual_adc_mspm0g3507-c)
- [`modules/signal_dual_adc_phase.c`：5 个](#file-modules-signal_dual_adc_phase-c)
- [`modules/signal_matrix_keypad_4x4.c`：11 个](#file-modules-signal_matrix_keypad_4x4-c)
- [`modules/signal_tft_ili9341.c`：35 个](#file-modules-signal_tft_ili9341-c)
- [`modules/signal_tft_ili9341_mspm0g3507.c`：5 个](#file-modules-signal_tft_ili9341_mspm0g3507-c)

<a id="file-main-c"></a>

## `main.c`

<a id="fn-main-c-App_SetPLLMultiplier"></a>

### `App_SetPLLMultiplier`

- 定义位置：[main.c:L83](main.c#L83)
- 属性：文件内部 `static`；功能分类：工程入口、调度与中断。
- 所属模块：PLL/Y 通道倍频控制、双 ADC 相位测量和 ILI9341 李萨如显示。
- 具体作用：设置 1–9 倍 PLL GPIO 编码，并同步相位算法的 fY/fX。
- 函数签名：

```c
static void App_SetPLLMultiplier(uint8_t multiplier)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `multiplier` | `uint8_t` | [in] | 倍频、增益或显示倍率。 | 签名/实现语义 |

<a id="fn-main-c-App_SetYVMultiplier"></a>

### `App_SetYVMultiplier`

- 定义位置：[main.c:L118](main.c#L118)
- 属性：文件内部 `static`；功能分类：工程入口、调度与中断。
- 所属模块：PLL/Y 通道倍频控制、双 ADC 相位测量和 ILI9341 李萨如显示。
- 具体作用：设置 Y 通道量程/增益控制 GPIO。
- 函数签名：

```c
static void App_SetYVMultiplier(uint8_t multiplier)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `multiplier` | `uint8_t` | [in] | 倍频、增益或显示倍率。 | 签名/实现语义 |

<a id="fn-main-c-App_SetWaveMode"></a>

### `App_SetWaveMode`

- 定义位置：[main.c:L143](main.c#L143)
- 属性：文件内部 `static`；功能分类：工程入口、调度与中断。
- 所属模块：PLL/Y 通道倍频控制、双 ADC 相位测量和 ILI9341 李萨如显示。
- 具体作用：切换外部波形控制 GPIO 状态。
- 函数签名：

```c
static void App_SetWaveMode(uint8_t mode)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `mode` | `uint8_t` | [in] | 工作模式枚举，决定采集、显示或算法分支。 | 签名/实现语义 |

<a id="fn-main-c-App_ProcessPLLKey"></a>

### `App_ProcessPLLKey`

- 定义位置：[main.c:L162](main.c#L162)
- 属性：文件内部 `static`；功能分类：矩阵键盘与交互输入。
- 所属模块：PLL/Y 通道倍频控制、双 ADC 相位测量和 ILI9341 李萨如显示。
- 具体作用：应用层（PLL/Y 通道倍频控制、双 ADC 相位测量和 ILI9341 李萨如显示）：对输入数组执行该模块的核心算法并写入结果结构。
- 函数签名：

```c
static void App_ProcessPLLKey(char symbol)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `symbol` | `char` | [in] | 矩阵键盘映射得到的符号字符。 | 签名/实现语义 |

<a id="fn-main-c-App_ProcessYVKey"></a>

### `App_ProcessYVKey`

- 定义位置：[main.c:L170](main.c#L170)
- 属性：文件内部 `static`；功能分类：矩阵键盘与交互输入。
- 所属模块：PLL/Y 通道倍频控制、双 ADC 相位测量和 ILI9341 李萨如显示。
- 具体作用：应用层（PLL/Y 通道倍频控制、双 ADC 相位测量和 ILI9341 李萨如显示）：对输入数组执行该模块的核心算法并写入结果结构。
- 函数签名：

```c
static void App_ProcessYVKey(char symbol)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `symbol` | `char` | [in] | 矩阵键盘映射得到的符号字符。 | 签名/实现语义 |

<a id="fn-main-c-SysTick_Handler"></a>

### `SysTick_Handler`

- 定义位置：[main.c:L188](main.c#L188)
- 属性：ISR（由硬件/内核调用）；功能分类：工程入口、调度与中断。
- 所属模块：PLL/Y 通道倍频控制、双 ADC 相位测量和 ILI9341 李萨如显示。
- 具体作用：应用层（PLL/Y 通道倍频控制、双 ADC 相位测量和 ILI9341 李萨如显示）：SysTick 中断：提供 1 ms 时基并驱动键盘周期扫描/事件入队。
- 函数签名：

```c
void SysTick_Handler(void)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数：无（`void`）。

<a id="fn-main-c-App_DrawPLLMultiplier"></a>

### `App_DrawPLLMultiplier`

- 定义位置：[main.c:L206](main.c#L206)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：PLL/Y 通道倍频控制、双 ADC 相位测量和 ILI9341 李萨如显示。
- 具体作用：作为文件内部辅助函数，绘制模块专用状态和数据；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
static tft_ili9341_status_t App_DrawPLLMultiplier(uint8_t multiplier)
```

- 返回值（`tft_ili9341_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `multiplier` | `uint8_t` | [in] | 倍频、增益或显示倍率。 | 签名/实现语义 |

<a id="fn-main-c-App_DrawYVMultiplier"></a>

### `App_DrawYVMultiplier`

- 定义位置：[main.c:L220](main.c#L220)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：PLL/Y 通道倍频控制、双 ADC 相位测量和 ILI9341 李萨如显示。
- 具体作用：作为文件内部辅助函数，绘制模块专用状态和数据；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
static tft_ili9341_status_t App_DrawYVMultiplier(uint8_t multiplier)
```

- 返回值（`tft_ili9341_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `multiplier` | `uint8_t` | [in] | 倍频、增益或显示倍率。 | 签名/实现语义 |

<a id="fn-main-c-App_DrawWaveMode"></a>

### `App_DrawWaveMode`

- 定义位置：[main.c:L234](main.c#L234)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：PLL/Y 通道倍频控制、双 ADC 相位测量和 ILI9341 李萨如显示。
- 具体作用：作为文件内部辅助函数，绘制波形；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
static tft_ili9341_status_t App_DrawWaveMode(uint8_t mode)
```

- 返回值（`tft_ili9341_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `mode` | `uint8_t` | [in] | 工作模式枚举，决定采集、显示或算法分支。 | 签名/实现语义 |

<a id="fn-main-c-App_DrawPhaseDegrees"></a>

### `App_DrawPhaseDegrees`

- 定义位置：[main.c:L250](main.c#L250)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：PLL/Y 通道倍频控制、双 ADC 相位测量和 ILI9341 李萨如显示。
- 具体作用：作为文件内部辅助函数，绘制相位；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
static tft_ili9341_status_t App_DrawPhaseDegrees( int16_t phase_degrees, uint8_t valid)
```

- 返回值（`tft_ili9341_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `phase_degrees` | `int16_t` | [in] | 相位角，单位度。 | 签名/实现语义 |
| `valid` | `uint8_t` | [in] | 控制 `App_DrawPhaseDegrees` 的 `valid` 输入量；类型为 `uint8_t`，有效范围由函数中的参数校验决定。 | 签名/实现语义 |

<a id="fn-main-c-Lissajous_MapX"></a>

### `Lissajous_MapX`

- 定义位置：[main.c:L271](main.c#L271)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：PLL/Y 通道倍频控制、双 ADC 相位测量和 ILI9341 李萨如显示。
- 具体作用：作为文件内部辅助函数，映射李萨如轨迹；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
static int32_t Lissajous_MapX(uint16_t sample)
```

- 返回值（`int32_t`）：返回 `Lissajous_MapX` 计算得到的整数结果；位宽和符号由 `int32_t` 决定。（依据：类型/函数名语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `sample` | `uint16_t` | [in] | 单个样本值。 | 签名/实现语义 |

<a id="fn-main-c-Lissajous_MapY"></a>

### `Lissajous_MapY`

- 定义位置：[main.c:L279](main.c#L279)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：PLL/Y 通道倍频控制、双 ADC 相位测量和 ILI9341 李萨如显示。
- 具体作用：作为文件内部辅助函数，映射李萨如轨迹；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
static int32_t Lissajous_MapY(uint16_t sample)
```

- 返回值（`int32_t`）：返回 `Lissajous_MapY` 计算得到的整数结果；位宽和符号由 `int32_t` 决定。（依据：类型/函数名语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `sample` | `uint16_t` | [in] | 单个样本值。 | 签名/实现语义 |

<a id="fn-main-c-Lissajous_DrawStaticFrame"></a>

### `Lissajous_DrawStaticFrame`

- 定义位置：[main.c:L287](main.c#L287)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：PLL/Y 通道倍频控制、双 ADC 相位测量和 ILI9341 李萨如显示。
- 具体作用：作为文件内部辅助函数，绘制李萨如轨迹；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
static tft_ili9341_status_t Lissajous_DrawStaticFrame(void)
```

- 返回值（`tft_ili9341_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数：无（`void`）。

<a id="fn-main-c-Lissajous_DrawFrame"></a>

### `Lissajous_DrawFrame`

- 定义位置：[main.c:L304](main.c#L304)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：PLL/Y 通道倍频控制、双 ADC 相位测量和 ILI9341 李萨如显示。
- 具体作用：把同步 X/Y ADC 帧映射为 ILI9341 李萨如轨迹。
- 函数签名：

```c
static tft_ili9341_status_t Lissajous_DrawFrame(void)
```

- 返回值（`tft_ili9341_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数：无（`void`）。

<a id="fn-main-c-main"></a>

### `main`

- 定义位置：[main.c:L336](main.c#L336)
- 属性：工程入口；功能分类：工程入口、调度与中断。
- 所属模块：PLL/Y 通道倍频控制、双 ADC 相位测量和 ILI9341 李萨如显示。
- 具体作用：应用层（PLL/Y 通道倍频控制、双 ADC 相位测量和 ILI9341 李萨如显示）：工程入口：初始化 SysConfig 和所选模块，随后进入应用主循环。
- 函数签名：

```c
int main(void)
```

- 返回值（`int`）：嵌入式主循环正常情况下不会返回；保留的 `int` 返回类型用于满足 C 入口约定。（依据：签名/实现语义）
- 参数：无（`void`）。


<a id="file-modules-signal_dual_adc_mspm0g3507-c"></a>

## `modules/signal_dual_adc_mspm0g3507.c`

<a id="fn-modules-signal_dual_adc_mspm0g3507-c-SignalDualADC_SetDMATransferMode"></a>

### `SignalDualADC_SetDMATransferMode`

- 定义位置：[modules/signal_dual_adc_mspm0g3507.c:L37](modules/signal_dual_adc_mspm0g3507.c#L37)
- 属性：文件内部 `static`；功能分类：ADC、DMA、连续采集与缓冲。
- 所属模块：MSPM0G3507 双 ADC 同定时器事件同步 DMA 采集。
- 具体作用：为双路 ADC 的 DMA 通道设置单次或循环搬运模式。
- 函数签名：

```c
static void SignalDualADC_SetDMATransferMode( DL_DMA_TRANSFER_MODE transfer_mode)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `transfer_mode` | `DL_DMA_TRANSFER_MODE` | [in] | 阻塞式总线的发送/接收工作模式。 | 签名/实现语义 |

<a id="fn-modules-signal_dual_adc_mspm0g3507-c-SignalDualADC_ConfigureDMABlock"></a>

### `SignalDualADC_ConfigureDMABlock`

- 定义位置：[modules/signal_dual_adc_mspm0g3507.c:L44](modules/signal_dual_adc_mspm0g3507.c#L44)
- 属性：文件内部 `static`；功能分类：ADC、DMA、连续采集与缓冲。
- 所属模块：MSPM0G3507 双 ADC 同定时器事件同步 DMA 采集。
- 具体作用：把某个连续采集块的 A/B 目标地址和传输长度写入两路 DMA。
- 函数签名：

```c
static void SignalDualADC_ConfigureDMABlock(uint8_t block_index)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `block_index` | `uint8_t` | [in] | DMA/存储块索引。 | 签名/实现语义 |

<a id="fn-modules-signal_dual_adc_mspm0g3507-c-SignalDualADC_CalculateTimerCount"></a>

### `SignalDualADC_CalculateTimerCount`

- 定义位置：[modules/signal_dual_adc_mspm0g3507.c:L73](modules/signal_dual_adc_mspm0g3507.c#L73)
- 属性：文件内部 `static`；功能分类：ADC、DMA、连续采集与缓冲。
- 所属模块：MSPM0G3507 双 ADC 同定时器事件同步 DMA 采集。
- 具体作用：内部辅助（MSPM0G3507 双 ADC 同定时器事件同步 DMA 采集）：返回或更新当前累计数量。
- 函数签名：

```c
static signal_result_t SignalDualADC_CalculateTimerCount( uint32_t sample_rate_hz, uint32_t *timer_count, uint32_t *actual_rate_hz)
```

- 返回值（`signal_result_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `sample_rate_hz` | `uint32_t` | [in] | 采样率，单位 Hz；必须与实际定时器触发率一致。 | 签名/实现语义 |
| `timer_count` | `uint32_t *` | [in/out] | 定时器当前/目标计数值，单位为 timer tick。 | 签名/实现语义 |
| `actual_rate_hz` | `uint32_t *` | [out] | 输出参数：由定时器整数分频后实际实现的速率，单位 Hz。 | 签名/实现语义 |

<a id="fn-modules-signal_dual_adc_mspm0g3507-c-SignalDualADC_Init"></a>

### `SignalDualADC_Init`

- 定义位置：[modules/signal_dual_adc_mspm0g3507.c:L91](modules/signal_dual_adc_mspm0g3507.c#L91)
- 属性：公开接口；功能分类：ADC、DMA、连续采集与缓冲。
- 所属模块：MSPM0G3507 双 ADC 同定时器事件同步 DMA 采集。
- 具体作用：公开接口（MSPM0G3507 双 ADC 同定时器事件同步 DMA 采集）：初始化模块对象、硬件绑定和运行状态。
- 函数签名：

```c
signal_result_t SignalDualADC_Init(const signal_dual_adc_config_t *config)
```

- 返回值（`signal_result_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `config` | `const signal_dual_adc_config_t *` | [in] | 模块配置结构体，包含初始化所需的采样率、时钟、容量或硬件参数；调用期间必须保持可读。 | 签名/实现语义 |

<a id="fn-modules-signal_dual_adc_mspm0g3507-c-SignalDualADC_SetSampleRate"></a>

### `SignalDualADC_SetSampleRate`

- 定义位置：[modules/signal_dual_adc_mspm0g3507.c:L136](modules/signal_dual_adc_mspm0g3507.c#L136)
- 属性：公开接口；功能分类：ADC、DMA、连续采集与缓冲。
- 所属模块：MSPM0G3507 双 ADC 同定时器事件同步 DMA 采集。
- 具体作用：公开接口（MSPM0G3507 双 ADC 同定时器事件同步 DMA 采集）：按目标采样率重算并写入采样 Timer 周期。
- 函数签名：

```c
signal_result_t SignalDualADC_SetSampleRate(uint32_t sample_rate_hz)
```

- 返回值（`signal_result_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `sample_rate_hz` | `uint32_t` | [in] | 采样率，单位 Hz；必须与实际定时器触发率一致。 | 签名/实现语义 |

<a id="fn-modules-signal_dual_adc_mspm0g3507-c-SignalDualADC_Start"></a>

### `SignalDualADC_Start`

- 定义位置：[modules/signal_dual_adc_mspm0g3507.c:L153](modules/signal_dual_adc_mspm0g3507.c#L153)
- 属性：公开接口；功能分类：ADC、DMA、连续采集与缓冲。
- 所属模块：MSPM0G3507 双 ADC 同定时器事件同步 DMA 采集。
- 具体作用：公开接口（MSPM0G3507 双 ADC 同定时器事件同步 DMA 采集）：启动一次硬件搬运、采集或输出操作。
- 函数签名：

```c
signal_result_t SignalDualADC_Start(uint16_t *channel_a, uint16_t *channel_b, uint16_t sample_count)
```

- 返回值（`signal_result_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `channel_a` | `uint16_t *` | [in/out] | 同步采集 A 通道样本缓冲区；硬件采集函数会把 ADC 码写入其中。 | 签名/实现语义 |
| `channel_b` | `uint16_t *` | [in/out] | 同步采集 B 通道样本缓冲区；硬件采集函数会把 ADC 码写入其中。 | 签名/实现语义 |
| `sample_count` | `uint16_t` | [in] | 本次采集或处理的样本数。 | 签名/实现语义 |

<a id="fn-modules-signal_dual_adc_mspm0g3507-c-SignalDualADC_StartContinuous"></a>

### `SignalDualADC_StartContinuous`

- 定义位置：[modules/signal_dual_adc_mspm0g3507.c:L214](modules/signal_dual_adc_mspm0g3507.c#L214)
- 属性：公开接口；功能分类：ADC、DMA、连续采集与缓冲。
- 所属模块：MSPM0G3507 双 ADC 同定时器事件同步 DMA 采集。
- 具体作用：公开接口（MSPM0G3507 双 ADC 同定时器事件同步 DMA 采集）：启动连续多块 DMA 采集并发布完整块快照。
- 函数签名：

```c
signal_result_t SignalDualADC_StartContinuous(uint16_t *channel_a, uint16_t *channel_b, uint16_t samples_per_block, uint8_t block_count)
```

- 返回值（`signal_result_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `channel_a` | `uint16_t *` | [in/out] | 同步采集 A 通道样本缓冲区；硬件采集函数会把 ADC 码写入其中。 | 签名/实现语义 |
| `channel_b` | `uint16_t *` | [in/out] | 同步采集 B 通道样本缓冲区；硬件采集函数会把 ADC 码写入其中。 | 签名/实现语义 |
| `samples_per_block` | `uint16_t` | [in] | 连续 DMA 每个块包含的样本数。 | 签名/实现语义 |
| `block_count` | `uint8_t` | [in] | 连续 DMA 使用的块数量。 | 签名/实现语义 |

<a id="fn-modules-signal_dual_adc_mspm0g3507-c-SignalDualADC_Stop"></a>

### `SignalDualADC_Stop`

- 定义位置：[modules/signal_dual_adc_mspm0g3507.c:L269](modules/signal_dual_adc_mspm0g3507.c#L269)
- 属性：公开接口；功能分类：ADC、DMA、连续采集与缓冲。
- 所属模块：MSPM0G3507 双 ADC 同定时器事件同步 DMA 采集。
- 具体作用：公开接口（MSPM0G3507 双 ADC 同定时器事件同步 DMA 采集）：停止当前硬件操作并恢复空闲状态。
- 函数签名：

```c
void SignalDualADC_Stop(void)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数：无（`void`）。

<a id="fn-modules-signal_dual_adc_mspm0g3507-c-SignalDualADC_IsFinished"></a>

### `SignalDualADC_IsFinished`

- 定义位置：[modules/signal_dual_adc_mspm0g3507.c:L289](modules/signal_dual_adc_mspm0g3507.c#L289)
- 属性：公开接口；功能分类：ADC、DMA、连续采集与缓冲。
- 所属模块：MSPM0G3507 双 ADC 同定时器事件同步 DMA 采集。
- 具体作用：公开接口（MSPM0G3507 双 ADC 同定时器事件同步 DMA 采集）：查询本次 DMA/算法处理是否完成。
- 函数签名：

```c
bool SignalDualADC_IsFinished(void)
```

- 返回值（`bool`）：返回布尔判定：`true` 表示 `SignalDualADC_IsFinished` 所检查的条件成立，`false` 表示不成立。（依据：签名/函数名语义）
- 参数：无（`void`）。

<a id="fn-modules-signal_dual_adc_mspm0g3507-c-SignalDualADC_IsContinuous"></a>

### `SignalDualADC_IsContinuous`

- 定义位置：[modules/signal_dual_adc_mspm0g3507.c:L290](modules/signal_dual_adc_mspm0g3507.c#L290)
- 属性：公开接口；功能分类：ADC、DMA、连续采集与缓冲。
- 所属模块：MSPM0G3507 双 ADC 同定时器事件同步 DMA 采集。
- 具体作用：公开接口（MSPM0G3507 双 ADC 同定时器事件同步 DMA 采集）：查询模块是否处于连续采集模式。
- 函数签名：

```c
bool SignalDualADC_IsContinuous(void)
```

- 返回值（`bool`）：返回布尔判定：`true` 表示 `SignalDualADC_IsContinuous` 所检查的条件成立，`false` 表示不成立。（依据：签名/函数名语义）
- 参数：无（`void`）。

<a id="fn-modules-signal_dual_adc_mspm0g3507-c-SignalDualADC_GetContinuousBlockSequence"></a>

### `SignalDualADC_GetContinuousBlockSequence`

- 定义位置：[modules/signal_dual_adc_mspm0g3507.c:L291](modules/signal_dual_adc_mspm0g3507.c#L291)
- 属性：公开接口；功能分类：ADC、DMA、连续采集与缓冲。
- 所属模块：MSPM0G3507 双 ADC 同定时器事件同步 DMA 采集。
- 具体作用：公开接口（MSPM0G3507 双 ADC 同定时器事件同步 DMA 采集）：读取连续 DMA 的已发布块序号。
- 函数签名：

```c
uint32_t SignalDualADC_GetContinuousBlockSequence(void)
```

- 返回值（`uint32_t`）：返回相应数组、槽位或 DMA 块索引。（依据：函数名语义）
- 参数：无（`void`）。

<a id="fn-modules-signal_dual_adc_mspm0g3507-c-SignalDualADC_GetContinuousCompletedBlock"></a>

### `SignalDualADC_GetContinuousCompletedBlock`

- 定义位置：[modules/signal_dual_adc_mspm0g3507.c:L295](modules/signal_dual_adc_mspm0g3507.c#L295)
- 属性：公开接口；功能分类：ADC、DMA、连续采集与缓冲。
- 所属模块：MSPM0G3507 双 ADC 同定时器事件同步 DMA 采集。
- 具体作用：公开接口（MSPM0G3507 双 ADC 同定时器事件同步 DMA 采集）：读取连续 DMA 最近完成的块索引。
- 函数签名：

```c
uint8_t SignalDualADC_GetContinuousCompletedBlock(void)
```

- 返回值（`uint8_t`）：返回相应数组、槽位或 DMA 块索引。（依据：函数名语义）
- 参数：无（`void`）。

<a id="fn-modules-signal_dual_adc_mspm0g3507-c-SignalDualADC_GetContinuousSnapshot"></a>

### `SignalDualADC_GetContinuousSnapshot`

- 定义位置：[modules/signal_dual_adc_mspm0g3507.c:L299](modules/signal_dual_adc_mspm0g3507.c#L299)
- 属性：公开接口；功能分类：ADC、DMA、连续采集与缓冲。
- 所属模块：MSPM0G3507 双 ADC 同定时器事件同步 DMA 采集。
- 具体作用：公开接口（MSPM0G3507 双 ADC 同定时器事件同步 DMA 采集）：原子读取连续 DMA 已完成块的序号和索引。
- 函数签名：

```c
bool SignalDualADC_GetContinuousSnapshot(uint32_t *sequence, uint8_t *completed_block)
```

- 返回值（`bool`）：返回布尔判定：`true` 表示 `SignalDualADC_GetContinuousSnapshot` 所检查的条件成立，`false` 表示不成立。（依据：签名/函数名语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `sequence` | `uint32_t *` | [out] | 连续 DMA 发布序号，用于判断是否出现新块或丢块。 | 签名/实现语义 |
| `completed_block` | `uint8_t *` | [out] | 最近完成且可安全读取的 DMA 块索引。 | 签名/实现语义 |

<a id="fn-modules-signal_dual_adc_mspm0g3507-c-SignalDualADC_GetStatus"></a>

### `SignalDualADC_GetStatus`

- 定义位置：[modules/signal_dual_adc_mspm0g3507.c:L316](modules/signal_dual_adc_mspm0g3507.c#L316)
- 属性：公开接口；功能分类：ADC、DMA、连续采集与缓冲。
- 所属模块：MSPM0G3507 双 ADC 同定时器事件同步 DMA 采集。
- 具体作用：公开接口（MSPM0G3507 双 ADC 同定时器事件同步 DMA 采集）：返回模块当前运行状态、错误码和完成信息。
- 函数签名：

```c
signal_status_t SignalDualADC_GetStatus(void)
```

- 返回值（`signal_status_t`）：返回模块当前运行状态枚举，例如未初始化、空闲、忙、完成或错误。（依据：类型/实现语义）
- 参数：无（`void`）。

<a id="fn-modules-signal_dual_adc_mspm0g3507-c-SignalDualADC_GetChannelA"></a>

### `SignalDualADC_GetChannelA`

- 定义位置：[modules/signal_dual_adc_mspm0g3507.c:L317](modules/signal_dual_adc_mspm0g3507.c#L317)
- 属性：公开接口；功能分类：ADC、DMA、连续采集与缓冲。
- 所属模块：MSPM0G3507 双 ADC 同定时器事件同步 DMA 采集。
- 具体作用：返回本次双 ADC 采集所绑定的 A 通道缓冲区首地址。
- 函数签名：

```c
const uint16_t *SignalDualADC_GetChannelA(void)
```

- 返回值（`const uint16_t *`）：返回内部对象/缓冲区指针；调用者不得越界访问，若接口允许失败还需先检查是否为 `NULL`。（依据：类型/实现语义）
- 参数：无（`void`）。

<a id="fn-modules-signal_dual_adc_mspm0g3507-c-SignalDualADC_GetChannelB"></a>

### `SignalDualADC_GetChannelB`

- 定义位置：[modules/signal_dual_adc_mspm0g3507.c:L318](modules/signal_dual_adc_mspm0g3507.c#L318)
- 属性：公开接口；功能分类：ADC、DMA、连续采集与缓冲。
- 所属模块：MSPM0G3507 双 ADC 同定时器事件同步 DMA 采集。
- 具体作用：返回本次双 ADC 采集所绑定的 B 通道缓冲区首地址。
- 函数签名：

```c
const uint16_t *SignalDualADC_GetChannelB(void)
```

- 返回值（`const uint16_t *`）：返回内部对象/缓冲区指针；调用者不得越界访问，若接口允许失败还需先检查是否为 `NULL`。（依据：类型/实现语义）
- 参数：无（`void`）。

<a id="fn-modules-signal_dual_adc_mspm0g3507-c-SignalDualADC_GetSampleCount"></a>

### `SignalDualADC_GetSampleCount`

- 定义位置：[modules/signal_dual_adc_mspm0g3507.c:L319](modules/signal_dual_adc_mspm0g3507.c#L319)
- 属性：公开接口；功能分类：ADC、DMA、连续采集与缓冲。
- 所属模块：MSPM0G3507 双 ADC 同定时器事件同步 DMA 采集。
- 具体作用：公开接口（MSPM0G3507 双 ADC 同定时器事件同步 DMA 采集）：返回或更新当前累计数量。
- 函数签名：

```c
uint16_t SignalDualADC_GetSampleCount(void)
```

- 返回值（`uint16_t`）：返回当前有效元素、样本或事件数量。（依据：函数名语义）
- 参数：无（`void`）。

<a id="fn-modules-signal_dual_adc_mspm0g3507-c-SignalDualADC_GetConfiguredRate"></a>

### `SignalDualADC_GetConfiguredRate`

- 定义位置：[modules/signal_dual_adc_mspm0g3507.c:L320](modules/signal_dual_adc_mspm0g3507.c#L320)
- 属性：公开接口；功能分类：ADC、DMA、连续采集与缓冲。
- 所属模块：MSPM0G3507 双 ADC 同定时器事件同步 DMA 采集。
- 具体作用：公开接口（MSPM0G3507 双 ADC 同定时器事件同步 DMA 采集）：返回 Timer 量化后实际采用的采样/更新率。
- 函数签名：

```c
uint32_t SignalDualADC_GetConfiguredRate(void)
```

- 返回值（`uint32_t`）：返回量化后实际速率/频率，单位 Hz。（依据：函数名语义）
- 参数：无（`void`）。

<a id="fn-modules-signal_dual_adc_mspm0g3507-c-SignalDualADC_GetModuleMaturity"></a>

### `SignalDualADC_GetModuleMaturity`

- 定义位置：[modules/signal_dual_adc_mspm0g3507.c:L321](modules/signal_dual_adc_mspm0g3507.c#L321)
- 属性：公开接口；功能分类：ADC、DMA、连续采集与缓冲。
- 所属模块：MSPM0G3507 双 ADC 同定时器事件同步 DMA 采集。
- 具体作用：公开接口（MSPM0G3507 双 ADC 同定时器事件同步 DMA 采集）：返回模块成熟度和验证等级元数据。
- 函数签名：

```c
signal_module_status_t SignalDualADC_GetModuleMaturity(void)
```

- 返回值（`signal_module_status_t`）：返回模块成熟度/验证等级元数据，供应用判断示例、冻结或硬件验证状态。（依据：类型/实现语义）
- 参数：无（`void`）。

<a id="fn-modules-signal_dual_adc_mspm0g3507-c-DMA_IRQHandler"></a>

### `DMA_IRQHandler`

- 定义位置：[modules/signal_dual_adc_mspm0g3507.c:L326](modules/signal_dual_adc_mspm0g3507.c#L326)
- 属性：ISR（由硬件/内核调用）；功能分类：ADC、DMA、连续采集与缓冲。
- 所属模块：MSPM0G3507 双 ADC 同定时器事件同步 DMA 采集。
- 具体作用：处理双 ADC DMA 完成事件；同步两通道完成状态并发布可读取的完整块。
- 函数签名：

```c
void DMA_IRQHandler(void)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数：无（`void`）。


<a id="file-modules-signal_dual_adc_phase-c"></a>

## `modules/signal_dual_adc_phase.c`

<a id="fn-modules-signal_dual_adc_phase-c-SignalDualADCPhase_FindBounds"></a>

### `SignalDualADCPhase_FindBounds`

- 定义位置：[modules/signal_dual_adc_phase.c:L11](modules/signal_dual_adc_phase.c#L11)
- 属性：文件内部 `static`；功能分类：频率、相位与同步检波。
- 所属模块：双路原始 ADC 动态中点、滞回过零及平均相位差。
- 具体作用：遍历 ADC 原始码，返回最小值和最大值供动态中点/迟滞计算。
- 函数签名：

```c
static void SignalDualADCPhase_FindBounds( const uint16_t *samples, uint32_t sample_count, signal_dual_adc_phase_bounds_t *bounds)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `samples` | `const uint16_t *` | [in] | 时域样本数组；样本的单位由模块决定（ADC 码、伏特或归一化值）。 | 签名/实现语义 |
| `sample_count` | `uint32_t` | [in] | 本次采集或处理的样本数。 | 签名/实现语义 |
| `bounds` | `signal_dual_adc_phase_bounds_t *` | [out] | 上下界/安全范围结果结构体。 | 签名/实现语义 |

<a id="fn-modules-signal_dual_adc_phase-c-SignalDualADCPhase_FindRisingCrossings"></a>

### `SignalDualADCPhase_FindRisingCrossings`

- 定义位置：[modules/signal_dual_adc_phase.c:L33](modules/signal_dual_adc_phase.c#L33)
- 属性：文件内部 `static`；功能分类：频率、相位与同步检波。
- 所属模块：双路原始 ADC 动态中点、滞回过零及平均相位差。
- 具体作用：以动态中点和迟滞搜索上升过零点，并记录交越样本索引。
- 函数签名：

```c
static uint16_t SignalDualADCPhase_FindRisingCrossings( const uint16_t *samples, uint32_t sample_count, uint16_t threshold, uint16_t hysteresis, uint32_t *crossings, uint16_t crossing_capacity)
```

- 返回值（`uint16_t`）：返回 `SignalDualADCPhase_FindRisingCrossings` 计算得到的整数结果；位宽和符号由 `uint16_t` 决定。（依据：类型/函数名语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `samples` | `const uint16_t *` | [in] | 时域样本数组；样本的单位由模块决定（ADC 码、伏特或归一化值）。 | 签名/实现语义 |
| `sample_count` | `uint32_t` | [in] | 本次采集或处理的样本数。 | 签名/实现语义 |
| `threshold` | `uint16_t` | [in] | 判决门限；单位与输入数据一致。 | 签名/实现语义 |
| `hysteresis` | `uint16_t` | [in] | 迟滞量；单位与被比较的数据一致。 | 签名/实现语义 |
| `crossings` | `uint32_t *` | [in/out] | 检测到的过零位置数组。 | 签名/实现语义 |
| `crossing_capacity` | `uint16_t` | [in] | 过零位置数组可容纳的最大元素数。 | 签名/实现语义 |

<a id="fn-modules-signal_dual_adc_phase-c-SignalDualADCPhase_AveragePeriod"></a>

### `SignalDualADCPhase_AveragePeriod`

- 定义位置：[modules/signal_dual_adc_phase.c:L83](modules/signal_dual_adc_phase.c#L83)
- 属性：文件内部 `static`；功能分类：频率、相位与同步检波。
- 所属模块：双路原始 ADC 动态中点、滞回过零及平均相位差。
- 具体作用：对同一通道相邻上升过零间隔求平均周期样本数。
- 函数签名：

```c
static uint32_t SignalDualADCPhase_AveragePeriod( const uint32_t *crossings, uint16_t crossing_count)
```

- 返回值（`uint32_t`）：返回 `SignalDualADCPhase_AveragePeriod` 计算得到的整数结果；位宽和符号由 `uint32_t` 决定。（依据：类型/函数名语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `crossings` | `const uint32_t *` | [in] | 检测到的过零位置数组。 | 签名/实现语义 |
| `crossing_count` | `uint16_t` | [in] | 有效过零点数量。 | 签名/实现语义 |

<a id="fn-modules-signal_dual_adc_phase-c-SignalDualADCPhase_AverageWrapped"></a>

### `SignalDualADCPhase_AverageWrapped`

- 定义位置：[modules/signal_dual_adc_phase.c:L95](modules/signal_dual_adc_phase.c#L95)
- 属性：文件内部 `static`；功能分类：频率、相位与同步检波。
- 所属模块：双路原始 ADC 动态中点、滞回过零及平均相位差。
- 具体作用：对已按周期回绕的多组相位延迟做圆周意义上的平均。
- 函数签名：

```c
static int32_t SignalDualADCPhase_AverageWrapped( const int16_t *phases, uint16_t phase_count)
```

- 返回值（`int32_t`）：返回 `SignalDualADCPhase_AverageWrapped` 计算得到的整数结果；位宽和符号由 `int32_t` 决定。（依据：类型/函数名语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `phases` | `const int16_t *` | [in] | `phases` 数据对象/缓冲区；结合类型 `const int16_t *` 由函数读取或写回。 | 签名/实现语义 |
| `phase_count` | `uint16_t` | [in] | 有效元素、样本、字节或事件的数量。 | 签名/实现语义 |

<a id="fn-modules-signal_dual_adc_phase-c-SignalDualADCPhase_Process"></a>

### `SignalDualADCPhase_Process`

- 定义位置：[modules/signal_dual_adc_phase.c:L133](modules/signal_dual_adc_phase.c#L133)
- 属性：公开接口；功能分类：频率、相位与同步检波。
- 所属模块：双路原始 ADC 动态中点、滞回过零及平均相位差。
- 具体作用：从一帧双路同步 ADC 原始码计算 Y 相对 X 的相位差。
- 函数签名：

```c
signal_algorithm_status_t SignalDualADCPhase_Process( const uint16_t *samples_x, const uint16_t *samples_y, uint32_t sample_count, uint32_t sample_rate_hz, const signal_dual_adc_phase_config_t *config, signal_dual_adc_phase_result_t *result)
```

- 返回值（`signal_algorithm_status_t`）：成功返回 SIGNAL_ALGORITHM_OK；参数、数据量、幅度或数值不满足条件时返回对应错误码。（依据：源码 `@return`）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `samples_x` | `const uint16_t *` | [in] | X 路 ADC 原始码数组，只读。 | 源码 `@param` |
| `samples_y` | `const uint16_t *` | [in] | Y 路 ADC 原始码数组，只读，与 X 路同步且等长。 | 源码 `@param` |
| `sample_count` | `uint32_t` | [in] | 每路数组的样本数，至少 2，不能超过 65535。 | 源码 `@param` |
| `sample_rate_hz` | `uint32_t` | [in] | 实际同步采样率，单位 Hz，必须大于 0。 | 源码 `@param` |
| `config` | `const signal_dual_adc_phase_config_t *` | [in] | 检测参数；frequency_ratio 为 fY/fX，范围 1~5。 | 源码 `@param` |
| `result` | `signal_dual_adc_phase_result_t *` | [out] | 输出结果；phase_degrees 范围 [-180,180]，正值表示 Y 超前。 | 源码 `@param` |


<a id="file-modules-signal_matrix_keypad_4x4-c"></a>

## `modules/signal_matrix_keypad_4x4.c`

<a id="fn-modules-signal_matrix_keypad_4x4-c-keypad_release_all_rows"></a>

### `keypad_release_all_rows`

- 定义位置：[modules/signal_matrix_keypad_4x4.c:L17](modules/signal_matrix_keypad_4x4.c#L17)
- 属性：文件内部 `static`；功能分类：矩阵键盘与交互输入。
- 所属模块：4×4 矩阵键盘行列扫描、消抖、鬼键过滤和符号映射。
- 具体作用：内部辅助（4×4 矩阵键盘行列扫描、消抖、鬼键过滤和符号映射）：释放已消费的数据块，使 DMA 可以再次覆盖。
- 函数签名：

```c
static signal_result_t keypad_release_all_rows( signal_matrix_keypad_4x4_t *keypad)
```

- 返回值（`signal_result_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `keypad` | `signal_matrix_keypad_4x4_t *` | [in/out] | 4×4 矩阵键盘对象，保存 GPIO 回调、消抖计数和按键状态。 | 签名/实现语义 |

<a id="fn-modules-signal_matrix_keypad_4x4-c-keypad_ghost_possible"></a>

### `keypad_ghost_possible`

- 定义位置：[modules/signal_matrix_keypad_4x4.c:L32](modules/signal_matrix_keypad_4x4.c#L32)
- 属性：文件内部 `static`；功能分类：矩阵键盘与交互输入。
- 所属模块：4×4 矩阵键盘行列扫描、消抖、鬼键过滤和符号映射。
- 具体作用：检查当前行列按下组合是否可能形成矩阵键盘鬼键；发现多行多列同时有效时拒绝上报。
- 函数签名：

```c
static bool keypad_ghost_possible(uint16_t mask)
```

- 返回值（`bool`）：返回布尔判定：`true` 表示 `keypad_ghost_possible` 所检查的条件成立，`false` 表示不成立。（依据：签名/函数名语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `mask` | `uint16_t` | [in] | 控制 `keypad_ghost_possible` 的 `mask` 输入量；类型为 `uint16_t`，有效范围由函数中的参数校验决定。 | 签名/实现语义 |

<a id="fn-modules-signal_matrix_keypad_4x4-c-SignalMatrixKeypad4x4_Init"></a>

### `SignalMatrixKeypad4x4_Init`

- 定义位置：[modules/signal_matrix_keypad_4x4.c:L53](modules/signal_matrix_keypad_4x4.c#L53)
- 属性：公开接口；功能分类：矩阵键盘与交互输入。
- 所属模块：4×4 矩阵键盘行列扫描、消抖、鬼键过滤和符号映射。
- 具体作用：根据调用者提供的配置或对象完成一次初始化；通常在 `SYSCFG_DL_init()` 之后且仅调用一次。
- 函数签名：

```c
signal_result_t SignalMatrixKeypad4x4_Init( signal_matrix_keypad_4x4_t *keypad, const signal_matrix_keypad_4x4_config_t *config)
```

- 返回值（`signal_result_t`）：返回 signal_result_t 类型结果；调用者应检查该值。（依据：源码 `@return`）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `keypad` | `signal_matrix_keypad_4x4_t *` | [in/out] | `keypad`（`signal_matrix_keypad_4x4_t *`）是该 API 的输入/输出参数；按本 README 前面的数据单位和边界条件准备。 | 源码 `@param` |
| `config` | `const signal_matrix_keypad_4x4_config_t *` | [in] | 调用者填写的配置对象。先阅读该类型的成员；它控制本次初始化或处理方式。 | 源码 `@param` |

<a id="fn-modules-signal_matrix_keypad_4x4-c-SignalMatrixKeypad4x4_Scan"></a>

### `SignalMatrixKeypad4x4_Scan`

- 定义位置：[modules/signal_matrix_keypad_4x4.c:L80](modules/signal_matrix_keypad_4x4.c#L80)
- 属性：公开接口；功能分类：矩阵键盘与交互输入。
- 所属模块：4×4 矩阵键盘行列扫描、消抖、鬼键过滤和符号映射。
- 具体作用：逐行驱动 4×4 矩阵键盘并读取四列，执行消抖、按下/释放状态更新和事件入队。
- 函数签名：

```c
signal_result_t SignalMatrixKeypad4x4_Scan( signal_matrix_keypad_4x4_t *keypad, signal_matrix_keypad_4x4_event_t *event)
```

- 返回值（`signal_result_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `keypad` | `signal_matrix_keypad_4x4_t *` | [in/out] | 4×4 矩阵键盘对象，保存 GPIO 回调、消抖计数和按键状态。 | 签名/实现语义 |
| `event` | `signal_matrix_keypad_4x4_event_t *` | [in/out] | 单个触发/键盘/捕获事件对象。 | 签名/实现语义 |

<a id="fn-modules-signal_matrix_keypad_4x4-c-SignalMatrixKeypad4x4_GetKey"></a>

### `SignalMatrixKeypad4x4_GetKey`

- 定义位置：[modules/signal_matrix_keypad_4x4.c:L155](modules/signal_matrix_keypad_4x4.c#L155)
- 属性：公开接口；功能分类：矩阵键盘与交互输入。
- 所属模块：4×4 矩阵键盘行列扫描、消抖、鬼键过滤和符号映射。
- 具体作用：读取最近一次操作保存的状态、结果或配置；先确认前置操作已经成功。
- 函数签名：

```c
signal_result_t SignalMatrixKeypad4x4_GetKey( const signal_matrix_keypad_4x4_t *keypad, uint8_t key_index, char *symbol)
```

- 返回值（`signal_result_t`）：返回 signal_result_t 类型结果；调用者应检查该值。（依据：源码 `@return`）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `keypad` | `const signal_matrix_keypad_4x4_t *` | [in/out] | `keypad`（`const signal_matrix_keypad_4x4_t *`）是该 API 的输入/输出参数；按本 README 前面的数据单位和边界条件准备。 | 源码 `@param` |
| `key_index` | `uint8_t` | [in] | 索引或通道号；范围由相应数组长度、FFT bin 数或当前硬件配置决定。 | 源码 `@param` |
| `symbol` | `char *` | [in/out] | `symbol`（`char *`）是该 API 的输入/输出参数；按本 README 前面的数据单位和边界条件准备。 | 源码 `@param` |

<a id="fn-modules-signal_matrix_keypad_4x4-c-SignalMatrixKeypad4x4_GetFirstPressed"></a>

### `SignalMatrixKeypad4x4_GetFirstPressed`

- 定义位置：[modules/signal_matrix_keypad_4x4.c:L171](modules/signal_matrix_keypad_4x4.c#L171)
- 属性：公开接口；功能分类：矩阵键盘与交互输入。
- 所属模块：4×4 矩阵键盘行列扫描、消抖、鬼键过滤和符号映射。
- 具体作用：读取最近一次操作保存的状态、结果或配置；先确认前置操作已经成功。
- 函数签名：

```c
signal_result_t SignalMatrixKeypad4x4_GetFirstPressed( const signal_matrix_keypad_4x4_t *keypad, char *symbol, uint8_t *key_index)
```

- 返回值（`signal_result_t`）：返回 signal_result_t 类型结果；调用者应检查该值。（依据：源码 `@return`）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `keypad` | `const signal_matrix_keypad_4x4_t *` | [in/out] | `keypad`（`const signal_matrix_keypad_4x4_t *`）是该 API 的输入/输出参数；按本 README 前面的数据单位和边界条件准备。 | 源码 `@param` |
| `symbol` | `char *` | [in/out] | `symbol`（`char *`）是该 API 的输入/输出参数；按本 README 前面的数据单位和边界条件准备。 | 源码 `@param` |
| `key_index` | `uint8_t *` | [in/out] | 索引或通道号；范围由相应数组长度、FFT bin 数或当前硬件配置决定。 | 源码 `@param` |

<a id="fn-modules-signal_matrix_keypad_4x4-c-SignalMatrixKeypad4x4_GetModuleStatus"></a>

### `SignalMatrixKeypad4x4_GetModuleStatus`

- 定义位置：[modules/signal_matrix_keypad_4x4.c:L194](modules/signal_matrix_keypad_4x4.c#L194)
- 属性：公开接口；功能分类：矩阵键盘与交互输入。
- 所属模块：4×4 矩阵键盘行列扫描、消抖、鬼键过滤和符号映射。
- 具体作用：读取模块当前的验证成熟度或静态状态，不会启动硬件操作。
- 函数签名：

```c
signal_module_status_t SignalMatrixKeypad4x4_GetModuleStatus(void)
```

- 返回值（`signal_module_status_t`）：返回 signal_module_status_t 类型结果；调用者应检查该值。（依据：源码 `@return`）
- 参数：无（`void`）。

<a id="fn-modules-signal_matrix_keypad_4x4-c-mspm0g3507_keypad_drive_row"></a>

### `mspm0g3507_keypad_drive_row`

- 定义位置：[modules/signal_matrix_keypad_4x4.c:L203](modules/signal_matrix_keypad_4x4.c#L203)
- 属性：文件内部 `static`；功能分类：矩阵键盘与交互输入。
- 所属模块：4×4 矩阵键盘行列扫描、消抖、鬼键过滤和符号映射。
- 具体作用：把指定键盘行拉到有效电平，同时释放其余行。
- 函数签名：

```c
static signal_result_t mspm0g3507_keypad_drive_row( void *context, uint8_t row, bool active)
```

- 返回值（`signal_result_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `context` | `void *` | [in/out] | 驱动/算法上下文或回调用户数据，用于保存实例状态并避免依赖全局变量。 | 签名/实现语义 |
| `row` | `uint8_t` | [in] | 键盘/字模/表格的行索引。 | 签名/实现语义 |
| `active` | `bool` | [in] | 启用/激活标志。 | 签名/实现语义 |

<a id="fn-modules-signal_matrix_keypad_4x4-c-mspm0g3507_keypad_read_column"></a>

### `mspm0g3507_keypad_read_column`

- 定义位置：[modules/signal_matrix_keypad_4x4.c:L225](modules/signal_matrix_keypad_4x4.c#L225)
- 属性：文件内部 `static`；功能分类：矩阵键盘与交互输入。
- 所属模块：4×4 矩阵键盘行列扫描、消抖、鬼键过滤和符号映射。
- 具体作用：读取指定键盘列 GPIO 电平并转换为按下/未按下状态。
- 函数签名：

```c
static signal_result_t mspm0g3507_keypad_read_column( void *context, uint8_t column, bool *active)
```

- 返回值（`signal_result_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `context` | `void *` | [in/out] | 驱动/算法上下文或回调用户数据，用于保存实例状态并避免依赖全局变量。 | 签名/实现语义 |
| `column` | `uint8_t` | [in] | 键盘/字模/表格的列索引。 | 签名/实现语义 |
| `active` | `bool *` | [in/out] | 启用/激活标志。 | 签名/实现语义 |

<a id="fn-modules-signal_matrix_keypad_4x4-c-mspm0g3507_keypad_delay_us"></a>

### `mspm0g3507_keypad_delay_us`

- 定义位置：[modules/signal_matrix_keypad_4x4.c:L244](modules/signal_matrix_keypad_4x4.c#L244)
- 属性：文件内部 `static`；功能分类：矩阵键盘与交互输入。
- 所属模块：4×4 矩阵键盘行列扫描、消抖、鬼键过滤和符号映射。
- 具体作用：为键盘行切换和消抖提供微秒级忙等待。
- 函数签名：

```c
static void mspm0g3507_keypad_delay_us( void *context, uint32_t microseconds)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `context` | `void *` | [in/out] | 驱动/算法上下文或回调用户数据，用于保存实例状态并避免依赖全局变量。 | 签名/实现语义 |
| `microseconds` | `uint32_t` | [in] | 延时时间，单位 µs。 | 签名/实现语义 |

<a id="fn-modules-signal_matrix_keypad_4x4-c-SignalMatrixKeypad4x4_ReadNewSymbol"></a>

### `SignalMatrixKeypad4x4_ReadNewSymbol`

- 定义位置：[modules/signal_matrix_keypad_4x4.c:L261](modules/signal_matrix_keypad_4x4.c#L261)
- 属性：公开接口；功能分类：矩阵键盘与交互输入。
- 所属模块：4×4 矩阵键盘行列扫描、消抖、鬼键过滤和符号映射。
- 具体作用：从键盘事件队列读取一次新按下事件并映射为字符；没有新事件时返回 false。
- 函数签名：

```c
signal_result_t SignalMatrixKeypad4x4_ReadNewSymbol(char *symbol)
```

- 返回值（`signal_result_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `symbol` | `char *` | [in/out] | 矩阵键盘映射得到的符号字符。 | 签名/实现语义 |


<a id="file-modules-signal_tft_ili9341-c"></a>

## `modules/signal_tft_ili9341.c`

<a id="fn-modules-signal_tft_ili9341-c-tft_is_valid"></a>

### `tft_is_valid`

- 定义位置：[modules/signal_tft_ili9341.c:L74](modules/signal_tft_ili9341.c#L74)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ILI9341 SPI 屏驱动、图元和文字输出。
- 具体作用：检查 ILI9341 上下文及必需的 SPI 写入、D/C 控制回调是否有效。
- 函数签名：

```c
static bool tft_is_valid(const tft_ili9341_t *tft)
```

- 返回值（`bool`）：返回布尔判定：`true` 表示 `tft_is_valid` 所检查的条件成立，`false` 表示不成立。（依据：签名/函数名语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `const tft_ili9341_t *` | [in] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_ili9341-c-tft_get_font"></a>

### `tft_get_font`

- 定义位置：[modules/signal_tft_ili9341.c:L80](modules/signal_tft_ili9341.c#L80)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ILI9341 SPI 屏驱动、图元和文字输出。
- 具体作用：作为文件内部辅助函数，读取模块专用状态和数据；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
static const tft_font_descriptor_t *tft_get_font( tft_ili9341_font_t font)
```

- 返回值（`const tft_font_descriptor_t *`）：返回内部对象/缓冲区指针；调用者不得越界访问，若接口允许失败还需先检查是否为 `NULL`。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `font` | `tft_ili9341_font_t` | [in] | 字体描述符，包含字模尺寸、字符范围和位图地址。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_ili9341-c-tft_set_cs"></a>

### `tft_set_cs`

- 定义位置：[modules/signal_tft_ili9341.c:L96](modules/signal_tft_ili9341.c#L96)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ILI9341 SPI 屏驱动、图元和文字输出。
- 具体作用：作为文件内部辅助函数，设置模块专用状态和数据；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
static void tft_set_cs(tft_ili9341_t *tft, bool high)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_ili9341_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `high` | `bool` | [in] | 高电平、上限或区间较大端点。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_ili9341-c-tft_lock"></a>

### `tft_lock`

- 定义位置：[modules/signal_tft_ili9341.c:L103](modules/signal_tft_ili9341.c#L103)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ILI9341 SPI 屏驱动、图元和文字输出。
- 具体作用：若配置了总线加锁回调则取得锁，防止一条 TFT 事务被其他代码打断。
- 函数签名：

```c
static void tft_lock(tft_ili9341_t *tft)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_ili9341_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_ili9341-c-tft_unlock"></a>

### `tft_unlock`

- 定义位置：[modules/signal_tft_ili9341.c:L110](modules/signal_tft_ili9341.c#L110)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ILI9341 SPI 屏驱动、图元和文字输出。
- 具体作用：若配置了总线解锁回调则释放锁。
- 函数签名：

```c
static void tft_unlock(tft_ili9341_t *tft)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_ili9341_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_ili9341-c-tft_transaction_unlocked"></a>

### `tft_transaction_unlocked`

- 定义位置：[modules/signal_tft_ili9341.c:L117](modules/signal_tft_ili9341.c#L117)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ILI9341 SPI 屏驱动、图元和文字输出。
- 具体作用：在外层已持锁时设置 D/C 和片选，通过写回调发送一个命令或数据缓冲区。
- 函数签名：

```c
static tft_ili9341_status_t tft_transaction_unlocked(tft_ili9341_t *tft, bool data_mode, const uint8_t *data, size_t length)
```

- 返回值（`tft_ili9341_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_ili9341_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `data_mode` | `bool` | [in] | 总线当前发送的是命令还是数据的标志。 | 签名/实现语义 |
| `data` | `const uint8_t *` | [in] | 待处理的数据/总线字节缓冲区；是否原地修改由参数方向和函数说明决定。 | 签名/实现语义 |
| `length` | `size_t` | [in] | 数据、文本或波表的有效长度。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_ili9341-c-tft_transaction"></a>

### `tft_transaction`

- 定义位置：[modules/signal_tft_ili9341.c:L139](modules/signal_tft_ili9341.c#L139)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ILI9341 SPI 屏驱动、图元和文字输出。
- 具体作用：取得总线锁、执行一次 ILI9341 命令/数据事务并释放锁。
- 函数签名：

```c
static tft_ili9341_status_t tft_transaction(tft_ili9341_t *tft, bool data_mode, const uint8_t *data, size_t length)
```

- 返回值（`tft_ili9341_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_ili9341_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `data_mode` | `bool` | [in] | 总线当前发送的是命令还是数据的标志。 | 签名/实现语义 |
| `data` | `const uint8_t *` | [in] | 待处理的数据/总线字节缓冲区；是否原地修改由参数方向和函数说明决定。 | 签名/实现语义 |
| `length` | `size_t` | [in] | 数据、文本或波表的有效长度。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_ili9341-c-tft_command_data_unlocked"></a>

### `tft_command_data_unlocked`

- 定义位置：[modules/signal_tft_ili9341.c:L155](modules/signal_tft_ili9341.c#L155)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ILI9341 SPI 屏驱动、图元和文字输出。
- 具体作用：在已持锁时先发送命令字节，再按需发送参数数据。
- 函数签名：

```c
static tft_ili9341_status_t tft_command_data_unlocked( tft_ili9341_t *tft, uint8_t command, const uint8_t *data, size_t length)
```

- 返回值（`tft_ili9341_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_ili9341_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `command` | `uint8_t` | [in] | 发送给 TFT/外部器件的命令字节或命令码。 | 签名/实现语义 |
| `data` | `const uint8_t *` | [in] | 待处理的数据/总线字节缓冲区；是否原地修改由参数方向和函数说明决定。 | 签名/实现语义 |
| `length` | `size_t` | [in] | 数据、文本或波表的有效长度。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_ili9341-c-tft_command_data"></a>

### `tft_command_data`

- 定义位置：[modules/signal_tft_ili9341.c:L170](modules/signal_tft_ili9341.c#L170)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ILI9341 SPI 屏驱动、图元和文字输出。
- 具体作用：取得总线锁后发送 ILI9341 命令及参数，再释放锁。
- 函数签名：

```c
static tft_ili9341_status_t tft_command_data(tft_ili9341_t *tft, uint8_t command, const uint8_t *data, size_t length)
```

- 返回值（`tft_ili9341_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_ili9341_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `command` | `uint8_t` | [in] | 发送给 TFT/外部器件的命令字节或命令码。 | 签名/实现语义 |
| `data` | `const uint8_t *` | [in] | 待处理的数据/总线字节缓冲区；是否原地修改由参数方向和函数说明决定。 | 签名/实现语义 |
| `length` | `size_t` | [in] | 数据、文本或波表的有效长度。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_ili9341-c-TFT_ILI9341_WriteCommand"></a>

### `TFT_ILI9341_WriteCommand`

- 定义位置：[modules/signal_tft_ili9341.c:L186](modules/signal_tft_ili9341.c#L186)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ILI9341 SPI 屏驱动、图元和文字输出。
- 具体作用：作为模块公开接口，写入模块专用状态和数据；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
tft_ili9341_status_t TFT_ILI9341_WriteCommand(tft_ili9341_t *tft, uint8_t command)
```

- 返回值（`tft_ili9341_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_ili9341_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `command` | `uint8_t` | [in] | 发送给 TFT/外部器件的命令字节或命令码。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_ili9341-c-TFT_ILI9341_WriteData"></a>

### `TFT_ILI9341_WriteData`

- 定义位置：[modules/signal_tft_ili9341.c:L192](modules/signal_tft_ili9341.c#L192)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ILI9341 SPI 屏驱动、图元和文字输出。
- 具体作用：作为模块公开接口，写入模块专用状态和数据；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
tft_ili9341_status_t TFT_ILI9341_WriteData(tft_ili9341_t *tft, const uint8_t *data, size_t length)
```

- 返回值（`tft_ili9341_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_ili9341_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `data` | `const uint8_t *` | [in] | 待处理的数据/总线字节缓冲区；是否原地修改由参数方向和函数说明决定。 | 签名/实现语义 |
| `length` | `size_t` | [in] | 数据、文本或波表的有效长度。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_ili9341-c-TFT_ILI9341_Init"></a>

### `TFT_ILI9341_Init`

- 定义位置：[modules/signal_tft_ili9341.c:L199](modules/signal_tft_ili9341.c#L199)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ILI9341 SPI 屏驱动、图元和文字输出。
- 具体作用：公开接口（ILI9341 SPI 屏驱动、图元和文字输出）：初始化模块对象、硬件绑定和运行状态。
- 函数签名：

```c
tft_ili9341_status_t TFT_ILI9341_Init(tft_ili9341_t *tft, const tft_ili9341_config_t *config, tft_ili9341_rotation_t rotation)
```

- 返回值（`tft_ili9341_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_ili9341_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `config` | `const tft_ili9341_config_t *` | [in] | 模块配置结构体，包含初始化所需的采样率、时钟、容量或硬件参数；调用期间必须保持可读。 | 签名/实现语义 |
| `rotation` | `tft_ili9341_rotation_t` | [in] | 屏幕旋转枚举，决定宽高和坐标方向。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_ili9341-c-TFT_ILI9341_SetRotation"></a>

### `TFT_ILI9341_SetRotation`

- 定义位置：[modules/signal_tft_ili9341.c:L260](modules/signal_tft_ili9341.c#L260)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ILI9341 SPI 屏驱动、图元和文字输出。
- 具体作用：作为模块公开接口，设置模块专用状态和数据；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
tft_ili9341_status_t TFT_ILI9341_SetRotation( tft_ili9341_t *tft, tft_ili9341_rotation_t rotation)
```

- 返回值（`tft_ili9341_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_ili9341_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `rotation` | `tft_ili9341_rotation_t` | [in] | 屏幕旋转枚举，决定宽高和坐标方向。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_ili9341-c-TFT_ILI9341_SetBacklight"></a>

### `TFT_ILI9341_SetBacklight`

- 定义位置：[modules/signal_tft_ili9341.c:L290](modules/signal_tft_ili9341.c#L290)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ILI9341 SPI 屏驱动、图元和文字输出。
- 具体作用：作为模块公开接口，设置模块专用状态和数据；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
void TFT_ILI9341_SetBacklight(tft_ili9341_t *tft, bool on)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_ili9341_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `on` | `bool` | [in] | 开关状态；true 为开启，false 为关闭。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_ili9341-c-TFT_ILI9341_GetWidth"></a>

### `TFT_ILI9341_GetWidth`

- 定义位置：[modules/signal_tft_ili9341.c:L297](modules/signal_tft_ili9341.c#L297)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ILI9341 SPI 屏驱动、图元和文字输出。
- 具体作用：作为模块公开接口，读取模块专用状态和数据；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
uint16_t TFT_ILI9341_GetWidth(const tft_ili9341_t *tft)
```

- 返回值（`uint16_t`）：返回当前旋转方向下的屏幕宽度/高度，单位像素。（依据：函数名语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `const tft_ili9341_t *` | [in] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_ili9341-c-TFT_ILI9341_GetHeight"></a>

### `TFT_ILI9341_GetHeight`

- 定义位置：[modules/signal_tft_ili9341.c:L302](modules/signal_tft_ili9341.c#L302)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ILI9341 SPI 屏驱动、图元和文字输出。
- 具体作用：作为模块公开接口，读取模块专用状态和数据；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
uint16_t TFT_ILI9341_GetHeight(const tft_ili9341_t *tft)
```

- 返回值（`uint16_t`）：返回当前旋转方向下的屏幕宽度/高度，单位像素。（依据：函数名语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `const tft_ili9341_t *` | [in] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_ili9341-c-tft_set_address_window_unlocked"></a>

### `tft_set_address_window_unlocked`

- 定义位置：[modules/signal_tft_ili9341.c:L307](modules/signal_tft_ili9341.c#L307)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ILI9341 SPI 屏驱动、图元和文字输出。
- 具体作用：作为文件内部辅助函数，设置TFT 显存写入窗口；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
static tft_ili9341_status_t tft_set_address_window_unlocked( tft_ili9341_t *tft, uint16_t x0, uint16_t y0, uint16_t x1, uint16_t y1)
```

- 返回值（`tft_ili9341_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_ili9341_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `x0` | `uint16_t` | [in] | 线段/矩形起点横坐标，单位像素。 | 签名/实现语义 |
| `y0` | `uint16_t` | [in] | 线段/矩形起点纵坐标，单位像素。 | 签名/实现语义 |
| `x1` | `uint16_t` | [in] | 线段/矩形终点横坐标，单位像素。 | 签名/实现语义 |
| `y1` | `uint16_t` | [in] | 线段/矩形终点纵坐标，单位像素。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_ili9341-c-TFT_ILI9341_SetAddressWindow"></a>

### `TFT_ILI9341_SetAddressWindow`

- 定义位置：[modules/signal_tft_ili9341.c:L350](modules/signal_tft_ili9341.c#L350)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ILI9341 SPI 屏驱动、图元和文字输出。
- 具体作用：作为模块公开接口，设置TFT 显存写入窗口；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
tft_ili9341_status_t TFT_ILI9341_SetAddressWindow(tft_ili9341_t *tft, uint16_t x0, uint16_t y0, uint16_t x1, uint16_t y1)
```

- 返回值（`tft_ili9341_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_ili9341_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `x0` | `uint16_t` | [in] | 线段/矩形起点横坐标，单位像素。 | 签名/实现语义 |
| `y0` | `uint16_t` | [in] | 线段/矩形起点纵坐标，单位像素。 | 签名/实现语义 |
| `x1` | `uint16_t` | [in] | 线段/矩形终点横坐标，单位像素。 | 签名/实现语义 |
| `y1` | `uint16_t` | [in] | 线段/矩形终点纵坐标，单位像素。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_ili9341-c-tft_write_repeated_color"></a>

### `tft_write_repeated_color`

- 定义位置：[modules/signal_tft_ili9341.c:L367](modules/signal_tft_ili9341.c#L367)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ILI9341 SPI 屏驱动、图元和文字输出。
- 具体作用：作为文件内部辅助函数，写入模块专用状态和数据；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
static tft_ili9341_status_t tft_write_repeated_color(tft_ili9341_t *tft, uint16_t color, uint32_t pixel_count)
```

- 返回值（`tft_ili9341_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_ili9341_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `color` | `uint16_t` | [in] | RGB565 颜色值。 | 签名/实现语义 |
| `pixel_count` | `uint32_t` | [in] | 有效元素、样本、字节或事件的数量。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_ili9341-c-tft_fill_rect_unlocked"></a>

### `tft_fill_rect_unlocked`

- 定义位置：[modules/signal_tft_ili9341.c:L392](modules/signal_tft_ili9341.c#L392)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ILI9341 SPI 屏驱动、图元和文字输出。
- 具体作用：内部辅助（ILI9341 SPI 屏驱动、图元和文字输出）：连续产生样本并填满调用者提供的输出缓冲区。
- 函数签名：

```c
static tft_ili9341_status_t tft_fill_rect_unlocked(tft_ili9341_t *tft, int32_t x, int32_t y, int32_t width, int32_t height, uint16_t color)
```

- 返回值（`tft_ili9341_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_ili9341_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `x` | `int32_t` | [in] | 横坐标、第一操作数或 X 通道值；显示函数中单位为像素。 | 签名/实现语义 |
| `y` | `int32_t` | [in] | 纵坐标、第二操作数或 Y 通道值；显示函数中单位为像素。 | 签名/实现语义 |
| `width` | `int32_t` | [in] | 图形、位图或显示区域宽度，通常单位为像素。 | 签名/实现语义 |
| `height` | `int32_t` | [in] | 图形、位图或显示区域高度，通常单位为像素。 | 签名/实现语义 |
| `color` | `uint16_t` | [in] | RGB565 颜色值。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_ili9341-c-TFT_ILI9341_FillRect"></a>

### `TFT_ILI9341_FillRect`

- 定义位置：[modules/signal_tft_ili9341.c:L440](modules/signal_tft_ili9341.c#L440)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ILI9341 SPI 屏驱动、图元和文字输出。
- 具体作用：公开接口（ILI9341 SPI 屏驱动、图元和文字输出）：连续产生样本并填满调用者提供的输出缓冲区。
- 函数签名：

```c
tft_ili9341_status_t TFT_ILI9341_FillRect(tft_ili9341_t *tft, int32_t x, int32_t y, int32_t width, int32_t height, uint16_t color)
```

- 返回值（`tft_ili9341_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_ili9341_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `x` | `int32_t` | [in] | 横坐标、第一操作数或 X 通道值；显示函数中单位为像素。 | 签名/实现语义 |
| `y` | `int32_t` | [in] | 纵坐标、第二操作数或 Y 通道值；显示函数中单位为像素。 | 签名/实现语义 |
| `width` | `int32_t` | [in] | 图形、位图或显示区域宽度，通常单位为像素。 | 签名/实现语义 |
| `height` | `int32_t` | [in] | 图形、位图或显示区域高度，通常单位为像素。 | 签名/实现语义 |
| `color` | `uint16_t` | [in] | RGB565 颜色值。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_ili9341-c-TFT_ILI9341_FillScreen"></a>

### `TFT_ILI9341_FillScreen`

- 定义位置：[modules/signal_tft_ili9341.c:L458](modules/signal_tft_ili9341.c#L458)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ILI9341 SPI 屏驱动、图元和文字输出。
- 具体作用：公开接口（ILI9341 SPI 屏驱动、图元和文字输出）：连续产生样本并填满调用者提供的输出缓冲区。
- 函数签名：

```c
tft_ili9341_status_t TFT_ILI9341_FillScreen(tft_ili9341_t *tft, uint16_t color)
```

- 返回值（`tft_ili9341_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_ili9341_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `color` | `uint16_t` | [in] | RGB565 颜色值。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_ili9341-c-TFT_ILI9341_DrawPixel"></a>

### `TFT_ILI9341_DrawPixel`

- 定义位置：[modules/signal_tft_ili9341.c:L467](modules/signal_tft_ili9341.c#L467)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ILI9341 SPI 屏驱动、图元和文字输出。
- 具体作用：作为模块公开接口，绘制模块专用状态和数据；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
tft_ili9341_status_t TFT_ILI9341_DrawPixel(tft_ili9341_t *tft, int32_t x, int32_t y, uint16_t color)
```

- 返回值（`tft_ili9341_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_ili9341_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `x` | `int32_t` | [in] | 横坐标、第一操作数或 X 通道值；显示函数中单位为像素。 | 签名/实现语义 |
| `y` | `int32_t` | [in] | 纵坐标、第二操作数或 Y 通道值；显示函数中单位为像素。 | 签名/实现语义 |
| `color` | `uint16_t` | [in] | RGB565 颜色值。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_ili9341-c-TFT_ILI9341_DrawLine"></a>

### `TFT_ILI9341_DrawLine`

- 定义位置：[modules/signal_tft_ili9341.c:L483](modules/signal_tft_ili9341.c#L483)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ILI9341 SPI 屏驱动、图元和文字输出。
- 具体作用：作为模块公开接口，绘制线段/曲线；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
tft_ili9341_status_t TFT_ILI9341_DrawLine(tft_ili9341_t *tft, int32_t x0, int32_t y0, int32_t x1, int32_t y1, uint16_t color)
```

- 返回值（`tft_ili9341_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_ili9341_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `x0` | `int32_t` | [in] | 线段/矩形起点横坐标，单位像素。 | 签名/实现语义 |
| `y0` | `int32_t` | [in] | 线段/矩形起点纵坐标，单位像素。 | 签名/实现语义 |
| `x1` | `int32_t` | [in] | 线段/矩形终点横坐标，单位像素。 | 签名/实现语义 |
| `y1` | `int32_t` | [in] | 线段/矩形终点纵坐标，单位像素。 | 签名/实现语义 |
| `color` | `uint16_t` | [in] | RGB565 颜色值。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_ili9341-c-TFT_ILI9341_DrawRect"></a>

### `TFT_ILI9341_DrawRect`

- 定义位置：[modules/signal_tft_ili9341.c:L527](modules/signal_tft_ili9341.c#L527)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ILI9341 SPI 屏驱动、图元和文字输出。
- 具体作用：作为模块公开接口，绘制模块专用状态和数据；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
tft_ili9341_status_t TFT_ILI9341_DrawRect(tft_ili9341_t *tft, int32_t x, int32_t y, int32_t width, int32_t height, uint16_t color)
```

- 返回值（`tft_ili9341_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_ili9341_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `x` | `int32_t` | [in] | 横坐标、第一操作数或 X 通道值；显示函数中单位为像素。 | 签名/实现语义 |
| `y` | `int32_t` | [in] | 纵坐标、第二操作数或 Y 通道值；显示函数中单位为像素。 | 签名/实现语义 |
| `width` | `int32_t` | [in] | 图形、位图或显示区域宽度，通常单位为像素。 | 签名/实现语义 |
| `height` | `int32_t` | [in] | 图形、位图或显示区域高度，通常单位为像素。 | 签名/实现语义 |
| `color` | `uint16_t` | [in] | RGB565 颜色值。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_ili9341-c-TFT_ILI9341_DrawRGB565"></a>

### `TFT_ILI9341_DrawRGB565`

- 定义位置：[modules/signal_tft_ili9341.c:L559](modules/signal_tft_ili9341.c#L559)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ILI9341 SPI 屏驱动、图元和文字输出。
- 具体作用：作为模块公开接口，绘制模块专用状态和数据；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
tft_ili9341_status_t TFT_ILI9341_DrawRGB565(tft_ili9341_t *tft, int32_t x, int32_t y, int32_t width, int32_t height, const uint16_t *pixels)
```

- 返回值（`tft_ili9341_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_ili9341_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `x` | `int32_t` | [in] | 横坐标、第一操作数或 X 通道值；显示函数中单位为像素。 | 签名/实现语义 |
| `y` | `int32_t` | [in] | 纵坐标、第二操作数或 Y 通道值；显示函数中单位为像素。 | 签名/实现语义 |
| `width` | `int32_t` | [in] | 图形、位图或显示区域宽度，通常单位为像素。 | 签名/实现语义 |
| `height` | `int32_t` | [in] | 图形、位图或显示区域高度，通常单位为像素。 | 签名/实现语义 |
| `pixels` | `const uint16_t *` | [in] | RGB565 像素数组。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_ili9341-c-TFT_ILI9341_GetFontMetrics"></a>

### `TFT_ILI9341_GetFontMetrics`

- 定义位置：[modules/signal_tft_ili9341.c:L643](modules/signal_tft_ili9341.c#L643)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ILI9341 SPI 屏驱动、图元和文字输出。
- 具体作用：作为模块公开接口，读取模块专用状态和数据；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
tft_ili9341_status_t TFT_ILI9341_GetFontMetrics( tft_ili9341_font_t font, uint8_t *width, uint8_t *height)
```

- 返回值（`tft_ili9341_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `font` | `tft_ili9341_font_t` | [in] | 字体描述符，包含字模尺寸、字符范围和位图地址。 | 签名/实现语义 |
| `width` | `uint8_t *` | [in/out] | 图形、位图或显示区域宽度，通常单位为像素。 | 签名/实现语义 |
| `height` | `uint8_t *` | [in/out] | 图形、位图或显示区域高度，通常单位为像素。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_ili9341-c-tft_bitmap_pixel"></a>

### `tft_bitmap_pixel`

- 定义位置：[modules/signal_tft_ili9341.c:L658](modules/signal_tft_ili9341.c#L658)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ILI9341 SPI 屏驱动、图元和文字输出。
- 具体作用：作为文件内部辅助函数，映射模块专用状态和数据；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
static bool tft_bitmap_pixel(const uint8_t *bitmap, size_t bytes_per_row, int32_t x, int32_t y)
```

- 返回值（`bool`）：返回布尔判定：`true` 表示 `tft_bitmap_pixel` 所检查的条件成立，`false` 表示不成立。（依据：签名/函数名语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `bitmap` | `const uint8_t *` | [in] | 单色或彩色位图数据。 | 签名/实现语义 |
| `bytes_per_row` | `size_t` | [in] | 位图每行占用的字节数。 | 签名/实现语义 |
| `x` | `int32_t` | [in] | 横坐标、第一操作数或 X 通道值；显示函数中单位为像素。 | 签名/实现语义 |
| `y` | `int32_t` | [in] | 纵坐标、第二操作数或 Y 通道值；显示函数中单位为像素。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_ili9341-c-TFT_ILI9341_DrawMonoBitmap"></a>

### `TFT_ILI9341_DrawMonoBitmap`

- 定义位置：[modules/signal_tft_ili9341.c:L669](modules/signal_tft_ili9341.c#L669)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ILI9341 SPI 屏驱动、图元和文字输出。
- 具体作用：作为模块公开接口，绘制模块专用状态和数据；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
tft_ili9341_status_t TFT_ILI9341_DrawMonoBitmap( tft_ili9341_t *tft, int32_t x, int32_t y, uint8_t width, uint8_t height, const uint8_t *bitmap, size_t bitmap_size, uint16_t foreground, uint16_t background, bool transparent_background)
```

- 返回值（`tft_ili9341_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_ili9341_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `x` | `int32_t` | [in] | 横坐标、第一操作数或 X 通道值；显示函数中单位为像素。 | 签名/实现语义 |
| `y` | `int32_t` | [in] | 纵坐标、第二操作数或 Y 通道值；显示函数中单位为像素。 | 签名/实现语义 |
| `width` | `uint8_t` | [in] | 图形、位图或显示区域宽度，通常单位为像素。 | 签名/实现语义 |
| `height` | `uint8_t` | [in] | 图形、位图或显示区域高度，通常单位为像素。 | 签名/实现语义 |
| `bitmap` | `const uint8_t *` | [in] | 单色或彩色位图数据。 | 签名/实现语义 |
| `bitmap_size` | `size_t` | [in] | 位图缓冲区字节数，用于边界检查。 | 签名/实现语义 |
| `foreground` | `uint16_t` | [in] | 文字/前景的 RGB565 颜色。 | 签名/实现语义 |
| `background` | `uint16_t` | [in] | 背景的 RGB565 颜色。 | 签名/实现语义 |
| `transparent_background` | `bool` | [in] | 是否跳过背景像素：true 为透明文字，false 会填背景色。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_ili9341-c-TFT_ILI9341_DrawChar"></a>

### `TFT_ILI9341_DrawChar`

- 定义位置：[modules/signal_tft_ili9341.c:L805](modules/signal_tft_ili9341.c#L805)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ILI9341 SPI 屏驱动、图元和文字输出。
- 具体作用：作为模块公开接口，绘制模块专用状态和数据；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
tft_ili9341_status_t TFT_ILI9341_DrawChar( tft_ili9341_t *tft, int32_t x, int32_t y, char character, tft_ili9341_font_t font, uint16_t foreground, uint16_t background, bool transparent_background)
```

- 返回值（`tft_ili9341_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_ili9341_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `x` | `int32_t` | [in] | 横坐标、第一操作数或 X 通道值；显示函数中单位为像素。 | 签名/实现语义 |
| `y` | `int32_t` | [in] | 纵坐标、第二操作数或 Y 通道值；显示函数中单位为像素。 | 签名/实现语义 |
| `character` | `char` | [in] | 待绘制或转换的字符。 | 签名/实现语义 |
| `font` | `tft_ili9341_font_t` | [in] | 字体描述符，包含字模尺寸、字符范围和位图地址。 | 签名/实现语义 |
| `foreground` | `uint16_t` | [in] | 文字/前景的 RGB565 颜色。 | 签名/实现语义 |
| `background` | `uint16_t` | [in] | 背景的 RGB565 颜色。 | 签名/实现语义 |
| `transparent_background` | `bool` | [in] | 是否跳过背景像素：true 为透明文字，false 会填背景色。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_ili9341-c-TFT_ILI9341_DrawString"></a>

### `TFT_ILI9341_DrawString`

- 定义位置：[modules/signal_tft_ili9341.c:L835](modules/signal_tft_ili9341.c#L835)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ILI9341 SPI 屏驱动、图元和文字输出。
- 具体作用：作为模块公开接口，绘制模块专用状态和数据；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
tft_ili9341_status_t TFT_ILI9341_DrawString( tft_ili9341_t *tft, int32_t x, int32_t y, const char *text, tft_ili9341_font_t font, uint16_t foreground, uint16_t background, bool transparent_background, bool wrap)
```

- 返回值（`tft_ili9341_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_ili9341_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `x` | `int32_t` | [in] | 横坐标、第一操作数或 X 通道值；显示函数中单位为像素。 | 签名/实现语义 |
| `y` | `int32_t` | [in] | 纵坐标、第二操作数或 Y 通道值；显示函数中单位为像素。 | 签名/实现语义 |
| `text` | `const char *` | [in] | 以 NUL 结尾的待显示/处理字符串。 | 签名/实现语义 |
| `font` | `tft_ili9341_font_t` | [in] | 字体描述符，包含字模尺寸、字符范围和位图地址。 | 签名/实现语义 |
| `foreground` | `uint16_t` | [in] | 文字/前景的 RGB565 颜色。 | 签名/实现语义 |
| `background` | `uint16_t` | [in] | 背景的 RGB565 颜色。 | 签名/实现语义 |
| `transparent_background` | `bool` | [in] | 是否跳过背景像素：true 为透明文字，false 会填背景色。 | 签名/实现语义 |
| `wrap` | `bool` | [in] | 文字到达屏幕边缘后是否自动换行。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_ili9341-c-tft_append_uint32"></a>

### `tft_append_uint32`

- 定义位置：[modules/signal_tft_ili9341.c:L884](modules/signal_tft_ili9341.c#L884)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ILI9341 SPI 屏驱动、图元和文字输出。
- 具体作用：作为文件内部辅助函数，追加整数；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
static size_t tft_append_uint32(char *buffer, size_t position, uint32_t value, uint8_t minimum_digits)
```

- 返回值（`size_t`）：返回当前缓冲区元素数或已追加的字符数；具体含义由函数作用决定。（依据：类型/函数名语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `buffer` | `char *` | [in/out] | 调用者提供的工作或输出缓冲区，生命周期必须覆盖本次操作。 | 签名/实现语义 |
| `position` | `size_t` | [in] | 数组、波表、像素或状态表中的索引位置。 | 签名/实现语义 |
| `value` | `uint32_t` | [in] | 要设置、换算或限制的输入值；量纲见函数名和相邻参数。 | 签名/实现语义 |
| `minimum_digits` | `uint8_t` | [in] | 整数显示的最少位数，不足时补前导字符。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_ili9341-c-TFT_ILI9341_DrawInt32"></a>

### `TFT_ILI9341_DrawInt32`

- 定义位置：[modules/signal_tft_ili9341.c:L904](modules/signal_tft_ili9341.c#L904)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ILI9341 SPI 屏驱动、图元和文字输出。
- 具体作用：作为模块公开接口，绘制整数；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
tft_ili9341_status_t TFT_ILI9341_DrawInt32( tft_ili9341_t *tft, int32_t x, int32_t y, int32_t value, tft_ili9341_font_t font, uint16_t foreground, uint16_t background, bool transparent_background)
```

- 返回值（`tft_ili9341_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_ili9341_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `x` | `int32_t` | [in] | 横坐标、第一操作数或 X 通道值；显示函数中单位为像素。 | 签名/实现语义 |
| `y` | `int32_t` | [in] | 纵坐标、第二操作数或 Y 通道值；显示函数中单位为像素。 | 签名/实现语义 |
| `value` | `int32_t` | [in] | 要设置、换算或限制的输入值；量纲见函数名和相邻参数。 | 签名/实现语义 |
| `font` | `tft_ili9341_font_t` | [in] | 字体描述符，包含字模尺寸、字符范围和位图地址。 | 签名/实现语义 |
| `foreground` | `uint16_t` | [in] | 文字/前景的 RGB565 颜色。 | 签名/实现语义 |
| `background` | `uint16_t` | [in] | 背景的 RGB565 颜色。 | 签名/实现语义 |
| `transparent_background` | `bool` | [in] | 是否跳过背景像素：true 为透明文字，false 会填背景色。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_ili9341-c-TFT_ILI9341_DrawFloat"></a>

### `TFT_ILI9341_DrawFloat`

- 定义位置：[modules/signal_tft_ili9341.c:L932](modules/signal_tft_ili9341.c#L932)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ILI9341 SPI 屏驱动、图元和文字输出。
- 具体作用：作为模块公开接口，绘制浮点数；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
tft_ili9341_status_t TFT_ILI9341_DrawFloat( tft_ili9341_t *tft, int32_t x, int32_t y, float value, uint8_t decimal_places, tft_ili9341_font_t font, uint16_t foreground, uint16_t background, bool transparent_background)
```

- 返回值（`tft_ili9341_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_ili9341_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `x` | `int32_t` | [in] | 横坐标、第一操作数或 X 通道值；显示函数中单位为像素。 | 签名/实现语义 |
| `y` | `int32_t` | [in] | 纵坐标、第二操作数或 Y 通道值；显示函数中单位为像素。 | 签名/实现语义 |
| `value` | `float` | [in] | 要设置、换算或限制的输入值；量纲见函数名和相邻参数。 | 签名/实现语义 |
| `decimal_places` | `uint8_t` | [in] | 十进制小数位数。 | 签名/实现语义 |
| `font` | `tft_ili9341_font_t` | [in] | 字体描述符，包含字模尺寸、字符范围和位图地址。 | 签名/实现语义 |
| `foreground` | `uint16_t` | [in] | 文字/前景的 RGB565 颜色。 | 签名/实现语义 |
| `background` | `uint16_t` | [in] | 背景的 RGB565 颜色。 | 签名/实现语义 |
| `transparent_background` | `bool` | [in] | 是否跳过背景像素：true 为透明文字，false 会填背景色。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_ili9341-c-SignalTFTILI9341_GetModuleStatus"></a>

### `SignalTFTILI9341_GetModuleStatus`

- 定义位置：[modules/signal_tft_ili9341.c:L997](modules/signal_tft_ili9341.c#L997)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ILI9341 SPI 屏驱动、图元和文字输出。
- 具体作用：读取模块当前的验证成熟度或静态状态，不会启动硬件操作。
- 函数签名：

```c
signal_module_status_t SignalTFTILI9341_GetModuleStatus(void)
```

- 返回值（`signal_module_status_t`）：返回 signal_module_status_t 类型结果；调用者应检查该值。（依据：源码 `@return`）
- 参数：无（`void`）。


<a id="file-modules-signal_tft_ili9341_mspm0g3507-c"></a>

## `modules/signal_tft_ili9341_mspm0g3507.c`

<a id="fn-modules-signal_tft_ili9341_mspm0g3507-c-SignalTFTMSPM0_Write"></a>

### `SignalTFTMSPM0_Write`

- 定义位置：[modules/signal_tft_ili9341_mspm0g3507.c:L14](modules/signal_tft_ili9341_mspm0g3507.c#L14)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ILI9341 到 MSPM0 SPI/GPIO 的平台适配。
- 具体作用：作为文件内部辅助函数，写入模块专用状态和数据；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
static int SignalTFTMSPM0_Write( void *context, const uint8_t *data, size_t length)
```

- 返回值（`int`）：返回总线回调状态；0 通常表示发送成功，非 0 表示失败。（依据：实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `context` | `void *` | [in/out] | 驱动/算法上下文或回调用户数据，用于保存实例状态并避免依赖全局变量。 | 签名/实现语义 |
| `data` | `const uint8_t *` | [in] | 待处理的数据/总线字节缓冲区；是否原地修改由参数方向和函数说明决定。 | 签名/实现语义 |
| `length` | `size_t` | [in] | 数据、文本或波表的有效长度。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_ili9341_mspm0g3507-c-SignalTFTMSPM0_SetDC"></a>

### `SignalTFTMSPM0_SetDC`

- 定义位置：[modules/signal_tft_ili9341_mspm0g3507.c:L29](modules/signal_tft_ili9341_mspm0g3507.c#L29)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ILI9341 到 MSPM0 SPI/GPIO 的平台适配。
- 具体作用：作为文件内部辅助函数，设置模块专用状态和数据；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
static void SignalTFTMSPM0_SetDC(void *context, bool high)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `context` | `void *` | [in/out] | 驱动/算法上下文或回调用户数据，用于保存实例状态并避免依赖全局变量。 | 签名/实现语义 |
| `high` | `bool` | [in] | 高电平、上限或区间较大端点。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_ili9341_mspm0g3507-c-SignalTFTMSPM0_SetBacklight"></a>

### `SignalTFTMSPM0_SetBacklight`

- 定义位置：[modules/signal_tft_ili9341_mspm0g3507.c:L39](modules/signal_tft_ili9341_mspm0g3507.c#L39)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ILI9341 到 MSPM0 SPI/GPIO 的平台适配。
- 具体作用：作为文件内部辅助函数，设置模块专用状态和数据；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
static void SignalTFTMSPM0_SetBacklight(void *context, bool high)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `context` | `void *` | [in/out] | 驱动/算法上下文或回调用户数据，用于保存实例状态并避免依赖全局变量。 | 签名/实现语义 |
| `high` | `bool` | [in] | 高电平、上限或区间较大端点。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_ili9341_mspm0g3507-c-SignalTFTMSPM0_DelayMs"></a>

### `SignalTFTMSPM0_DelayMs`

- 定义位置：[modules/signal_tft_ili9341_mspm0g3507.c:L49](modules/signal_tft_ili9341_mspm0g3507.c#L49)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ILI9341 到 MSPM0 SPI/GPIO 的平台适配。
- 具体作用：作为文件内部辅助函数，延时模块专用状态和数据；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
static void SignalTFTMSPM0_DelayMs(void *context, uint32_t milliseconds)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `context` | `void *` | [in/out] | 驱动/算法上下文或回调用户数据，用于保存实例状态并避免依赖全局变量。 | 签名/实现语义 |
| `milliseconds` | `uint32_t` | [in] | 延时时间，单位 ms。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_ili9341_mspm0g3507-c-SignalTFTILI9341_MSPM0_Init"></a>

### `SignalTFTILI9341_MSPM0_Init`

- 定义位置：[modules/signal_tft_ili9341_mspm0g3507.c:L58](modules/signal_tft_ili9341_mspm0g3507.c#L58)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ILI9341 到 MSPM0 SPI/GPIO 的平台适配。
- 具体作用：公开接口（ILI9341 到 MSPM0 SPI/GPIO 的平台适配）：初始化模块对象、硬件绑定和运行状态。
- 函数签名：

```c
tft_ili9341_status_t SignalTFTILI9341_MSPM0_Init( tft_ili9341_t *tft, tft_ili9341_rotation_t rotation)
```

- 返回值（`tft_ili9341_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_ili9341_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `rotation` | `tft_ili9341_rotation_t` | [in] | 屏幕旋转枚举，决定宽高和坐标方向。 | 签名/实现语义 |

## 7. 赛场复用检查表

1. 先从第 4 节确认应用实际调用的公开 API，再到第 6 节核对参数、返回值及 `static` 依赖。
2. 复制纯算法模块时同时复制同名 `.c/.h`、公共状态类型以及 `signal_math*` 依赖。
3. 复制 ADC/DMA、DAC、Capture、TFT、键盘、OPA/GPAMP/COMP 等硬件模块时，同步复现第 2 节 SysConfig 实例名和事件/IRQ/引脚。
4. 核对 `signal_config.h` 中的 Fs、N、VREF、ADC/DAC 位数、DMA 块数和显示尺寸；这些量决定参数单位和数组容量。
5. `example05` 标为构建排除的旧 DAC/DDS 文件不能因为“目录里存在”就认为当前固件会编译执行。
6. 本手册是静态审计结果，不等同于示波器/逻辑分析仪/实板验证；赛前至少对采样率、相位符号、门限电压和引脚复用做一次硬件复核。

---

生成器：`tools/generate_per_project_function_docs.py`；本文件校验记录：91 个函数、219 个具名参数。
