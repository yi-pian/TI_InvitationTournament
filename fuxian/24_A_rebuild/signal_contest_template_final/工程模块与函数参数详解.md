# 24_A_rebuild：工程模块与函数参数详解

> 工程定位：24_A 四问应用（DDS/VCA、扫频带宽、压摆率、静态功耗）。
> 本文由当前源码静态生成，行号均指向函数定义或调用的起始行；修改源码后请重新运行生成器。

## 1. 工程功能与主数据流

SysConfig 初始化后建立 AD9850、VCA820、ADC/DMA、键盘和 ST7789；按题号执行幅值控制、扫频带宽、压摆率或静态功耗测量，并把结果刷新到屏幕。

### 统计与阅读边界

- 工程内函数定义：**114** 个，其中 `main.c` 28 个、`modules` 86 个。
- 软件模块文件族：**14** 个；SysConfig 实例/单例记录：**12** 条。
- 函数目录覆盖 `main.c`、`modules/*.c` 和 `modules/*.h` 中带函数体的内联函数；不重复收录纯声明、宏、`Debug/Release` 生成副本和 TI/CMSIS 外部库函数体。
- 参数说明优先引用中文 `@param`；无注释时根据声明类型、命名、所属模块及实现语义整理，并在“说明依据”列标出。
- `[in/out]` 表示指针指向的对象可能被读取并更新。实际数组长度、单位和合法范围仍应以相邻参数、配置宏和函数体检查为准。

## 2. SysConfig 硬件/SDK 模块

这些名称来自 `signal_contest_template.syscfg`。迁移硬件函数时，必须同步迁移实例名、物理外设、DMA 通道、事件、IRQ 和引脚。

| DriverLib 模块 | SysConfig 变量 / 逻辑实例名 | 物理外设 | 关键配置摘要 |
|---|---|---|---|
| `ADC12` | `ADC122` / `POWER_ADC` | `ADC1` | adcMem0chansel="DL_ADC12_INPUT_CHAN_2"；enabledInterrupts=["DL_ADC12_INTERRUPT_MEM0_RESULT_LOADED"]；sampClkSrc="DL_ADC12_CLOCK_ULPCLK"；引脚：peripheral.adcPin2.$assign=PA17 |
| `ADC12` | `ADC121` / `SIGNAL_ADC` | `ADC0` | adcMem0chansel="DL_ADC12_INPUT_CHAN_2"；trigSrc="DL_ADC12_TRIG_SRC_EVENT"；configureDMA=true；enabledInterrupts=["DL_ADC12_INTERRUPT_DMA_DONE"]；sampClkSrc="DL_ADC12_CLOCK_ULPCLK"；DMA_CHANNEL.$name="SIGNAL_ADC_DMA"；DMA_CHANNEL.peripheral.$assign="DMA_CH0"；引脚：peripheral.adcPin2.$assign=PA25 |
| `Board` | `Board` / `Board` | `—` | 使用 SysConfig/SDK 默认值或仅参与全局配置 |
| `DAC12` | `DAC12` / `DAC12` | `DAC0` | 引脚：peripheral.OutPin.$assign=PA15 |
| `GPIO` | `GPIO1` / `DDS_GPIO` | `—` | 引脚：associatedPins[0].pin.$assign=PA12，associatedPins[1].pin.$assign=PA13，associatedPins[2].pin.$assign=PA28，associatedPins[3].pin.$assign=PA31，W_CLK=PORTA12，FQ_UD=PORTA13，DATA=PORTA28，RESET=PORTA31 |
| `GPIO` | `GPIO2` / `GPIO_KEYPAD` | `—` | 引脚：associatedPins[0].pin.$assign=PB16，KEYPAD_R1=PORTB16，KEYPAD_R2=PORTB0，KEYPAD_R3=PORTB7，KEYPAD_R4=PORTB17，KEYPAD_C1=PORTB18(INPUT/PULL_UP)，KEYPAD_C2=PORTB13(INPUT/PULL_UP)，KEYPAD_C3=PORTB20(INPUT/PULL_UP)，KEYPAD_C4=PORTB4(INPUT/PULL_UP) |
| `GPIO` | `GPIO3` / `GPIO_TFT_CTRL` | `—` | 引脚：TFT_DC=PORTB15，TFT_BLK=PORTB12 |
| `ProjectConfig` | `ProjectConfig` / `ProjectConfig` | `—` | 使用 SysConfig/SDK 默认值或仅参与全局配置 |
| `SPI` | `SPI1` / `SPI_TFT` | `—` | targetBitRate=1000000；引脚：peripheral.sclkPin.$assign=PB9，peripheral.mosiPin.$assign=PB8，peripheral.cs0Pin.$assign=PB6 |
| `SYSCTL` | `SYSCTL` / `SYSCTL` | `—` | 使用 SysConfig/SDK 默认值或仅参与全局配置 |
| `TIMER` | `TIMER1` / `SIGNAL_SAMPLE_TIMER` | `TIMG0` | timerPeriod="10us"；timerMode="PERIODIC" |
| `UART` | `UART1` / `SIGNAL_UART` | `UART0` | profile="CONFIG_PROFILE_1"；引脚：peripheral.rxPin.$assign=PA11，peripheral.txPin.$assign=PA10 |

## 3. 软件模块清单

“main 直接调用”只统计 `main.c` 的显式调用；0 次不等于无用，它可能由其他模块调用、作为 ISR/回调入口，或仅提供类型与配置。

| 模块文件族 | 功能分类 | 模块职责 | 函数定义数 | main 直接调用的接口 | 构建状态 |
|---|---|---|---:|---|---|
| `modules/ad9850.c`、`modules/ad9850.h` | 外部器件与阻塞式总线 | AD9850 并行/串行位操作、调谐字和输出控制 | 14 | `AD9850_Init`、`AD9850_SetFrequencyHz`、`AD9850_SetPowerDown` | 随工程编译/头文件依赖 |
| `modules/ad9850_mspm0_platform.c`、`modules/ad9850_mspm0_platform.h` | 外部器件与阻塞式总线 | AD9850 到 MSPM0 GPIO/延时接口的适配层 | 3 | — | 随工程编译/头文件依赖 |
| `modules/signal_adc_dma.c`、`modules/signal_adc_dma.h` | ADC、DMA、连续采集与缓冲 | 单路 ADC，由定时器事件触发并通过 DMA 采集 | 12 | `SignalADC_GetConfiguredTriggerRate`、`SignalADC_Init`、`SignalADC_IsFinished`、`SignalADC_SetSampleRate`、`SignalADC_Start` | 随工程编译/头文件依赖 |
| `modules/signal_algorithm_status.h` | 数学、状态与内部公共支持 | 算法状态码公共定义 | 0 | — | 随工程编译/头文件依赖 |
| `modules/signal_matrix_keypad_4x4.c`、`modules/signal_matrix_keypad_4x4.h` | 矩阵键盘与交互输入 | 4×4 矩阵键盘行列扫描、消抖、鬼键过滤和符号映射 | 11 | `SignalMatrixKeypad4x4_ReadNewSymbol` | 随工程编译/头文件依赖 |
| `modules/signal_robust_peak_to_peak.c`、`modules/signal_robust_peak_to_peak.h` | 稳健估计、校准与补偿 | 分位数峰峰值，降低毛刺影响 | 4 | `SignalRobustPeakToPeak_Process` | 随工程编译/头文件依赖 |
| `modules/signal_slew_rate.c`、`modules/signal_slew_rate.h` | 模拟前端、比较器、增益与功耗 | 从波形上升/下降沿计算压摆率 | 2 | `SignalSlewRate_Process` | 随工程编译/头文件依赖 |
| `modules/signal_static_power.c`、`modules/signal_static_power.h` | 模拟前端、比较器、增益与功耗 | 供电电压、电流到静态功耗的换算 | 1 | `SignalStaticPower_Calculate` | 随工程编译/头文件依赖 |
| `modules/signal_status.h` | 数学、状态与内部公共支持 | 驱动/模块统一状态码 | 0 | — | 随工程编译/头文件依赖 |
| `modules/signal_tft_st7789.c`、`modules/signal_tft_st7789.h` | TFT、字库、波形与界面绘制 | ST7789 SPI 屏驱动和基础图元 | 24 | `TFT_ST7789_FillRect`、`TFT_ST7789_FillScreen` | 随工程编译/头文件依赖 |
| `modules/signal_tft_st7789_font.c`、`modules/signal_tft_st7789_font.h` | TFT、字库、波形与界面绘制 | ST7789 多尺寸 ASCII 字库、数字和浮点文本 | 8 | `TFT_ST7789_DrawFloat`、`TFT_ST7789_DrawInt32`、`TFT_ST7789_DrawString` | 随工程编译/头文件依赖 |
| `modules/signal_tft_st7789_mspm0g3507.c`、`modules/signal_tft_st7789_mspm0g3507.h` | TFT、字库、波形与界面绘制 | ST7789 到 MSPM0 SPI/GPIO 的平台适配 | 6 | `SignalTFTST7789_MSPM0_Init` | 随工程编译/头文件依赖 |
| `modules/signal_types.h` | 数学、状态与内部公共支持 | 采样率、波形等公共数据类型 | 0 | — | 随工程编译/头文件依赖 |
| `modules/signal_vca820_gain_control.c`、`modules/signal_vca820_gain_control.h` | 模拟前端、比较器、增益与功耗 | VCA820 控制电压、增益和 DAC 码预算 | 1 | `SignalVCA820_TargetVppToDACCode` | 随工程编译/头文件依赖 |

## 4. main.c 实际使用的模块接口

下表回答“这个工程实际调用了模块里的哪些函数”。调用位置给出当前实参；完整形参、方向和意义见后面的函数目录。

| 模块接口 | 定义文件 | 作用 | main.c 调用位置与当前实参 |
|---|---|---|---|
| `AD9850_Init` | `modules/ad9850.c` | 公开接口（AD9850 并行/串行位操作、调谐字和输出控制）：初始化模块对象、硬件绑定和运行状态。 | [main.c:L972](main.c#L972)：`AD9850_Init(&g_ad9850, &g_ad9850_config)` |
| `AD9850_SetFrequencyHz` | `modules/ad9850.c` | 公开接口（AD9850 并行/串行位操作、调谐字和输出控制）：重算相位步进或调谐字并设置输出频率。 | [main.c:L268](main.c#L268)：`AD9850_SetFrequencyHz(&g_ad9850, frequency_hz)`<br>[main.c:L395](main.c#L395)：`AD9850_SetFrequencyHz(&g_ad9850, frequency_hz)` |
| `AD9850_SetPowerDown` | `modules/ad9850.c` | 设置或清除 AD9850 控制字中的掉电位。 | [main.c:L267](main.c#L267)：`AD9850_SetPowerDown(&g_ad9850, false)`<br>[main.c:L554](main.c#L554)：`AD9850_SetPowerDown(&g_ad9850, true)` |
| `SignalADC_GetConfiguredTriggerRate` | `modules/signal_adc_dma.c` | 取得由 Timer 整数计数配置推导出的事件触发率。 | [main.c:L472](main.c#L472)：`SignalADC_GetConfiguredTriggerRate()` |
| `SignalADC_Init` | `modules/signal_adc_dma.c` | 初始化 ADC+DMA 模块的运行时状态。 | [main.c:L970](main.c#L970)：`SignalADC_Init(&adc_config)` |
| `SignalADC_IsFinished` | `modules/signal_adc_dma.c` | 查询本次采集是否完成。 | [main.c:L313](main.c#L313)：`SignalADC_IsFinished()` |
| `SignalADC_SetSampleRate` | `modules/signal_adc_dma.c` | 修改目标采样事件率并计算最近的整数 Timer 配置触发率。 | [main.c:L367](main.c#L367)：`SignalADC_SetSampleRate(APP_WAVE_SAMPLE_RATE_HZ)`<br>[main.c:L470](main.c#L470)：`SignalADC_SetSampleRate(MODE3_WAVE_SAMPLE_RATE_HZ)` |
| `SignalADC_Start` | `modules/signal_adc_dma.c` | 启动一次 N 点采集。 | [main.c:L310](main.c#L310)：`SignalADC_Start(g_wave_capture.raw, APP_WAVE_SAMPLE_COUNT)` |
| `SignalMatrixKeypad4x4_ReadNewSymbol` | `modules/signal_matrix_keypad_4x4.c` | 从键盘事件队列读取一次新按下事件并映射为字符；没有新事件时返回 false。 | [main.c:L988](main.c#L988)：`SignalMatrixKeypad4x4_ReadNewSymbol(&symbol)` |
| `SignalRobustPeakToPeak_Process` | `modules/signal_robust_peak_to_peak.c` | 用上下分位数差代替 max-min，降低少量极端毛刺影响。 | [main.c:L477](main.c#L477)：`SignalRobustPeakToPeak_Process(g_wave_voltage, APP_WAVE_SAMPLE_COUNT, &g_mode3_robust_config, g_wave_capture.robust_workspace, APP_WAVE_SAMPLE_COUNT, &levels)` |
| `SignalSlewRate_Process` | `modules/signal_slew_rate.c` | 公开接口（从波形上升/下降沿计算压摆率）：对输入数组执行该模块的核心算法并写入结果结构。 | [main.c:L490](main.c#L490)：`SignalSlewRate_Process(g_wave_voltage, APP_WAVE_SAMPLE_COUNT, levels.lower_voltage_v, levels.upper_voltage_v, configured_sample_rate_hz, &g_mode3_slew_config, &edge_times)` |
| `SignalStaticPower_Calculate` | `modules/signal_static_power.c` | 由供电电压和电流计算静态功耗，并按输出指针返回电流与功率。 | [main.c:L565](main.c#L565)：`SignalStaticPower_Calculate(g_mode4_shunt_voltage_v, MODE4_SHUNT_RESISTANCE_OHM, MODE4_SUPPLY_VOLTAGE_V, MODE4_RAIL_COUNT_FACTOR, &g_mode4_current_ma, &g_mode4_power_mw)` |
| `SignalTFTST7789_MSPM0_Init` | `modules/signal_tft_st7789_mspm0g3507.c` | 公开接口（ST7789 到 MSPM0 SPI/GPIO 的平台适配）：初始化模块对象、硬件绑定和运行状态。 | [main.c:L978](main.c#L978)：`SignalTFTST7789_MSPM0_Init(&g_tft, TFT_ST7789_ROTATION_270, 0U, 0U)` |
| `SignalVCA820_TargetVppToDACCode` | `modules/signal_vca820_gain_control.c` | 作为模块公开接口，读取DAC 数据/配置；具体输入、输出和量纲见下方参数及返回值说明。 | [main.c:L277](main.c#L277)：`SignalVCA820_TargetVppToDACCode(target_vpp, &g_vca820_config, &dac_code)` |
| `TFT_ST7789_DrawFloat` | `modules/signal_tft_st7789_font.c` | 按指定小数位格式化浮点数后用指定字体绘制。 | [main.c:L762](main.c#L762)：`TFT_ST7789_DrawFloat(&g_tft, 104, y, value, precision, TFT_ST7789_FONT_8X16, color, TFT_ST7789_BLACK, false)`<br>[main.c:L929](main.c#L929)：`TFT_ST7789_DrawFloat(&g_tft, 104, 104, g_target_output_vpp, 2U, TFT_ST7789_FONT_8X16, TFT_ST7789_GREEN, TFT_ST7789_BLACK, false)` |
| `TFT_ST7789_DrawInt32` | `modules/signal_tft_st7789_font.c` | 把有符号 32 位整数格式化后用指定字体绘制。 | [main.c:L741](main.c#L741)：`TFT_ST7789_DrawInt32(&g_tft, 104, y, value, TFT_ST7789_FONT_8X16, color, TFT_ST7789_BLACK, false)`<br>[main.c:L924](main.c#L924)：`TFT_ST7789_DrawInt32(&g_tft, 104, 72, (int32_t)g_target_frequency_hz, TFT_ST7789_FONT_8X16, TFT_ST7789_GREEN, TFT_ST7789_BLACK, false)`<br>[main.c:L943](main.c#L943)：`TFT_ST7789_DrawInt32(&g_tft, 104, 168, (int32_t)g_keypad_input_value, TFT_ST7789_FONT_8X16, TFT_ST7789_WHITE, TFT_ST7789_BLACK, false)` |
| `TFT_ST7789_DrawString` | `modules/signal_tft_st7789_font.c` | 逐字符绘制 NUL 结尾字符串，并按配置处理换行和透明背景。 | [main.c:L710](main.c#L710)：`TFT_ST7789_DrawString(&g_tft, 8, y, label, TFT_ST7789_FONT_8X16, TFT_ST7789_WHITE, TFT_ST7789_BLACK, false, false)`<br>[main.c:L725](main.c#L725)：`TFT_ST7789_DrawString(&g_tft, 104, y, text, TFT_ST7789_FONT_8X16, color, TFT_ST7789_BLACK, false, false)`<br>[main.c:L745](main.c#L745)：`TFT_ST7789_DrawString(&g_tft, 240, y, unit, TFT_ST7789_FONT_8X16, TFT_ST7789_WHITE, TFT_ST7789_BLACK, false, false)`<br>[main.c:L766](main.c#L766)：`TFT_ST7789_DrawString(&g_tft, 240, y, unit, TFT_ST7789_FONT_8X16, TFT_ST7789_WHITE, TFT_ST7789_BLACK, false, false)`<br>[main.c:L776](main.c#L776)：`TFT_ST7789_DrawString(&g_tft, 8, 8, "2024A OPAMP TESTER", TFT_ST7789_FONT_8X16, TFT_ST7789_CYAN, TFT_ST7789_BLACK, false, false)`<br>[main.c:L937](main.c#L937)：`TFT_ST7789_DrawString(&g_tft, 104, 136, edit_text, TFT_ST7789_FONT_8X16, TFT_ST7789_YELLOW, TFT_ST7789_BLACK, false, false)`<br>[main.c:L947](main.c#L947)：`TFT_ST7789_DrawString(&g_tft, 104, 168, "-", TFT_ST7789_FONT_8X16, TFT_ST7789_WHITE, TFT_ST7789_BLACK, false, false)` |
| `TFT_ST7789_FillRect` | `modules/signal_tft_st7789.c` | 裁剪目标区域后用同一 RGB565 颜色填充矩形。 | [main.c:L708](main.c#L708)：`TFT_ST7789_FillRect(&g_tft, 8, y, 304, 16, TFT_ST7789_BLACK)`<br>[main.c:L922](main.c#L922)：`TFT_ST7789_FillRect(&g_tft, 104, 72, 128, 16, TFT_ST7789_BLACK)`<br>[main.c:L927](main.c#L927)：`TFT_ST7789_FillRect(&g_tft, 104, 104, 128, 16, TFT_ST7789_BLACK)`<br>[main.c:L935](main.c#L935)：`TFT_ST7789_FillRect(&g_tft, 104, 136, 64, 16, TFT_ST7789_BLACK)`<br>[main.c:L940](main.c#L940)：`TFT_ST7789_FillRect(&g_tft, 104, 168, 136, 16, TFT_ST7789_BLACK)` |
| `TFT_ST7789_FillScreen` | `modules/signal_tft_st7789.c` | 以指定 RGB565 颜色填充整块屏幕。 | [main.c:L980](main.c#L980)：`TFT_ST7789_FillScreen(&g_tft, TFT_ST7789_BLACK)` |

## 5. main.c 直接调用的 SDK / CMSIS-DSP / C 数学接口

这里只记录工程源码直接调用的外部库函数；其函数体属于 TI SDK、CMSIS 或 C 运行库，不计入本工程函数定义总数。实参按当前源码原样列出，迁移时要同时核对目标 SDK 版本的原型。

### `DL_ADC12_clearInterruptStatus`

- 作用：清除 ADC12 指定中断标志。
- 形参与意义：

| 形参 | 方向 | 意义 |
|---|---|---|
| `adc` | [in] | ADC 外设基地址。 |
| `mask` | [in] | 要清除的中断位掩码。 |
- 返回：无返回值。
- 当前工程调用：[main.c:L529](main.c#L529)：`DL_ADC12_clearInterruptStatus(POWER_ADC_INST, DL_ADC12_INTERRUPT_MEM0_RESULT_LOADED)`

### `DL_ADC12_enableConversions`

- 作用：允许指定 ADC12 实例开始转换。
- 形参与意义：

| 形参 | 方向 | 意义 |
|---|---|---|
| `adc` | [in] | ADC 外设基地址，例如 SIGNAL_ADC_INST。 |
- 返回：无返回值。
- 当前工程调用：[main.c:L543](main.c#L543)：`DL_ADC12_enableConversions(POWER_ADC_INST)`

### `DL_ADC12_getMemResult`

- 作用：读取 ADC12 某个 MEM 转换结果寄存器。
- 形参与意义：

| 形参 | 方向 | 意义 |
|---|---|---|
| `adc` | [in] | ADC 外设基地址。 |
| `memory` | [in] | 结果存储器索引，如 DL_ADC12_MEM_IDX_0。 |
- 返回：返回该 MEM 中的 ADC 原始码。
- 当前工程调用：[main.c:L539](main.c#L539)：`DL_ADC12_getMemResult(POWER_ADC_INST, POWER_ADC_ADCMEM_0)`

### `DL_ADC12_getRawInterruptStatus`

- 作用：读取 ADC12 原始中断状态。
- 形参与意义：

| 形参 | 方向 | 意义 |
|---|---|---|
| `adc` | [in] | ADC 外设基地址。 |
| `mask` | [in] | 要查询的 ADC 中断位掩码。 |
- 返回：返回与掩码相交的原始中断状态位。
- 当前工程调用：[main.c:L534](main.c#L534)：`DL_ADC12_getRawInterruptStatus(POWER_ADC_INST, DL_ADC12_INTERRUPT_MEM0_RESULT_LOADED)`

### `DL_ADC12_startConversion`

- 作用：向指定 ADC12 实例发出软件启动转换命令。
- 形参与意义：

| 形参 | 方向 | 意义 |
|---|---|---|
| `adc` | [in] | ADC 外设基地址。 |
- 返回：无返回值。
- 当前工程调用：[main.c:L532](main.c#L532)：`DL_ADC12_startConversion(POWER_ADC_INST)`

### `DL_Common_delayCycles`

- 作用：按 CPU 周期进行忙等待。
- 形参与意义：

| 形参 | 方向 | 意义 |
|---|---|---|
| `cycles` | [in] | 要等待的内核周期数；实际时间还取决于 CPU 时钟。 |
- 返回：无返回值。
- 当前工程调用：[main.c:L258](main.c#L258)：`DL_Common_delayCycles(CPUCLK_FREQ / 1000000U)`；[main.c:L1021](main.c#L1021)：`DL_Common_delayCycles(CPUCLK_FREQ / 200U)`

### `DL_DAC12_output12`

- 作用：把 12 位数字码写到片内 DAC12 输出。
- 形参与意义：

| 形参 | 方向 | 意义 |
|---|---|---|
| `dac` | [in] | DAC 外设基地址。 |
| `code` | [in] | 0～4095 的 12 位输出码。 |
- 返回：无返回值。
- 当前工程调用：[main.c:L287](main.c#L287)：`DL_DAC12_output12(DAC0, dac_code)`；[main.c:L465](main.c#L465)：`DL_DAC12_output12(DAC0, 0U)`；[main.c:L556](main.c#L556)：`DL_DAC12_output12(DAC0, 0U)`

### `SYSCFG_DL_init`

- 作用：执行 SysConfig 生成的整板初始化，配置时钟、引脚和外设实例。
- 参数：无。
- 返回：无返回值。
- 当前工程调用：[main.c:L968](main.c#L968)：`SYSCFG_DL_init()`

### `__WFI`

- 作用：执行 Wait For Interrupt，使内核休眠到中断到来。
- 参数：无。
- 返回：无返回值。
- 当前工程调用：[main.c:L314](main.c#L314)：`__WFI()`

### `arm_mean_f32`

- 作用：计算 float32 数组均值。
- 形参与意义：

| 形参 | 方向 | 意义 |
|---|---|---|
| `source` | [in] | 输入浮点样本数组。 |
| `count` | [in] | 数组元素数。 |
| `result` | [out] | 均值输出地址。 |
- 返回：无返回值；结果写入 result。
- 当前工程调用：[main.c:L335](main.c#L335)：`arm_mean_f32(g_wave_voltage, APP_WAVE_SAMPLE_COUNT, &mean_frame_v)`；[main.c:L562](main.c#L562)：`arm_mean_f32(g_power_voltage, APP_POWER_SAMPLE_COUNT, &g_mode4_shunt_voltage_v)`

### `arm_offset_f32`

- 作用：给 float32 数组每个元素加同一偏置。
- 形参与意义：

| 形参 | 方向 | 意义 |
|---|---|---|
| `source` | [in] | 输入数组。 |
| `offset` | [in] | 逐元素相加的常量。 |
| `destination` | [out] | 输出数组，可按 CMSIS 约束与输入重叠。 |
| `count` | [in] | 处理元素数。 |
- 返回：无返回值。
- 当前工程调用：[main.c:L336](main.c#L336)：`arm_offset_f32(g_wave_voltage, -mean_frame_v, g_wave_capture.robust_workspace, APP_WAVE_SAMPLE_COUNT)`

### `arm_rms_f32`

- 作用：计算 float32 数组的均方根。
- 形参与意义：

| 形参 | 方向 | 意义 |
|---|---|---|
| `source` | [in] | 输入浮点样本数组。 |
| `count` | [in] | 样本数。 |
| `result` | [out] | RMS 输出地址。 |
- 返回：无返回值；结果写入 result。
- 当前工程调用：[main.c:L338](main.c#L338)：`arm_rms_f32(g_wave_capture.robust_workspace, APP_WAVE_SAMPLE_COUNT, &ac_rms_frame_v)`

## 6. 全部函数定义、作用、返回值与参数

本节逐个列出本工程源码中的所有函数定义。公开接口适合跨文件调用；`static` 函数通常依赖同一 `.c` 的静态状态，复用时优先整体复制所属模块。

### 文件导航

- [`main.c`：28 个](#file-main-c)
- [`modules/ad9850.c`：14 个](#file-modules-ad9850-c)
- [`modules/ad9850_mspm0_platform.c`：3 个](#file-modules-ad9850_mspm0_platform-c)
- [`modules/signal_adc_dma.c`：12 个](#file-modules-signal_adc_dma-c)
- [`modules/signal_matrix_keypad_4x4.c`：11 个](#file-modules-signal_matrix_keypad_4x4-c)
- [`modules/signal_robust_peak_to_peak.c`：4 个](#file-modules-signal_robust_peak_to_peak-c)
- [`modules/signal_slew_rate.c`：2 个](#file-modules-signal_slew_rate-c)
- [`modules/signal_static_power.c`：1 个](#file-modules-signal_static_power-c)
- [`modules/signal_tft_st7789.c`：24 个](#file-modules-signal_tft_st7789-c)
- [`modules/signal_tft_st7789_font.c`：8 个](#file-modules-signal_tft_st7789_font-c)
- [`modules/signal_tft_st7789_mspm0g3507.c`：6 个](#file-modules-signal_tft_st7789_mspm0g3507-c)
- [`modules/signal_vca820_gain_control.c`：1 个](#file-modules-signal_vca820_gain_control-c)

<a id="file-main-c"></a>

## `main.c`

<a id="fn-main-c-App_DelayUs"></a>

### `App_DelayUs`

- 定义位置：[main.c:L255](main.c#L255)
- 属性：文件内部 `static`；功能分类：工程入口、调度与中断。
- 所属模块：24_A 四问应用（DDS/VCA、扫频带宽、压摆率、静态功耗）。
- 具体作用：作为文件内部辅助函数，延时模块专用状态和数据；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
static void App_DelayUs(uint32_t delay_us)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `delay_us` | `uint32_t` | [in] | 忙等待时间，单位 µs。 | 签名/实现语义 |

<a id="fn-main-c-App_DDSWakeAndSetFrequency"></a>

### `App_DDSWakeAndSetFrequency`

- 定义位置：[main.c:L264](main.c#L264)
- 属性：文件内部 `static`；功能分类：DAC、DDS、波形发生与扫频。
- 所属模块：24_A 四问应用（DDS/VCA、扫频带宽、压摆率、静态功耗）。
- 具体作用：退出 AD9850 掉电状态并设置目标输出频率。
- 函数签名：

```c
static void App_DDSWakeAndSetFrequency(uint32_t frequency_hz)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `frequency_hz` | `uint32_t` | [in] | 频率，单位 Hz。 | 签名/实现语义 |

<a id="fn-main-c-VCA820_TargetVppToDACCode"></a>

### `VCA820_TargetVppToDACCode`

- 定义位置：[main.c:L272](main.c#L272)
- 属性：文件内部 `static`；功能分类：DAC、DDS、波形发生与扫频。
- 所属模块：24_A 四问应用（DDS/VCA、扫频带宽、压摆率、静态功耗）。
- 具体作用：把目标输出 Vpp 换算为 VCA820 控制 DAC 码。
- 函数签名：

```c
static uint16_t VCA820_TargetVppToDACCode(float target_vpp)
```

- 返回值（`uint16_t`）：返回 `VCA820_TargetVppToDACCode` 计算得到的整数结果；位宽和符号由 `uint16_t` 决定。（依据：类型/函数名语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `target_vpp` | `float` | [in] | 期望输出峰峰值，单位由应用约定，当前工程按 V 使用。 | 签名/实现语义 |

<a id="fn-main-c-VCA820_SetTargetVpp"></a>

### `VCA820_SetTargetVpp`

- 定义位置：[main.c:L283](main.c#L283)
- 属性：文件内部 `static`；功能分类：模拟前端、比较器、增益与功耗。
- 所属模块：24_A 四问应用（DDS/VCA、扫频带宽、压摆率、静态功耗）。
- 具体作用：写 DAC 控制 VCA820，并返回实际采用的 DAC 码。
- 函数签名：

```c
static uint16_t VCA820_SetTargetVpp(float target_vpp)
```

- 返回值（`uint16_t`）：返回 `VCA820_SetTargetVpp` 计算得到的整数结果；位宽和符号由 `uint16_t` 决定。（依据：类型/函数名语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `target_vpp` | `float` | [in] | 期望输出峰峰值，单位由应用约定，当前工程按 V 使用。 | 签名/实现语义 |

<a id="fn-main-c-App_ConvertADCToVoltage"></a>

### `App_ConvertADCToVoltage`

- 定义位置：[main.c:L292](main.c#L292)
- 属性：文件内部 `static`；功能分类：ADC、DMA、连续采集与缓冲。
- 所属模块：24_A 四问应用（DDS/VCA、扫频带宽、压摆率、静态功耗）。
- 具体作用：作为文件内部辅助函数，换算ADC 数据/配置；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
static void App_ConvertADCToVoltage(const uint16_t *raw_codes, float *voltage_v, uint32_t count, float input_scale, float offset_voltage_v)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `raw_codes` | `const uint16_t *` | [in] | ADC 原始码数组，未完成参考电压、偏置或前端增益换算。 | 签名/实现语义 |
| `voltage_v` | `float *` | [in/out] | 以伏特为单位的电压值或电压数组。 | 签名/实现语义 |
| `count` | `uint32_t` | [in] | 本次参与处理的元素/样本数量。 | 签名/实现语义 |
| `input_scale` | `float` | [in] | 把 ADC 引脚电压还原到被测端电压的比例系数。 | 签名/实现语义 |
| `offset_voltage_v` | `float` | [in] | 换算时需要扣除或加入的直流偏置，单位 V。 | 签名/实现语义 |

<a id="fn-main-c-App_CaptureWaveformVoltage"></a>

### `App_CaptureWaveformVoltage`

- 定义位置：[main.c:L307](main.c#L307)
- 属性：文件内部 `static`；功能分类：触发、捕获、存储与回放。
- 所属模块：24_A 四问应用（DDS/VCA、扫频带宽、压摆率、静态功耗）。
- 具体作用：作为文件内部辅助函数，采集采集波形的电压数组；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
static void App_CaptureWaveformVoltage(void)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数：无（`void`）。

<a id="fn-main-c-App_MeasureACRMS"></a>

### `App_MeasureACRMS`

- 定义位置：[main.c:L322](main.c#L322)
- 属性：文件内部 `static`；功能分类：基础时域与电压测量。
- 所属模块：24_A 四问应用（DDS/VCA、扫频带宽、压摆率、静态功耗）。
- 具体作用：作为文件内部辅助函数，测量有效值；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
static void App_MeasureACRMS( uint32_t frame_count, float *ac_rms_v, float *mean_voltage_v)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `frame_count` | `uint32_t` | [in] | 要累计或平均的采集帧数。 | 签名/实现语义 |
| `ac_rms_v` | `float *` | [out] | 电压，单位 V。 | 签名/实现语义 |
| `mean_voltage_v` | `float *` | [out] | 电压，单位 V。 | 签名/实现语义 |

<a id="fn-main-c-App_RunQuestion2"></a>

### `App_RunQuestion2`

- 定义位置：[main.c:L351](main.c#L351)
- 属性：文件内部 `static`；功能分类：工程入口、调度与中断。
- 所属模块：24_A 四问应用（DDS/VCA、扫频带宽、压摆率、静态功耗）。
- 具体作用：执行 Q2 对数/分段扫频，测量各点 AC RMS，并求增益、截止频率和带宽。
- 函数签名：

```c
static void App_RunQuestion2(void)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数：无（`void`）。

<a id="fn-main-c-App_RunQuestion3"></a>

### `App_RunQuestion3`

- 定义位置：[main.c:L448](main.c#L448)
- 属性：文件内部 `static`；功能分类：工程入口、调度与中断。
- 所属模块：24_A 四问应用（DDS/VCA、扫频带宽、压摆率、静态功耗）。
- 具体作用：执行 Q3 波形采集、稳健 Vpp 和上升/下降沿压摆率测量。
- 函数签名：

```c
static void App_RunQuestion3(void)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数：无（`void`）。

<a id="fn-main-c-App_ReadPowerADCBlock"></a>

### `App_ReadPowerADCBlock`

- 定义位置：[main.c:L522](main.c#L522)
- 属性：文件内部 `static`；功能分类：模拟前端、比较器、增益与功耗。
- 所属模块：24_A 四问应用（DDS/VCA、扫频带宽、压摆率、静态功耗）。
- 具体作用：作为文件内部辅助函数，读取静态功耗；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
static void App_ReadPowerADCBlock(void)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数：无（`void`）。

<a id="fn-main-c-App_RunQuestion4"></a>

### `App_RunQuestion4`

- 定义位置：[main.c:L548](main.c#L548)
- 属性：文件内部 `static`；功能分类：工程入口、调度与中断。
- 所属模块：24_A 四问应用（DDS/VCA、扫频带宽、压摆率、静态功耗）。
- 具体作用：执行 Q4：关闭 DDS，采集电源电压/采样电阻压降并计算静态功耗。
- 函数签名：

```c
static void App_RunQuestion4(void)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数：无（`void`）。

<a id="fn-main-c-App_ClearKeypadEntry"></a>

### `App_ClearKeypadEntry`

- 定义位置：[main.c:L578](main.c#L578)
- 属性：文件内部 `static`；功能分类：矩阵键盘与交互输入。
- 所属模块：24_A 四问应用（DDS/VCA、扫频带宽、压摆率、静态功耗）。
- 具体作用：应用层（24_A 四问应用（DDS/VCA、扫频带宽、压摆率、静态功耗））：清除缓存、计数、标志或显示区域。
- 函数签名：

```c
static void App_ClearKeypadEntry(void)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数：无（`void`）。

<a id="fn-main-c-App_ApplyQuestion1Entry"></a>

### `App_ApplyQuestion1Entry`

- 定义位置：[main.c:L586](main.c#L586)
- 属性：文件内部 `static`；功能分类：工程入口、调度与中断。
- 所属模块：24_A 四问应用（DDS/VCA、扫频带宽、压摆率、静态功耗）。
- 具体作用：作为文件内部辅助函数，应用模块专用状态和数据；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
static void App_ApplyQuestion1Entry(void)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数：无（`void`）。

<a id="fn-main-c-App_SelectMode"></a>

### `App_SelectMode`

- 定义位置：[main.c:L615](main.c#L615)
- 属性：文件内部 `static`；功能分类：工程入口、调度与中断。
- 所属模块：24_A 四问应用（DDS/VCA、扫频带宽、压摆率、静态功耗）。
- 具体作用：作为文件内部辅助函数，选择模块专用状态和数据；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
static void App_SelectMode(app_mode_t mode)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `mode` | `app_mode_t` | [in] | 工作模式枚举，决定采集、显示或算法分支。 | 签名/实现语义 |

<a id="fn-main-c-App_HandleKey"></a>

### `App_HandleKey`

- 定义位置：[main.c:L638](main.c#L638)
- 属性：文件内部 `static`；功能分类：矩阵键盘与交互输入。
- 所属模块：24_A 四问应用（DDS/VCA、扫频带宽、压摆率、静态功耗）。
- 具体作用：作为文件内部辅助函数，处理键盘事件；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
static void App_HandleKey(char symbol)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `symbol` | `char` | [in] | 矩阵键盘映射得到的符号字符。 | 签名/实现语义 |

<a id="fn-main-c-App_MeasurementStateText"></a>

### `App_MeasurementStateText`

- 定义位置：[main.c:L689](main.c#L689)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：24_A 四问应用（DDS/VCA、扫频带宽、压摆率、静态功耗）。
- 具体作用：作为文件内部辅助函数，测量测量状态文本；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
static const char *App_MeasurementStateText(void)
```

- 返回值（`const char *`）：返回内部对象/缓冲区指针；调用者不得越界访问，若接口允许失败还需先检查是否为 `NULL`。（依据：类型/实现语义）
- 参数：无（`void`）。

<a id="fn-main-c-App_TFTPrepareRow"></a>

### `App_TFTPrepareRow`

- 定义位置：[main.c:L706](main.c#L706)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：24_A 四问应用（DDS/VCA、扫频带宽、压摆率、静态功耗）。
- 具体作用：作为文件内部辅助函数，准备模块专用状态和数据；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
static void App_TFTPrepareRow(int16_t y, const char *label)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `y` | `int16_t` | [in] | 数值/样本坐标操作数；具体量纲由该算法的调用上下文决定。 | 签名/实现语义 |
| `label` | `const char *` | [in] | 界面字段标签字符串。 | 签名/实现语义 |

<a id="fn-main-c-App_TFTDrawTextRow"></a>

### `App_TFTDrawTextRow`

- 定义位置：[main.c:L718](main.c#L718)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：24_A 四问应用（DDS/VCA、扫频带宽、压摆率、静态功耗）。
- 具体作用：作为文件内部辅助函数，绘制文本；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
static void App_TFTDrawTextRow( int16_t y, const char *label, const char *text, uint16_t color)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `y` | `int16_t` | [in] | 纵坐标、第二操作数或 Y 通道值；显示函数中单位为像素。 | 签名/实现语义 |
| `label` | `const char *` | [in] | 界面字段标签字符串。 | 签名/实现语义 |
| `text` | `const char *` | [in] | 以 NUL 结尾的待显示/处理字符串。 | 签名/实现语义 |
| `color` | `uint16_t` | [in] | RGB565 颜色值。 | 签名/实现语义 |

<a id="fn-main-c-App_TFTDrawIntRow"></a>

### `App_TFTDrawIntRow`

- 定义位置：[main.c:L733](main.c#L733)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：24_A 四问应用（DDS/VCA、扫频带宽、压摆率、静态功耗）。
- 具体作用：作为文件内部辅助函数，绘制整数；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
static void App_TFTDrawIntRow( int16_t y, const char *label, int32_t value, const char *unit, uint16_t color)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `y` | `int16_t` | [in] | 纵坐标、第二操作数或 Y 通道值；显示函数中单位为像素。 | 签名/实现语义 |
| `label` | `const char *` | [in] | 界面字段标签字符串。 | 签名/实现语义 |
| `value` | `int32_t` | [in] | 要设置、换算或限制的输入值；量纲见函数名和相邻参数。 | 签名/实现语义 |
| `unit` | `const char *` | [in] | 数值后显示的单位字符串。 | 签名/实现语义 |
| `color` | `uint16_t` | [in] | RGB565 颜色值。 | 签名/实现语义 |

<a id="fn-main-c-App_TFTDrawFloatRow"></a>

### `App_TFTDrawFloatRow`

- 定义位置：[main.c:L753](main.c#L753)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：24_A 四问应用（DDS/VCA、扫频带宽、压摆率、静态功耗）。
- 具体作用：作为文件内部辅助函数，绘制浮点数；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
static void App_TFTDrawFloatRow( int16_t y, const char *label, float value, uint8_t precision, const char *unit, uint16_t color)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `y` | `int16_t` | [in] | 纵坐标、第二操作数或 Y 通道值；显示函数中单位为像素。 | 签名/实现语义 |
| `label` | `const char *` | [in] | 界面字段标签字符串。 | 签名/实现语义 |
| `value` | `float` | [in] | 要设置、换算或限制的输入值；量纲见函数名和相邻参数。 | 签名/实现语义 |
| `precision` | `uint8_t` | [in] | 浮点显示的小数位数。 | 签名/实现语义 |
| `unit` | `const char *` | [in] | 数值后显示的单位字符串。 | 签名/实现语义 |
| `color` | `uint16_t` | [in] | RGB565 颜色值。 | 签名/实现语义 |

<a id="fn-main-c-App_TFTDrawStaticLayout"></a>

### `App_TFTDrawStaticLayout`

- 定义位置：[main.c:L774](main.c#L774)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：24_A 四问应用（DDS/VCA、扫频带宽、压摆率、静态功耗）。
- 具体作用：作为文件内部辅助函数，绘制静态界面布局；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
static void App_TFTDrawStaticLayout(void)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数：无（`void`）。

<a id="fn-main-c-App_TFTDrawQuestion1"></a>

### `App_TFTDrawQuestion1`

- 定义位置：[main.c:L784](main.c#L784)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：24_A 四问应用（DDS/VCA、扫频带宽、压摆率、静态功耗）。
- 具体作用：作为文件内部辅助函数，绘制模块专用状态和数据；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
static void App_TFTDrawQuestion1(void)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数：无（`void`）。

<a id="fn-main-c-App_TFTDrawQuestion2"></a>

### `App_TFTDrawQuestion2`

- 定义位置：[main.c:L813](main.c#L813)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：24_A 四问应用（DDS/VCA、扫频带宽、压摆率、静态功耗）。
- 具体作用：作为文件内部辅助函数，绘制模块专用状态和数据；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
static void App_TFTDrawQuestion2(void)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数：无（`void`）。

<a id="fn-main-c-App_TFTDrawQuestion3"></a>

### `App_TFTDrawQuestion3`

- 定义位置：[main.c:L840](main.c#L840)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：24_A 四问应用（DDS/VCA、扫频带宽、压摆率、静态功耗）。
- 具体作用：作为文件内部辅助函数，绘制模块专用状态和数据；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
static void App_TFTDrawQuestion3(void)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数：无（`void`）。

<a id="fn-main-c-App_TFTDrawQuestion4"></a>

### `App_TFTDrawQuestion4`

- 定义位置：[main.c:L862](main.c#L862)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：24_A 四问应用（DDS/VCA、扫频带宽、压摆率、静态功耗）。
- 具体作用：作为文件内部辅助函数，绘制模块专用状态和数据；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
static void App_TFTDrawQuestion4(void)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数：无（`void`）。

<a id="fn-main-c-App_TFTRefreshValues"></a>

### `App_TFTRefreshValues`

- 定义位置：[main.c:L884](main.c#L884)
- 属性：文件内部 `static`；功能分类：工程入口、调度与中断。
- 所属模块：24_A 四问应用（DDS/VCA、扫频带宽、压摆率、静态功耗）。
- 具体作用：作为文件内部辅助函数，刷新测量值；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
static void App_TFTRefreshValues(void)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数：无（`void`）。

<a id="fn-main-c-App_TFTRefreshQuestion1Changed"></a>

### `App_TFTRefreshQuestion1Changed`

- 定义位置：[main.c:L910](main.c#L910)
- 属性：文件内部 `static`；功能分类：工程入口、调度与中断。
- 所属模块：24_A 四问应用（DDS/VCA、扫频带宽、压摆率、静态功耗）。
- 具体作用：作为文件内部辅助函数，刷新模块专用状态和数据；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
static void App_TFTRefreshQuestion1Changed(void)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数：无（`void`）。

<a id="fn-main-c-main"></a>

### `main`

- 定义位置：[main.c:L960](main.c#L960)
- 属性：工程入口；功能分类：工程入口、调度与中断。
- 所属模块：24_A 四问应用（DDS/VCA、扫频带宽、压摆率、静态功耗）。
- 具体作用：应用层（24_A 四问应用（DDS/VCA、扫频带宽、压摆率、静态功耗））：工程入口：初始化 SysConfig 和所选模块，随后进入应用主循环。
- 函数签名：

```c
int main(void)
```

- 返回值（`int`）：嵌入式主循环正常情况下不会返回；保留的 `int` 返回类型用于满足 C 入口约定。（依据：签名/实现语义）
- 参数：无（`void`）。


<a id="file-modules-ad9850-c"></a>

## `modules/ad9850.c`

<a id="fn-modules-ad9850-c-AD9850_MaxU32"></a>

### `AD9850_MaxU32`

- 定义位置：[modules/ad9850.c:L5](modules/ad9850.c#L5)
- 属性：文件内部 `static`；功能分类：外部器件与阻塞式总线。
- 所属模块：AD9850 并行/串行位操作、调谐字和输出控制。
- 具体作用：返回两个无符号 32 位数中的较大值，用于保证时序下限。
- 函数签名：

```c
static uint32_t AD9850_MaxU32(uint32_t a, uint32_t b)
```

- 返回值（`uint32_t`）：返回 `AD9850_MaxU32` 计算得到的整数结果；位宽和符号由 `uint32_t` 决定。（依据：类型/函数名语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `a` | `uint32_t` | [in] | 控制 `AD9850_MaxU32` 的 `a` 输入量；类型为 `uint32_t`，有效范围由函数中的参数校验决定。 | 签名/实现语义 |
| `b` | `uint32_t` | [in] | 控制 `AD9850_MaxU32` 的 `b` 输入量；类型为 `uint32_t`，有效范围由函数中的参数校验决定。 | 签名/实现语义 |

<a id="fn-modules-ad9850-c-AD9850_CyclesToUsCeil"></a>

### `AD9850_CyclesToUsCeil`

- 定义位置：[modules/ad9850.c:L10](modules/ad9850.c#L10)
- 属性：文件内部 `static`；功能分类：外部器件与阻塞式总线。
- 所属模块：AD9850 并行/串行位操作、调谐字和输出控制。
- 具体作用：把参考时钟周期数向上取整换算为微秒，避免 GPIO 脉冲短于 AD9850 时序要求。
- 函数签名：

```c
static uint32_t AD9850_CyclesToUsCeil( uint32_t cycles, uint32_t reference_clock_hz)
```

- 返回值（`uint32_t`）：返回 `AD9850_CyclesToUsCeil` 计算得到的整数结果；位宽和符号由 `uint32_t` 决定。（依据：类型/函数名语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `cycles` | `uint32_t` | [in] | CPU/外设时钟周期数。 | 签名/实现语义 |
| `reference_clock_hz` | `uint32_t` | [in] | 器件/计数器参考时钟，单位 Hz。 | 签名/实现语义 |

<a id="fn-modules-ad9850-c-AD9850_WriteLine"></a>

### `AD9850_WriteLine`

- 定义位置：[modules/ad9850.c:L20](modules/ad9850.c#L20)
- 属性：文件内部 `static`；功能分类：外部器件与阻塞式总线。
- 所属模块：AD9850 并行/串行位操作、调谐字和输出控制。
- 具体作用：通过平台回调设置 AD9850 的指定控制线电平。
- 函数签名：

```c
static ad9850_status_t AD9850_WriteLine( ad9850_t *device, ad9850_line_t line, bool high)
```

- 返回值（`ad9850_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `device` | `ad9850_t *` | [in/out] | 外部器件驱动对象，保存 GPIO/SPI 回调、时钟和器件状态。 | 签名/实现语义 |
| `line` | `ad9850_line_t` | [in] | 控制 `AD9850_WriteLine` 的 `line` 输入量；类型为 `ad9850_line_t`，有效范围由函数中的参数校验决定。 | 签名/实现语义 |
| `high` | `bool` | [in] | 高电平、上限或区间较大端点。 | 签名/实现语义 |

<a id="fn-modules-ad9850-c-AD9850_DelayEdge"></a>

### `AD9850_DelayEdge`

- 定义位置：[modules/ad9850.c:L30](modules/ad9850.c#L30)
- 属性：文件内部 `static`；功能分类：外部器件与阻塞式总线。
- 所属模块：AD9850 并行/串行位操作、调谐字和输出控制。
- 具体作用：按已计算的最小边沿间隔延时，满足 AD9850 串行写入时序。
- 函数签名：

```c
static void AD9850_DelayEdge(ad9850_t *device)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `device` | `ad9850_t *` | [in/out] | 外部器件驱动对象，保存 GPIO/SPI 回调、时钟和器件状态。 | 签名/实现语义 |

<a id="fn-modules-ad9850-c-AD9850_Pulse"></a>

### `AD9850_Pulse`

- 定义位置：[modules/ad9850.c:L38](modules/ad9850.c#L38)
- 属性：文件内部 `static`；功能分类：外部器件与阻塞式总线。
- 所属模块：AD9850 并行/串行位操作、调谐字和输出控制。
- 具体作用：在指定控制线上产生一次低/高（或高/低）脉冲。
- 函数签名：

```c
static ad9850_status_t AD9850_Pulse( ad9850_t *device, ad9850_line_t line, uint32_t high_time_us)
```

- 返回值（`ad9850_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `device` | `ad9850_t *` | [in/out] | 外部器件驱动对象，保存 GPIO/SPI 回调、时钟和器件状态。 | 签名/实现语义 |
| `line` | `ad9850_line_t` | [in] | 控制 `AD9850_Pulse` 的 `line` 输入量；类型为 `ad9850_line_t`，有效范围由函数中的参数校验决定。 | 签名/实现语义 |
| `high_time_us` | `uint32_t` | [in] | 控制 `AD9850_Pulse` 的 `high_time_us` 输入量；类型为 `uint32_t`，有效范围由函数中的参数校验决定。 | 签名/实现语义 |

<a id="fn-modules-ad9850-c-AD9850_WriteBit"></a>

### `AD9850_WriteBit`

- 定义位置：[modules/ad9850.c:L59](modules/ad9850.c#L59)
- 属性：文件内部 `static`；功能分类：外部器件与阻塞式总线。
- 所属模块：AD9850 并行/串行位操作、调谐字和输出控制。
- 具体作用：按 LSB-first 协议向 AD9850 DATA/W_CLK 串行写入一位。
- 函数签名：

```c
static ad9850_status_t AD9850_WriteBit( ad9850_t *device, bool high)
```

- 返回值（`ad9850_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `device` | `ad9850_t *` | [in/out] | 外部器件驱动对象，保存 GPIO/SPI 回调、时钟和器件状态。 | 签名/实现语义 |
| `high` | `bool` | [in] | 高电平、上限或区间较大端点。 | 签名/实现语义 |

<a id="fn-modules-ad9850-c-AD9850_WriteByteLsbFirst"></a>

### `AD9850_WriteByteLsbFirst`

- 定义位置：[modules/ad9850.c:L73](modules/ad9850.c#L73)
- 属性：文件内部 `static`；功能分类：外部器件与阻塞式总线。
- 所属模块：AD9850 并行/串行位操作、调谐字和输出控制。
- 具体作用：按最低位先发的顺序向 AD9850 连续写入一个字节。
- 函数签名：

```c
static ad9850_status_t AD9850_WriteByteLsbFirst( ad9850_t *device, uint8_t value)
```

- 返回值（`ad9850_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `device` | `ad9850_t *` | [in/out] | 外部器件驱动对象，保存 GPIO/SPI 回调、时钟和器件状态。 | 签名/实现语义 |
| `value` | `uint8_t` | [in] | 要设置、换算或限制的输入值；量纲见函数名和相邻参数。 | 签名/实现语义 |

<a id="fn-modules-ad9850-c-AD9850_WriteFrame"></a>

### `AD9850_WriteFrame`

- 定义位置：[modules/ad9850.c:L88](modules/ad9850.c#L88)
- 属性：文件内部 `static`；功能分类：外部器件与阻塞式总线。
- 所属模块：AD9850 并行/串行位操作、调谐字和输出控制。
- 具体作用：向 AD9850 写入 32 位频率字和 8 位控制字，并用 FQ_UD 锁存。
- 函数签名：

```c
static ad9850_status_t AD9850_WriteFrame( ad9850_t *device, uint32_t tuning_word, uint8_t phase_code, bool power_down)
```

- 返回值（`ad9850_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `device` | `ad9850_t *` | [in/out] | 外部器件驱动对象，保存 GPIO/SPI 回调、时钟和器件状态。 | 签名/实现语义 |
| `tuning_word` | `uint32_t` | [in] | DDS 频率调谐字；位宽和换算关系由器件决定。 | 签名/实现语义 |
| `phase_code` | `uint8_t` | [in] | 外部 DDS 相位寄存器编码。 | 签名/实现语义 |
| `power_down` | `bool` | [in] | 外部 DDS 的掉电控制标志。 | 签名/实现语义 |

<a id="fn-modules-ad9850-c-AD9850_ComputeTuningWord"></a>

### `AD9850_ComputeTuningWord`

- 定义位置：[modules/ad9850.c:L122](modules/ad9850.c#L122)
- 属性：公开接口；功能分类：外部器件与阻塞式总线。
- 所属模块：AD9850 并行/串行位操作、调谐字和输出控制。
- 具体作用：根据输出频率和参考时钟计算并舍入 32 位 DDS 调谐字。
- 函数签名：

```c
ad9850_status_t AD9850_ComputeTuningWord( uint32_t reference_clock_hz, uint32_t frequency_hz, uint32_t *tuning_word)
```

- 返回值（`ad9850_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `reference_clock_hz` | `uint32_t` | [in] | 器件/计数器参考时钟，单位 Hz。 | 签名/实现语义 |
| `frequency_hz` | `uint32_t` | [in] | 频率，单位 Hz。 | 签名/实现语义 |
| `tuning_word` | `uint32_t *` | [in/out] | DDS 频率调谐字；位宽和换算关系由器件决定。 | 签名/实现语义 |

<a id="fn-modules-ad9850-c-AD9850_Reset"></a>

### `AD9850_Reset`

- 定义位置：[modules/ad9850.c:L143](modules/ad9850.c#L143)
- 属性：公开接口；功能分类：外部器件与阻塞式总线。
- 所属模块：AD9850 并行/串行位操作、调谐字和输出控制。
- 具体作用：按器件要求产生 RESET/W_CLK/FQ_UD 序列，使 AD9850 回到已知状态。
- 函数签名：

```c
ad9850_status_t AD9850_Reset(ad9850_t *device)
```

- 返回值（`ad9850_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `device` | `ad9850_t *` | [in/out] | 外部器件驱动对象，保存 GPIO/SPI 回调、时钟和器件状态。 | 签名/实现语义 |

<a id="fn-modules-ad9850-c-AD9850_Init"></a>

### `AD9850_Init`

- 定义位置：[modules/ad9850.c:L188](modules/ad9850.c#L188)
- 属性：公开接口；功能分类：外部器件与阻塞式总线。
- 所属模块：AD9850 并行/串行位操作、调谐字和输出控制。
- 具体作用：公开接口（AD9850 并行/串行位操作、调谐字和输出控制）：初始化模块对象、硬件绑定和运行状态。
- 函数签名：

```c
ad9850_status_t AD9850_Init( ad9850_t *device, const ad9850_config_t *config)
```

- 返回值（`ad9850_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `device` | `ad9850_t *` | [in/out] | 外部器件驱动对象，保存 GPIO/SPI 回调、时钟和器件状态。 | 签名/实现语义 |
| `config` | `const ad9850_config_t *` | [in] | 模块配置结构体，包含初始化所需的采样率、时钟、容量或硬件参数；调用期间必须保持可读。 | 签名/实现语义 |

<a id="fn-modules-ad9850-c-AD9850_SetOutput"></a>

### `AD9850_SetOutput`

- 定义位置：[modules/ad9850.c:L233](modules/ad9850.c#L233)
- 属性：公开接口；功能分类：外部器件与阻塞式总线。
- 所属模块：AD9850 并行/串行位操作、调谐字和输出控制。
- 具体作用：组合频率调谐字、相位/控制位并更新 AD9850 输出。
- 函数签名：

```c
ad9850_status_t AD9850_SetOutput( ad9850_t *device, uint32_t frequency_hz, uint8_t phase_code, bool power_down)
```

- 返回值（`ad9850_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `device` | `ad9850_t *` | [in/out] | 外部器件驱动对象，保存 GPIO/SPI 回调、时钟和器件状态。 | 签名/实现语义 |
| `frequency_hz` | `uint32_t` | [in] | 频率，单位 Hz。 | 签名/实现语义 |
| `phase_code` | `uint8_t` | [in] | 外部 DDS 相位寄存器编码。 | 签名/实现语义 |
| `power_down` | `bool` | [in] | 外部 DDS 的掉电控制标志。 | 签名/实现语义 |

<a id="fn-modules-ad9850-c-AD9850_SetFrequencyHz"></a>

### `AD9850_SetFrequencyHz`

- 定义位置：[modules/ad9850.c:L270](modules/ad9850.c#L270)
- 属性：公开接口；功能分类：外部器件与阻塞式总线。
- 所属模块：AD9850 并行/串行位操作、调谐字和输出控制。
- 具体作用：公开接口（AD9850 并行/串行位操作、调谐字和输出控制）：重算相位步进或调谐字并设置输出频率。
- 函数签名：

```c
ad9850_status_t AD9850_SetFrequencyHz( ad9850_t *device, uint32_t frequency_hz)
```

- 返回值（`ad9850_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `device` | `ad9850_t *` | [in/out] | 外部器件驱动对象，保存 GPIO/SPI 回调、时钟和器件状态。 | 签名/实现语义 |
| `frequency_hz` | `uint32_t` | [in] | 频率，单位 Hz。 | 签名/实现语义 |

<a id="fn-modules-ad9850-c-AD9850_SetPowerDown"></a>

### `AD9850_SetPowerDown`

- 定义位置：[modules/ad9850.c:L283](modules/ad9850.c#L283)
- 属性：公开接口；功能分类：外部器件与阻塞式总线。
- 所属模块：AD9850 并行/串行位操作、调谐字和输出控制。
- 具体作用：设置或清除 AD9850 控制字中的掉电位。
- 函数签名：

```c
ad9850_status_t AD9850_SetPowerDown( ad9850_t *device, bool power_down)
```

- 返回值（`ad9850_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `device` | `ad9850_t *` | [in/out] | 外部器件驱动对象，保存 GPIO/SPI 回调、时钟和器件状态。 | 签名/实现语义 |
| `power_down` | `bool` | [in] | 外部 DDS 的掉电控制标志。 | 签名/实现语义 |


<a id="file-modules-ad9850_mspm0_platform-c"></a>

## `modules/ad9850_mspm0_platform.c`

<a id="fn-modules-ad9850_mspm0_platform-c-AD9850_MSPM0_GetPin"></a>

### `AD9850_MSPM0_GetPin`

- 定义位置：[modules/ad9850_mspm0_platform.c:L5](modules/ad9850_mspm0_platform.c#L5)
- 属性：文件内部 `static`；功能分类：外部器件与阻塞式总线。
- 所属模块：AD9850 到 MSPM0 GPIO/延时接口的适配层。
- 具体作用：把平台抽象的 AD9850 信号枚举映射到 MSPM0 GPIO 端口和引脚掩码。
- 函数签名：

```c
static bool AD9850_MSPM0_GetPin( ad9850_mspm0_platform_t *platform, ad9850_line_t line, GPIO_Regs **port, uint32_t *pin)
```

- 返回值（`bool`）：返回布尔判定：`true` 表示 `AD9850_MSPM0_GetPin` 所检查的条件成立，`false` 表示不成立。（依据：签名/函数名语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `platform` | `ad9850_mspm0_platform_t *` | [in/out] | 与 MSPM0 硬件相关的平台回调集合，例如写引脚和微秒延时函数。 | 签名/实现语义 |
| `line` | `ad9850_line_t` | [in] | 控制 `AD9850_MSPM0_GetPin` 的 `line` 输入量；类型为 `ad9850_line_t`，有效范围由函数中的参数校验决定。 | 签名/实现语义 |
| `port` | `GPIO_Regs **` | [in/out] | GPIO 端口基地址。 | 签名/实现语义 |
| `pin` | `uint32_t *` | [in/out] | GPIO 引脚编号或位掩码。 | 签名/实现语义 |

<a id="fn-modules-ad9850_mspm0_platform-c-AD9850_MSPM0_WriteLine"></a>

### `AD9850_MSPM0_WriteLine`

- 定义位置：[modules/ad9850_mspm0_platform.c:L35](modules/ad9850_mspm0_platform.c#L35)
- 属性：公开接口；功能分类：外部器件与阻塞式总线。
- 所属模块：AD9850 到 MSPM0 GPIO/延时接口的适配层。
- 具体作用：用 DriverLib 把 AD9850 某根控制线置高或置低。
- 函数签名：

```c
bool AD9850_MSPM0_WriteLine( void *context, ad9850_line_t line, bool high)
```

- 返回值（`bool`）：返回布尔判定：`true` 表示 `AD9850_MSPM0_WriteLine` 所检查的条件成立，`false` 表示不成立。（依据：签名/函数名语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `context` | `void *` | [in/out] | 驱动/算法上下文或回调用户数据，用于保存实例状态并避免依赖全局变量。 | 签名/实现语义 |
| `line` | `ad9850_line_t` | [in] | 控制 `AD9850_MSPM0_WriteLine` 的 `line` 输入量；类型为 `ad9850_line_t`，有效范围由函数中的参数校验决定。 | 签名/实现语义 |
| `high` | `bool` | [in] | 高电平、上限或区间较大端点。 | 签名/实现语义 |

<a id="fn-modules-ad9850_mspm0_platform-c-AD9850_MSPM0_DelayUs"></a>

### `AD9850_MSPM0_DelayUs`

- 定义位置：[modules/ad9850_mspm0_platform.c:L56](modules/ad9850_mspm0_platform.c#L56)
- 属性：公开接口；功能分类：外部器件与阻塞式总线。
- 所属模块：AD9850 到 MSPM0 GPIO/延时接口的适配层。
- 具体作用：把微秒延时换算为 CPU 周期并执行忙等待。
- 函数签名：

```c
void AD9850_MSPM0_DelayUs(void *context, uint32_t delay_us)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `context` | `void *` | [in/out] | 驱动/算法上下文或回调用户数据，用于保存实例状态并避免依赖全局变量。 | 签名/实现语义 |
| `delay_us` | `uint32_t` | [in] | 忙等待时间，单位 µs。 | 签名/实现语义 |


<a id="file-modules-signal_adc_dma-c"></a>

## `modules/signal_adc_dma.c`

<a id="fn-modules-signal_adc_dma-c-SignalADC_CalculateTimerCount"></a>

### `SignalADC_CalculateTimerCount`

- 定义位置：[modules/signal_adc_dma.c:L31](modules/signal_adc_dma.c#L31)
- 属性：文件内部 `static`；功能分类：ADC、DMA、连续采集与缓冲。
- 所属模块：单路 ADC，由定时器事件触发并通过 DMA 采集。
- 具体作用：内部辅助（单路 ADC，由定时器事件触发并通过 DMA 采集）：返回或更新当前累计数量。
- 函数签名：

```c
static signal_result_t SignalADC_CalculateTimerCount(uint32_t sample_rate_hz, uint32_t *timer_count, uint32_t *configured_trigger_rate_hz)
```

- 返回值（`signal_result_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `sample_rate_hz` | `uint32_t` | [in] | 采样率，单位 Hz；必须与实际定时器触发率一致。 | 签名/实现语义 |
| `timer_count` | `uint32_t *` | [in/out] | 定时器当前/目标计数值，单位为 timer tick。 | 签名/实现语义 |
| `configured_trigger_rate_hz` | `uint32_t *` | [in/out] | 已配置的实际 ADC 触发率，单位 Hz。 | 签名/实现语义 |

<a id="fn-modules-signal_adc_dma-c-SignalADC_Init"></a>

### `SignalADC_Init`

- 定义位置：[modules/signal_adc_dma.c:L63](modules/signal_adc_dma.c#L63)
- 属性：公开接口；功能分类：ADC、DMA、连续采集与缓冲。
- 所属模块：单路 ADC，由定时器事件触发并通过 DMA 采集。
- 具体作用：初始化 ADC+DMA 模块的运行时状态。
- 函数签名：

```c
signal_result_t SignalADC_Init(const signal_adc_dma_config_t *config)
```

- 返回值（`signal_result_t`）：SIGNAL_RESULT_OK 表示成功；其他值表示参数或范围错误。（依据：源码 `@return`）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `config` | `const signal_adc_dma_config_t *` | [in] | 采样率、定时器计数时钟和最大周期计数。 | 源码 `@param` |

<a id="fn-modules-signal_adc_dma-c-SignalADC_SetSampleRate"></a>

### `SignalADC_SetSampleRate`

- 定义位置：[modules/signal_adc_dma.c:L104](modules/signal_adc_dma.c#L104)
- 属性：公开接口；功能分类：ADC、DMA、连续采集与缓冲。
- 所属模块：单路 ADC，由定时器事件触发并通过 DMA 采集。
- 具体作用：修改目标采样事件率并计算最近的整数 Timer 配置触发率。
- 函数签名：

```c
signal_result_t SignalADC_SetSampleRate(uint32_t sample_rate_hz)
```

- 返回值（`signal_result_t`）：SIGNAL_RESULT_OK 表示成功；运行中返回 SIGNAL_RESULT_BUSY。（依据：源码 `@return`）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `sample_rate_hz` | `uint32_t` | [in] | 目标采样事件率，单位 Hz。 | 源码 `@param` |

<a id="fn-modules-signal_adc_dma-c-SignalADC_Start"></a>

### `SignalADC_Start`

- 定义位置：[modules/signal_adc_dma.c:L142](modules/signal_adc_dma.c#L142)
- 属性：公开接口；功能分类：ADC、DMA、连续采集与缓冲。
- 所属模块：单路 ADC，由定时器事件触发并通过 DMA 采集。
- 具体作用：启动一次 N 点采集。
- 函数签名：

```c
signal_result_t SignalADC_Start(uint16_t *buffer, uint16_t sample_count)
```

- 返回值（`signal_result_t`）：SIGNAL_RESULT_OK 表示已启动。（依据：源码 `@return`）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `buffer` | `uint16_t *` | [in/out] | DMA 目标缓冲区，元素类型必须为 uint16_t。 | 源码 `@param` |
| `sample_count` | `uint16_t` | [in] | 采样点数，范围 1~65535。 | 源码 `@param` |

<a id="fn-modules-signal_adc_dma-c-SignalADC_Stop"></a>

### `SignalADC_Stop`

- 定义位置：[modules/signal_adc_dma.c:L192](modules/signal_adc_dma.c#L192)
- 属性：公开接口；功能分类：ADC、DMA、连续采集与缓冲。
- 所属模块：单路 ADC，由定时器事件触发并通过 DMA 采集。
- 具体作用：立即停止当前采集并把模块恢复为空闲状态。
- 函数签名：

```c
void SignalADC_Stop(void)
```

- 返回值（`void`）：无。（依据：源码 `@return`）
- 参数：无（`void`）。

<a id="fn-modules-signal_adc_dma-c-SignalADC_IsFinished"></a>

### `SignalADC_IsFinished`

- 定义位置：[modules/signal_adc_dma.c:L210](modules/signal_adc_dma.c#L210)
- 属性：公开接口；功能分类：ADC、DMA、连续采集与缓冲。
- 所属模块：单路 ADC，由定时器事件触发并通过 DMA 采集。
- 具体作用：查询本次采集是否完成。
- 函数签名：

```c
bool SignalADC_IsFinished(void)
```

- 返回值（`bool`）：完成返回 true，否则返回 false。（依据：源码 `@return`）
- 参数：无（`void`）。

<a id="fn-modules-signal_adc_dma-c-SignalADC_GetStatus"></a>

### `SignalADC_GetStatus`

- 定义位置：[modules/signal_adc_dma.c:L219](modules/signal_adc_dma.c#L219)
- 属性：公开接口；功能分类：ADC、DMA、连续采集与缓冲。
- 所属模块：单路 ADC，由定时器事件触发并通过 DMA 采集。
- 具体作用：查询模块当前状态。
- 函数签名：

```c
signal_status_t SignalADC_GetStatus(void)
```

- 返回值（`signal_status_t`）：MODULE_IDLE、MODULE_RUNNING、MODULE_DONE 或 MODULE_ERROR。（依据：源码 `@return`）
- 参数：无（`void`）。

<a id="fn-modules-signal_adc_dma-c-SignalADC_GetBuffer"></a>

### `SignalADC_GetBuffer`

- 定义位置：[modules/signal_adc_dma.c:L229](modules/signal_adc_dma.c#L229)
- 属性：公开接口；功能分类：ADC、DMA、连续采集与缓冲。
- 所属模块：单路 ADC，由定时器事件触发并通过 DMA 采集。
- 具体作用：取得最近一次采集使用的缓冲区。
- 函数签名：

```c
const uint16_t *SignalADC_GetBuffer(void)
```

- 返回值（`const uint16_t *`）：缓冲区只读指针；尚未启动过采集时返回空指针。（依据：源码 `@return`）
- 参数：无（`void`）。

<a id="fn-modules-signal_adc_dma-c-SignalADC_GetSampleCount"></a>

### `SignalADC_GetSampleCount`

- 定义位置：[modules/signal_adc_dma.c:L238](modules/signal_adc_dma.c#L238)
- 属性：公开接口；功能分类：ADC、DMA、连续采集与缓冲。
- 所属模块：单路 ADC，由定时器事件触发并通过 DMA 采集。
- 具体作用：取得最近一次采集的点数。
- 函数签名：

```c
uint16_t SignalADC_GetSampleCount(void)
```

- 返回值（`uint16_t`）：采样点数；尚未启动过采集时返回 0。（依据：源码 `@return`）
- 参数：无（`void`）。

<a id="fn-modules-signal_adc_dma-c-SignalADC_GetConfiguredTriggerRate"></a>

### `SignalADC_GetConfiguredTriggerRate`

- 定义位置：[modules/signal_adc_dma.c:L248](modules/signal_adc_dma.c#L248)
- 属性：公开接口；功能分类：ADC、DMA、连续采集与缓冲。
- 所属模块：单路 ADC，由定时器事件触发并通过 DMA 采集。
- 具体作用：取得由 Timer 整数计数配置推导出的事件触发率。
- 函数签名：

```c
uint32_t SignalADC_GetConfiguredTriggerRate(void)
```

- 返回值（`uint32_t`）：配置触发率，单位 Hz；初始化失败时返回 0。（依据：源码 `@return`）
- 参数：无（`void`）。

<a id="fn-modules-signal_adc_dma-c-SignalADC_GetModuleMaturity"></a>

### `SignalADC_GetModuleMaturity`

- 定义位置：[modules/signal_adc_dma.c:L253](modules/signal_adc_dma.c#L253)
- 属性：公开接口；功能分类：ADC、DMA、连续采集与缓冲。
- 所属模块：单路 ADC，由定时器事件触发并通过 DMA 采集。
- 具体作用：返回模块证据成熟度；当前来自 2026-08-07 板载 TMP6131 验收。
- 函数签名：

```c
signal_module_status_t SignalADC_GetModuleMaturity(void)
```

- 返回值（`signal_module_status_t`）：返回模块成熟度/验证等级元数据，供应用判断示例、冻结或硬件验证状态。（依据：类型/实现语义）
- 参数：无（`void`）。

<a id="fn-modules-signal_adc_dma-c-SIGNAL_ADC_INST_IRQHandler"></a>

### `SIGNAL_ADC_INST_IRQHandler`

- 定义位置：[modules/signal_adc_dma.c:L262](modules/signal_adc_dma.c#L262)
- 属性：ISR（由硬件/内核调用）；功能分类：ADC、DMA、连续采集与缓冲。
- 所属模块：单路 ADC，由定时器事件触发并通过 DMA 采集。
- 具体作用：作为模块公开接口，处理ADC 数据/配置；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
void SIGNAL_ADC_INST_IRQHandler(void)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数：无（`void`）。


<a id="file-modules-signal_matrix_keypad_4x4-c"></a>

## `modules/signal_matrix_keypad_4x4.c`

<a id="fn-modules-signal_matrix_keypad_4x4-c-keypad_release_all_rows"></a>

### `keypad_release_all_rows`

- 定义位置：[modules/signal_matrix_keypad_4x4.c:L17](modules/signal_matrix_keypad_4x4.c#L17)
- 属性：文件内部 `static`；功能分类：矩阵键盘与交互输入。
- 所属模块：4×4 矩阵键盘行列扫描、消抖、鬼键过滤和符号映射。
- 具体作用：内部辅助（4×4 矩阵键盘行列扫描、消抖、鬼键过滤和符号映射）：释放已消费的数据块，使 DMA 可以再次覆盖。
- 函数签名：

```c
static signal_result_t keypad_release_all_rows( signal_matrix_keypad_4x4_t *keypad)
```

- 返回值（`signal_result_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `keypad` | `signal_matrix_keypad_4x4_t *` | [in/out] | 4×4 矩阵键盘对象，保存 GPIO 回调、消抖计数和按键状态。 | 签名/实现语义 |

<a id="fn-modules-signal_matrix_keypad_4x4-c-keypad_ghost_possible"></a>

### `keypad_ghost_possible`

- 定义位置：[modules/signal_matrix_keypad_4x4.c:L32](modules/signal_matrix_keypad_4x4.c#L32)
- 属性：文件内部 `static`；功能分类：矩阵键盘与交互输入。
- 所属模块：4×4 矩阵键盘行列扫描、消抖、鬼键过滤和符号映射。
- 具体作用：检查当前行列按下组合是否可能形成矩阵键盘鬼键；发现多行多列同时有效时拒绝上报。
- 函数签名：

```c
static bool keypad_ghost_possible(uint16_t mask)
```

- 返回值（`bool`）：返回布尔判定：`true` 表示 `keypad_ghost_possible` 所检查的条件成立，`false` 表示不成立。（依据：签名/函数名语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `mask` | `uint16_t` | [in] | 控制 `keypad_ghost_possible` 的 `mask` 输入量；类型为 `uint16_t`，有效范围由函数中的参数校验决定。 | 签名/实现语义 |

<a id="fn-modules-signal_matrix_keypad_4x4-c-SignalMatrixKeypad4x4_Init"></a>

### `SignalMatrixKeypad4x4_Init`

- 定义位置：[modules/signal_matrix_keypad_4x4.c:L53](modules/signal_matrix_keypad_4x4.c#L53)
- 属性：公开接口；功能分类：矩阵键盘与交互输入。
- 所属模块：4×4 矩阵键盘行列扫描、消抖、鬼键过滤和符号映射。
- 具体作用：根据调用者提供的配置或对象完成一次初始化；通常在 `SYSCFG_DL_init()` 之后且仅调用一次。
- 函数签名：

```c
signal_result_t SignalMatrixKeypad4x4_Init( signal_matrix_keypad_4x4_t *keypad, const signal_matrix_keypad_4x4_config_t *config)
```

- 返回值（`signal_result_t`）：返回 signal_result_t 类型结果；调用者应检查该值。（依据：源码 `@return`）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `keypad` | `signal_matrix_keypad_4x4_t *` | [in/out] | `keypad`（`signal_matrix_keypad_4x4_t *`）是该 API 的输入/输出参数；按本 README 前面的数据单位和边界条件准备。 | 源码 `@param` |
| `config` | `const signal_matrix_keypad_4x4_config_t *` | [in] | 调用者填写的配置对象。先阅读该类型的成员；它控制本次初始化或处理方式。 | 源码 `@param` |

<a id="fn-modules-signal_matrix_keypad_4x4-c-SignalMatrixKeypad4x4_Scan"></a>

### `SignalMatrixKeypad4x4_Scan`

- 定义位置：[modules/signal_matrix_keypad_4x4.c:L80](modules/signal_matrix_keypad_4x4.c#L80)
- 属性：公开接口；功能分类：矩阵键盘与交互输入。
- 所属模块：4×4 矩阵键盘行列扫描、消抖、鬼键过滤和符号映射。
- 具体作用：逐行驱动 4×4 矩阵键盘并读取四列，执行消抖、按下/释放状态更新和事件入队。
- 函数签名：

```c
signal_result_t SignalMatrixKeypad4x4_Scan( signal_matrix_keypad_4x4_t *keypad, signal_matrix_keypad_4x4_event_t *event)
```

- 返回值（`signal_result_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `keypad` | `signal_matrix_keypad_4x4_t *` | [in/out] | 4×4 矩阵键盘对象，保存 GPIO 回调、消抖计数和按键状态。 | 签名/实现语义 |
| `event` | `signal_matrix_keypad_4x4_event_t *` | [in/out] | 单个触发/键盘/捕获事件对象。 | 签名/实现语义 |

<a id="fn-modules-signal_matrix_keypad_4x4-c-SignalMatrixKeypad4x4_GetKey"></a>

### `SignalMatrixKeypad4x4_GetKey`

- 定义位置：[modules/signal_matrix_keypad_4x4.c:L155](modules/signal_matrix_keypad_4x4.c#L155)
- 属性：公开接口；功能分类：矩阵键盘与交互输入。
- 所属模块：4×4 矩阵键盘行列扫描、消抖、鬼键过滤和符号映射。
- 具体作用：读取最近一次操作保存的状态、结果或配置；先确认前置操作已经成功。
- 函数签名：

```c
signal_result_t SignalMatrixKeypad4x4_GetKey( const signal_matrix_keypad_4x4_t *keypad, uint8_t key_index, char *symbol)
```

- 返回值（`signal_result_t`）：返回 signal_result_t 类型结果；调用者应检查该值。（依据：源码 `@return`）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `keypad` | `const signal_matrix_keypad_4x4_t *` | [in/out] | `keypad`（`const signal_matrix_keypad_4x4_t *`）是该 API 的输入/输出参数；按本 README 前面的数据单位和边界条件准备。 | 源码 `@param` |
| `key_index` | `uint8_t` | [in] | 索引或通道号；范围由相应数组长度、FFT bin 数或当前硬件配置决定。 | 源码 `@param` |
| `symbol` | `char *` | [in/out] | `symbol`（`char *`）是该 API 的输入/输出参数；按本 README 前面的数据单位和边界条件准备。 | 源码 `@param` |

<a id="fn-modules-signal_matrix_keypad_4x4-c-SignalMatrixKeypad4x4_GetFirstPressed"></a>

### `SignalMatrixKeypad4x4_GetFirstPressed`

- 定义位置：[modules/signal_matrix_keypad_4x4.c:L171](modules/signal_matrix_keypad_4x4.c#L171)
- 属性：公开接口；功能分类：矩阵键盘与交互输入。
- 所属模块：4×4 矩阵键盘行列扫描、消抖、鬼键过滤和符号映射。
- 具体作用：读取最近一次操作保存的状态、结果或配置；先确认前置操作已经成功。
- 函数签名：

```c
signal_result_t SignalMatrixKeypad4x4_GetFirstPressed( const signal_matrix_keypad_4x4_t *keypad, char *symbol, uint8_t *key_index)
```

- 返回值（`signal_result_t`）：返回 signal_result_t 类型结果；调用者应检查该值。（依据：源码 `@return`）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `keypad` | `const signal_matrix_keypad_4x4_t *` | [in/out] | `keypad`（`const signal_matrix_keypad_4x4_t *`）是该 API 的输入/输出参数；按本 README 前面的数据单位和边界条件准备。 | 源码 `@param` |
| `symbol` | `char *` | [in/out] | `symbol`（`char *`）是该 API 的输入/输出参数；按本 README 前面的数据单位和边界条件准备。 | 源码 `@param` |
| `key_index` | `uint8_t *` | [in/out] | 索引或通道号；范围由相应数组长度、FFT bin 数或当前硬件配置决定。 | 源码 `@param` |

<a id="fn-modules-signal_matrix_keypad_4x4-c-SignalMatrixKeypad4x4_GetModuleStatus"></a>

### `SignalMatrixKeypad4x4_GetModuleStatus`

- 定义位置：[modules/signal_matrix_keypad_4x4.c:L194](modules/signal_matrix_keypad_4x4.c#L194)
- 属性：公开接口；功能分类：矩阵键盘与交互输入。
- 所属模块：4×4 矩阵键盘行列扫描、消抖、鬼键过滤和符号映射。
- 具体作用：读取模块当前的验证成熟度或静态状态，不会启动硬件操作。
- 函数签名：

```c
signal_module_status_t SignalMatrixKeypad4x4_GetModuleStatus(void)
```

- 返回值（`signal_module_status_t`）：返回 signal_module_status_t 类型结果；调用者应检查该值。（依据：源码 `@return`）
- 参数：无（`void`）。

<a id="fn-modules-signal_matrix_keypad_4x4-c-mspm0g3507_keypad_drive_row"></a>

### `mspm0g3507_keypad_drive_row`

- 定义位置：[modules/signal_matrix_keypad_4x4.c:L203](modules/signal_matrix_keypad_4x4.c#L203)
- 属性：文件内部 `static`；功能分类：矩阵键盘与交互输入。
- 所属模块：4×4 矩阵键盘行列扫描、消抖、鬼键过滤和符号映射。
- 具体作用：把指定键盘行拉到有效电平，同时释放其余行。
- 函数签名：

```c
static signal_result_t mspm0g3507_keypad_drive_row( void *context, uint8_t row, bool active)
```

- 返回值（`signal_result_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `context` | `void *` | [in/out] | 驱动/算法上下文或回调用户数据，用于保存实例状态并避免依赖全局变量。 | 签名/实现语义 |
| `row` | `uint8_t` | [in] | 键盘/字模/表格的行索引。 | 签名/实现语义 |
| `active` | `bool` | [in] | 启用/激活标志。 | 签名/实现语义 |

<a id="fn-modules-signal_matrix_keypad_4x4-c-mspm0g3507_keypad_read_column"></a>

### `mspm0g3507_keypad_read_column`

- 定义位置：[modules/signal_matrix_keypad_4x4.c:L225](modules/signal_matrix_keypad_4x4.c#L225)
- 属性：文件内部 `static`；功能分类：矩阵键盘与交互输入。
- 所属模块：4×4 矩阵键盘行列扫描、消抖、鬼键过滤和符号映射。
- 具体作用：读取指定键盘列 GPIO 电平并转换为按下/未按下状态。
- 函数签名：

```c
static signal_result_t mspm0g3507_keypad_read_column( void *context, uint8_t column, bool *active)
```

- 返回值（`signal_result_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `context` | `void *` | [in/out] | 驱动/算法上下文或回调用户数据，用于保存实例状态并避免依赖全局变量。 | 签名/实现语义 |
| `column` | `uint8_t` | [in] | 键盘/字模/表格的列索引。 | 签名/实现语义 |
| `active` | `bool *` | [in/out] | 启用/激活标志。 | 签名/实现语义 |

<a id="fn-modules-signal_matrix_keypad_4x4-c-mspm0g3507_keypad_delay_us"></a>

### `mspm0g3507_keypad_delay_us`

- 定义位置：[modules/signal_matrix_keypad_4x4.c:L244](modules/signal_matrix_keypad_4x4.c#L244)
- 属性：文件内部 `static`；功能分类：矩阵键盘与交互输入。
- 所属模块：4×4 矩阵键盘行列扫描、消抖、鬼键过滤和符号映射。
- 具体作用：为键盘行切换和消抖提供微秒级忙等待。
- 函数签名：

```c
static void mspm0g3507_keypad_delay_us( void *context, uint32_t microseconds)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `context` | `void *` | [in/out] | 驱动/算法上下文或回调用户数据，用于保存实例状态并避免依赖全局变量。 | 签名/实现语义 |
| `microseconds` | `uint32_t` | [in] | 延时时间，单位 µs。 | 签名/实现语义 |

<a id="fn-modules-signal_matrix_keypad_4x4-c-SignalMatrixKeypad4x4_ReadNewSymbol"></a>

### `SignalMatrixKeypad4x4_ReadNewSymbol`

- 定义位置：[modules/signal_matrix_keypad_4x4.c:L261](modules/signal_matrix_keypad_4x4.c#L261)
- 属性：公开接口；功能分类：矩阵键盘与交互输入。
- 所属模块：4×4 矩阵键盘行列扫描、消抖、鬼键过滤和符号映射。
- 具体作用：从键盘事件队列读取一次新按下事件并映射为字符；没有新事件时返回 false。
- 函数签名：

```c
signal_result_t SignalMatrixKeypad4x4_ReadNewSymbol(char *symbol)
```

- 返回值（`signal_result_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `symbol` | `char *` | [in/out] | 矩阵键盘映射得到的符号字符。 | 签名/实现语义 |


<a id="file-modules-signal_robust_peak_to_peak-c"></a>

## `modules/signal_robust_peak_to_peak.c`

<a id="fn-modules-signal_robust_peak_to_peak-c-SignalRobustP2P_Swap"></a>

### `SignalRobustP2P_Swap`

- 定义位置：[modules/signal_robust_peak_to_peak.c:L7](modules/signal_robust_peak_to_peak.c#L7)
- 属性：文件内部 `static`；功能分类：稳健估计、校准与补偿。
- 所属模块：分位数峰峰值，降低毛刺影响。
- 具体作用：交换两个浮点样本，供选择算法原地分区。
- 函数签名：

```c
static void SignalRobustP2P_Swap(float *left, float *right)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `left` | `float *` | [in/out] | `left` 数据对象/缓冲区；结合类型 `float *` 由函数读取或写回。 | 签名/实现语义 |
| `right` | `float *` | [in/out] | `right` 数据对象/缓冲区；结合类型 `float *` 由函数读取或写回。 | 签名/实现语义 |

<a id="fn-modules-signal_robust_peak_to_peak-c-SignalRobustP2P_Select"></a>

### `SignalRobustP2P_Select`

- 定义位置：[modules/signal_robust_peak_to_peak.c:L14](modules/signal_robust_peak_to_peak.c#L14)
- 属性：文件内部 `static`；功能分类：稳健估计、校准与补偿。
- 所属模块：分位数峰峰值，降低毛刺影响。
- 具体作用：用快速选择在工作数组中取得第 k 小元素，无需完整排序。
- 函数签名：

```c
static float SignalRobustP2P_Select(float *values, uint32_t count, uint32_t k)
```

- 返回值（`float`）：返回 `SignalRobustP2P_Select` 计算得到的单精度结果；单位由函数用途和输入参数决定。（依据：类型/函数名语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `values` | `float *` | [in/out] | `values` 数据对象/缓冲区；结合类型 `float *` 由函数读取或写回。 | 签名/实现语义 |
| `count` | `uint32_t` | [in] | 本次参与处理的元素/样本数量。 | 签名/实现语义 |
| `k` | `uint32_t` | [in] | 控制 `SignalRobustP2P_Select` 的 `k` 输入量；类型为 `uint32_t`，有效范围由函数中的参数校验决定。 | 签名/实现语义 |

<a id="fn-modules-signal_robust_peak_to_peak-c-SignalRobustP2P_Quantile"></a>

### `SignalRobustP2P_Quantile`

- 定义位置：[modules/signal_robust_peak_to_peak.c:L61](modules/signal_robust_peak_to_peak.c#L61)
- 属性：文件内部 `static`；功能分类：稳健估计、校准与补偿。
- 所属模块：分位数峰峰值，降低毛刺影响。
- 具体作用：按分位点计算相邻次序统计量并线性插值。
- 函数签名：

```c
static float SignalRobustP2P_Quantile( float *values, uint32_t count, float quantile)
```

- 返回值（`float`）：返回 `SignalRobustP2P_Quantile` 计算得到的单精度结果；单位由函数用途和输入参数决定。（依据：类型/函数名语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `values` | `float *` | [in/out] | `values` 数据对象/缓冲区；结合类型 `float *` 由函数读取或写回。 | 签名/实现语义 |
| `count` | `uint32_t` | [in] | 本次参与处理的元素/样本数量。 | 签名/实现语义 |
| `quantile` | `float` | [in] | 分位点，0～1 对应最小值到最大值。 | 签名/实现语义 |

<a id="fn-modules-signal_robust_peak_to_peak-c-SignalRobustPeakToPeak_Process"></a>

### `SignalRobustPeakToPeak_Process`

- 定义位置：[modules/signal_robust_peak_to_peak.c:L82](modules/signal_robust_peak_to_peak.c#L82)
- 属性：公开接口；功能分类：稳健估计、校准与补偿。
- 所属模块：分位数峰峰值，降低毛刺影响。
- 具体作用：用上下分位数差代替 max-min，降低少量极端毛刺影响。
- 函数签名：

```c
signal_algorithm_status_t SignalRobustPeakToPeak_Process( const float *voltage_v, uint32_t count, const signal_robust_peak_to_peak_config_t *config, float *workspace, uint32_t workspace_count, signal_robust_peak_to_peak_result_t *result)
```

- 返回值（`signal_algorithm_status_t`）：成功返回 SIGNAL_ALGORITHM_OK。（依据：源码 `@return`）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `voltage_v` | `const float *` | [in] | 输入电压，V，只读。 | 源码 `@param` |
| `count` | `uint32_t` | [in] | 点数，必须大于 0 且不超过 INT32_MAX。 | 源码 `@param` |
| `config` | `const signal_robust_peak_to_peak_config_t *` | [in] | 0~1 内 lower<upper 的线性插值分位数。 | 源码 `@param` |
| `workspace` | `float *` | [in/out] | 调用者 float 工作区，容量至少 count，会被重排。 | 源码 `@param` |
| `workspace_count` | `uint32_t` | [in] | 工作区容量。 | 源码 `@param` |
| `result` | `signal_robust_peak_to_peak_result_t *` | [out] | 输出上下分位电压与 robust Vpp，V。 | 源码 `@param` |


<a id="file-modules-signal_slew_rate-c"></a>

## `modules/signal_slew_rate.c`

<a id="fn-modules-signal_slew_rate-c-crossing"></a>

### `crossing`

- 定义位置：[modules/signal_slew_rate.c:L5](modules/signal_slew_rate.c#L5)
- 属性：文件内部 `static`；功能分类：模拟前端、比较器、增益与功耗。
- 所属模块：从波形上升/下降沿计算压摆率。
- 具体作用：在相邻采样点间对指定电平做线性插值，返回更精细的交越位置。
- 函数签名：

```c
static float crossing(float y0, float y1, float threshold, uint32_t index)
```

- 返回值（`float`）：返回 `crossing` 计算得到的单精度结果；单位由函数用途和输入参数决定。（依据：类型/函数名语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `y0` | `float` | [in] | 线段/矩形起点纵坐标，单位像素。 | 签名/实现语义 |
| `y1` | `float` | [in] | 线段/矩形终点纵坐标，单位像素。 | 签名/实现语义 |
| `threshold` | `float` | [in] | 判决门限；单位与输入数据一致。 | 签名/实现语义 |
| `index` | `uint32_t` | [in] | 数组、波表、像素或状态表中的索引位置。 | 签名/实现语义 |

<a id="fn-modules-signal_slew_rate-c-SignalSlewRate_Process"></a>

### `SignalSlewRate_Process`

- 定义位置：[modules/signal_slew_rate.c:L17](modules/signal_slew_rate.c#L17)
- 属性：公开接口；功能分类：模拟前端、比较器、增益与功耗。
- 所属模块：从波形上升/下降沿计算压摆率。
- 具体作用：公开接口（从波形上升/下降沿计算压摆率）：对输入数组执行该模块的核心算法并写入结果结构。
- 函数签名：

```c
signal_algorithm_status_t SignalSlewRate_Process( const float *samples, uint32_t sample_count, float low_voltage_v, float high_voltage_v, uint32_t sample_rate_hz, const signal_slew_rate_config_t *config, signal_slew_rate_result_t *result)
```

- 返回值（`signal_algorithm_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `samples` | `const float *` | [in] | 时域样本数组；样本的单位由模块决定（ADC 码、伏特或归一化值）。 | 签名/实现语义 |
| `sample_count` | `uint32_t` | [in] | 本次采集或处理的样本数。 | 签名/实现语义 |
| `low_voltage_v` | `float` | [in] | 低端标定/下限电压，单位 V。 | 签名/实现语义 |
| `high_voltage_v` | `float` | [in] | 高端标定/上限电压，单位 V。 | 签名/实现语义 |
| `sample_rate_hz` | `uint32_t` | [in] | 采样率，单位 Hz；必须与实际定时器触发率一致。 | 签名/实现语义 |
| `config` | `const signal_slew_rate_config_t *` | [in] | 模块配置结构体，包含初始化所需的采样率、时钟、容量或硬件参数；调用期间必须保持可读。 | 签名/实现语义 |
| `result` | `signal_slew_rate_result_t *` | [out] | 计算结果输出地址；成功返回后由函数写入。 | 签名/实现语义 |


<a id="file-modules-signal_static_power-c"></a>

## `modules/signal_static_power.c`

<a id="fn-modules-signal_static_power-c-SignalStaticPower_Calculate"></a>

### `SignalStaticPower_Calculate`

- 定义位置：[modules/signal_static_power.c:L3](modules/signal_static_power.c#L3)
- 属性：公开接口；功能分类：模拟前端、比较器、增益与功耗。
- 所属模块：供电电压、电流到静态功耗的换算。
- 具体作用：由供电电压和电流计算静态功耗，并按输出指针返回电流与功率。
- 函数签名：

```c
signal_algorithm_status_t SignalStaticPower_Calculate( float shunt_voltage_v, float shunt_resistance_ohm, float supply_voltage_v, float rail_count_factor, float *current_ma, float *power_mw)
```

- 返回值（`signal_algorithm_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `shunt_voltage_v` | `float` | [in] | 电压，单位 V。 | 签名/实现语义 |
| `shunt_resistance_ohm` | `float` | [in] | 电流采样电阻阻值，单位 Ω。 | 签名/实现语义 |
| `supply_voltage_v` | `float` | [in] | 供电电压，单位 V。 | 签名/实现语义 |
| `rail_count_factor` | `float` | [in] | 控制 `SignalStaticPower_Calculate` 的 `rail_count_factor` 输入量；类型为 `float`，有效范围由函数中的参数校验决定。 | 签名/实现语义 |
| `current_ma` | `float *` | [out] | 电流，单位 mA。 | 签名/实现语义 |
| `power_mw` | `float *` | [out] | 功率，单位 mW。 | 签名/实现语义 |


<a id="file-modules-signal_tft_st7789-c"></a>

## `modules/signal_tft_st7789.c`

<a id="fn-modules-signal_tft_st7789-c-valid"></a>

### `valid`

- 定义位置：[modules/signal_tft_st7789.c:L14](modules/signal_tft_st7789.c#L14)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ST7789 SPI 屏驱动和基础图元。
- 具体作用：内部辅助（ST7789 SPI 屏驱动和基础图元）：检查驱动上下文、回调和初始化状态是否可用。
- 函数签名：

```c
static bool valid(const tft_st7789_t *tft)
```

- 返回值（`bool`）：返回布尔判定：`true` 表示 `valid` 所检查的条件成立，`false` 表示不成立。（依据：签名/函数名语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `const tft_st7789_t *` | [in] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_st7789-c-cs"></a>

### `cs`

- 定义位置：[modules/signal_tft_st7789.c:L20](modules/signal_tft_st7789.c#L20)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ST7789 SPI 屏驱动和基础图元。
- 具体作用：通过总线回调设置 TFT 片选电平。
- 函数签名：

```c
static void cs(tft_st7789_t *tft, bool high)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_st7789_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `high` | `bool` | [in] | 高电平、上限或区间较大端点。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_st7789-c-transfer_unlocked"></a>

### `transfer_unlocked`

- 定义位置：[modules/signal_tft_st7789.c:L27](modules/signal_tft_st7789.c#L27)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ST7789 SPI 屏驱动和基础图元。
- 具体作用：内部辅助（ST7789 SPI 屏驱动和基础图元）：在外层已持锁时执行一次 SPI/GPIO 数据事务。
- 函数签名：

```c
static tft_st7789_status_t transfer_unlocked(tft_st7789_t *tft, bool data_mode, const uint8_t *data, size_t length)
```

- 返回值（`tft_st7789_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_st7789_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `data_mode` | `bool` | [in] | 总线当前发送的是命令还是数据的标志。 | 签名/实现语义 |
| `data` | `const uint8_t *` | [in] | 待处理的数据/总线字节缓冲区；是否原地修改由参数方向和函数说明决定。 | 签名/实现语义 |
| `length` | `size_t` | [in] | 数据、文本或波表的有效长度。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_st7789-c-transfer"></a>

### `transfer`

- 定义位置：[modules/signal_tft_st7789.c:L44](modules/signal_tft_st7789.c#L44)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ST7789 SPI 屏驱动和基础图元。
- 具体作用：内部辅助（ST7789 SPI 屏驱动和基础图元）：加锁后执行一次 SPI/GPIO 数据事务并解锁。
- 函数签名：

```c
static tft_st7789_status_t transfer(tft_st7789_t *tft, bool data_mode, const uint8_t *data, size_t length)
```

- 返回值（`tft_st7789_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_st7789_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `data_mode` | `bool` | [in] | 总线当前发送的是命令还是数据的标志。 | 签名/实现语义 |
| `data` | `const uint8_t *` | [in] | 待处理的数据/总线字节缓冲区；是否原地修改由参数方向和函数说明决定。 | 签名/实现语义 |
| `length` | `size_t` | [in] | 数据、文本或波表的有效长度。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_st7789-c-command_data_unlocked"></a>

### `command_data_unlocked`

- 定义位置：[modules/signal_tft_st7789.c:L55](modules/signal_tft_st7789.c#L55)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ST7789 SPI 屏驱动和基础图元。
- 具体作用：在已经取得总线锁的前提下发送一条 TFT 命令及其可选参数。
- 函数签名：

```c
static tft_st7789_status_t command_data_unlocked(tft_st7789_t *tft, uint8_t command, const uint8_t *data, size_t length)
```

- 返回值（`tft_st7789_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_st7789_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `command` | `uint8_t` | [in] | 发送给 TFT/外部器件的命令字节或命令码。 | 签名/实现语义 |
| `data` | `const uint8_t *` | [in] | 待处理的数据/总线字节缓冲区；是否原地修改由参数方向和函数说明决定。 | 签名/实现语义 |
| `length` | `size_t` | [in] | 数据、文本或波表的有效长度。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_st7789-c-command_data"></a>

### `command_data`

- 定义位置：[modules/signal_tft_st7789.c:L65](modules/signal_tft_st7789.c#L65)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ST7789 SPI 屏驱动和基础图元。
- 具体作用：取得总线锁后发送一条 TFT 命令及其可选参数，再释放总线。
- 函数签名：

```c
static tft_st7789_status_t command_data(tft_st7789_t *tft, uint8_t command, const uint8_t *data, size_t length)
```

- 返回值（`tft_st7789_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_st7789_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `command` | `uint8_t` | [in] | 发送给 TFT/外部器件的命令字节或命令码。 | 签名/实现语义 |
| `data` | `const uint8_t *` | [in] | 待处理的数据/总线字节缓冲区；是否原地修改由参数方向和函数说明决定。 | 签名/实现语义 |
| `length` | `size_t` | [in] | 数据、文本或波表的有效长度。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_st7789-c-TFT_ST7789_WriteCommand"></a>

### `TFT_ST7789_WriteCommand`

- 定义位置：[modules/signal_tft_st7789.c:L76](modules/signal_tft_st7789.c#L76)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ST7789 SPI 屏驱动和基础图元。
- 具体作用：通过 SPI 向 ST7789 发送一个命令字节。
- 函数签名：

```c
tft_st7789_status_t TFT_ST7789_WriteCommand(tft_st7789_t *tft, uint8_t command)
```

- 返回值（`tft_st7789_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_st7789_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `command` | `uint8_t` | [in] | 发送给 TFT/外部器件的命令字节或命令码。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_st7789-c-TFT_ST7789_WriteData"></a>

### `TFT_ST7789_WriteData`

- 定义位置：[modules/signal_tft_st7789.c:L81](modules/signal_tft_st7789.c#L81)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ST7789 SPI 屏驱动和基础图元。
- 具体作用：通过 SPI 向 ST7789 发送数据缓冲区。
- 函数签名：

```c
tft_st7789_status_t TFT_ST7789_WriteData(tft_st7789_t *tft, const uint8_t *data, size_t length)
```

- 返回值（`tft_st7789_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_st7789_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `data` | `const uint8_t *` | [in] | 待处理的数据/总线字节缓冲区；是否原地修改由参数方向和函数说明决定。 | 签名/实现语义 |
| `length` | `size_t` | [in] | 数据、文本或波表的有效长度。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_st7789-c-set_rotation_raw"></a>

### `set_rotation_raw`

- 定义位置：[modules/signal_tft_st7789.c:L87](modules/signal_tft_st7789.c#L87)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ST7789 SPI 屏驱动和基础图元。
- 具体作用：写 ST7789 MADCTL 寄存器并更新旋转后的逻辑宽高。
- 函数签名：

```c
static tft_st7789_status_t set_rotation_raw(tft_st7789_t *tft, tft_st7789_rotation_t rotation)
```

- 返回值（`tft_st7789_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_st7789_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `rotation` | `tft_st7789_rotation_t` | [in] | 屏幕旋转枚举，决定宽高和坐标方向。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_st7789-c-TFT_ST7789_Init"></a>

### `TFT_ST7789_Init`

- 定义位置：[modules/signal_tft_st7789.c:L105](modules/signal_tft_st7789.c#L105)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ST7789 SPI 屏驱动和基础图元。
- 具体作用：公开接口（ST7789 SPI 屏驱动和基础图元）：初始化模块对象、硬件绑定和运行状态。
- 函数签名：

```c
tft_st7789_status_t TFT_ST7789_Init(tft_st7789_t *tft, const tft_st7789_config_t *config, tft_st7789_rotation_t rotation)
```

- 返回值（`tft_st7789_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_st7789_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `config` | `const tft_st7789_config_t *` | [in] | 模块配置结构体，包含初始化所需的采样率、时钟、容量或硬件参数；调用期间必须保持可读。 | 签名/实现语义 |
| `rotation` | `tft_st7789_rotation_t` | [in] | 屏幕旋转枚举，决定宽高和坐标方向。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_st7789-c-TFT_ST7789_SetRotation"></a>

### `TFT_ST7789_SetRotation`

- 定义位置：[modules/signal_tft_st7789.c:L173](modules/signal_tft_st7789.c#L173)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ST7789 SPI 屏驱动和基础图元。
- 具体作用：切换屏幕旋转方向并同步坐标系宽度、高度和偏移。
- 函数签名：

```c
tft_st7789_status_t TFT_ST7789_SetRotation(tft_st7789_t *tft, tft_st7789_rotation_t rotation)
```

- 返回值（`tft_st7789_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_st7789_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `rotation` | `tft_st7789_rotation_t` | [in] | 屏幕旋转枚举，决定宽高和坐标方向。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_st7789-c-TFT_ST7789_SetBacklight"></a>

### `TFT_ST7789_SetBacklight`

- 定义位置：[modules/signal_tft_st7789.c:L181](modules/signal_tft_st7789.c#L181)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ST7789 SPI 屏驱动和基础图元。
- 具体作用：通过平台回调开启或关闭屏幕背光。
- 函数签名：

```c
void TFT_ST7789_SetBacklight(tft_st7789_t *tft, bool on)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_st7789_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `on` | `bool` | [in] | 开关状态；true 为开启，false 为关闭。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_st7789-c-TFT_ST7789_GetWidth"></a>

### `TFT_ST7789_GetWidth`

- 定义位置：[modules/signal_tft_st7789.c:L187](modules/signal_tft_st7789.c#L187)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ST7789 SPI 屏驱动和基础图元。
- 具体作用：返回当前旋转方向下的逻辑屏幕宽度，单位像素。
- 函数签名：

```c
uint16_t TFT_ST7789_GetWidth(const tft_st7789_t *tft)
```

- 返回值（`uint16_t`）：返回当前旋转方向下的屏幕宽度/高度，单位像素。（依据：函数名语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `const tft_st7789_t *` | [in] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_st7789-c-TFT_ST7789_GetHeight"></a>

### `TFT_ST7789_GetHeight`

- 定义位置：[modules/signal_tft_st7789.c:L188](modules/signal_tft_st7789.c#L188)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ST7789 SPI 屏驱动和基础图元。
- 具体作用：返回当前旋转方向下的逻辑屏幕高度，单位像素。
- 函数签名：

```c
uint16_t TFT_ST7789_GetHeight(const tft_st7789_t *tft)
```

- 返回值（`uint16_t`）：返回当前旋转方向下的屏幕宽度/高度，单位像素。（依据：函数名语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `const tft_st7789_t *` | [in] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_st7789-c-TFT_ST7789_SetAddressWindow"></a>

### `TFT_ST7789_SetAddressWindow`

- 定义位置：[modules/signal_tft_st7789.c:L190](modules/signal_tft_st7789.c#L190)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ST7789 SPI 屏驱动和基础图元。
- 具体作用：设置 ST7789 后续显存写入的矩形地址窗口。
- 函数签名：

```c
tft_st7789_status_t TFT_ST7789_SetAddressWindow(tft_st7789_t *tft, uint16_t x0, uint16_t y0, uint16_t x1, uint16_t y1)
```

- 返回值（`tft_st7789_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_st7789_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `x0` | `uint16_t` | [in] | 线段/矩形起点横坐标，单位像素。 | 签名/实现语义 |
| `y0` | `uint16_t` | [in] | 线段/矩形起点纵坐标，单位像素。 | 签名/实现语义 |
| `x1` | `uint16_t` | [in] | 线段/矩形终点横坐标，单位像素。 | 签名/实现语义 |
| `y1` | `uint16_t` | [in] | 线段/矩形终点纵坐标，单位像素。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_st7789-c-draw_pixels"></a>

### `draw_pixels`

- 定义位置：[modules/signal_tft_st7789.c:L210](modules/signal_tft_st7789.c#L210)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ST7789 SPI 屏驱动和基础图元。
- 具体作用：在已设置的地址窗口中批量发送 RGB565 像素。
- 函数签名：

```c
static tft_st7789_status_t draw_pixels(tft_st7789_t *tft, const uint16_t *pixels, size_t count)
```

- 返回值（`tft_st7789_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_st7789_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `pixels` | `const uint16_t *` | [in] | RGB565 像素数组。 | 签名/实现语义 |
| `count` | `size_t` | [in] | 本次参与处理的元素/样本数量。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_st7789-c-TFT_ST7789_DrawPixel"></a>

### `TFT_ST7789_DrawPixel`

- 定义位置：[modules/signal_tft_st7789.c:L224](modules/signal_tft_st7789.c#L224)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ST7789 SPI 屏驱动和基础图元。
- 具体作用：裁剪坐标后设置 1×1 地址窗口并写入一个 RGB565 像素。
- 函数签名：

```c
tft_st7789_status_t TFT_ST7789_DrawPixel(tft_st7789_t *tft, int32_t x, int32_t y, uint16_t color)
```

- 返回值（`tft_st7789_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_st7789_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `x` | `int32_t` | [in] | 横坐标、第一操作数或 X 通道值；显示函数中单位为像素。 | 签名/实现语义 |
| `y` | `int32_t` | [in] | 纵坐标、第二操作数或 Y 通道值；显示函数中单位为像素。 | 签名/实现语义 |
| `color` | `uint16_t` | [in] | RGB565 颜色值。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_st7789-c-TFT_ST7789_FillRect"></a>

### `TFT_ST7789_FillRect`

- 定义位置：[modules/signal_tft_st7789.c:L233](modules/signal_tft_st7789.c#L233)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ST7789 SPI 屏驱动和基础图元。
- 具体作用：裁剪目标区域后用同一 RGB565 颜色填充矩形。
- 函数签名：

```c
tft_st7789_status_t TFT_ST7789_FillRect(tft_st7789_t *tft, int32_t x, int32_t y, int32_t width, int32_t height, uint16_t color)
```

- 返回值（`tft_st7789_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_st7789_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `x` | `int32_t` | [in] | 横坐标、第一操作数或 X 通道值；显示函数中单位为像素。 | 签名/实现语义 |
| `y` | `int32_t` | [in] | 纵坐标、第二操作数或 Y 通道值；显示函数中单位为像素。 | 签名/实现语义 |
| `width` | `int32_t` | [in] | 图形、位图或显示区域宽度，通常单位为像素。 | 签名/实现语义 |
| `height` | `int32_t` | [in] | 图形、位图或显示区域高度，通常单位为像素。 | 签名/实现语义 |
| `color` | `uint16_t` | [in] | RGB565 颜色值。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_st7789-c-TFT_ST7789_FillScreen"></a>

### `TFT_ST7789_FillScreen`

- 定义位置：[modules/signal_tft_st7789.c:L246](modules/signal_tft_st7789.c#L246)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ST7789 SPI 屏驱动和基础图元。
- 具体作用：以指定 RGB565 颜色填充整块屏幕。
- 函数签名：

```c
tft_st7789_status_t TFT_ST7789_FillScreen(tft_st7789_t *tft, uint16_t color)
```

- 返回值（`tft_st7789_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_st7789_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `color` | `uint16_t` | [in] | RGB565 颜色值。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_st7789-c-TFT_ST7789_DrawLine"></a>

### `TFT_ST7789_DrawLine`

- 定义位置：[modules/signal_tft_st7789.c:L253](modules/signal_tft_st7789.c#L253)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ST7789 SPI 屏驱动和基础图元。
- 具体作用：使用整数增量算法在两点之间绘制 RGB565 线段。
- 函数签名：

```c
tft_st7789_status_t TFT_ST7789_DrawLine(tft_st7789_t *tft,int32_t x0,int32_t y0,int32_t x1,int32_t y1,uint16_t color)
```

- 返回值（`tft_st7789_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_st7789_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `x0` | `int32_t` | [in] | 线段/矩形起点横坐标，单位像素。 | 签名/实现语义 |
| `y0` | `int32_t` | [in] | 线段/矩形起点纵坐标，单位像素。 | 签名/实现语义 |
| `x1` | `int32_t` | [in] | 线段/矩形终点横坐标，单位像素。 | 签名/实现语义 |
| `y1` | `int32_t` | [in] | 线段/矩形终点纵坐标，单位像素。 | 签名/实现语义 |
| `color` | `uint16_t` | [in] | RGB565 颜色值。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_st7789-c-TFT_ST7789_DrawRect"></a>

### `TFT_ST7789_DrawRect`

- 定义位置：[modules/signal_tft_st7789.c:L263](modules/signal_tft_st7789.c#L263)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ST7789 SPI 屏驱动和基础图元。
- 具体作用：绘制矩形的四条边，不填充内部。
- 函数签名：

```c
tft_st7789_status_t TFT_ST7789_DrawRect(tft_st7789_t *tft,int32_t x,int32_t y,int32_t width,int32_t height,uint16_t color)
```

- 返回值（`tft_st7789_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_st7789_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `x` | `int32_t` | [in] | 横坐标、第一操作数或 X 通道值；显示函数中单位为像素。 | 签名/实现语义 |
| `y` | `int32_t` | [in] | 纵坐标、第二操作数或 Y 通道值；显示函数中单位为像素。 | 签名/实现语义 |
| `width` | `int32_t` | [in] | 图形、位图或显示区域宽度，通常单位为像素。 | 签名/实现语义 |
| `height` | `int32_t` | [in] | 图形、位图或显示区域高度，通常单位为像素。 | 签名/实现语义 |
| `color` | `uint16_t` | [in] | RGB565 颜色值。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_st7789-c-TFT_ST7789_DrawRGB565"></a>

### `TFT_ST7789_DrawRGB565`

- 定义位置：[modules/signal_tft_st7789.c:L266](modules/signal_tft_st7789.c#L266)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ST7789 SPI 屏驱动和基础图元。
- 具体作用：把调用者提供的 RGB565 像素块写到指定矩形区域。
- 函数签名：

```c
tft_st7789_status_t TFT_ST7789_DrawRGB565(tft_st7789_t *tft,int32_t x,int32_t y,int32_t width,int32_t height,const uint16_t *pixels)
```

- 返回值（`tft_st7789_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_st7789_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `x` | `int32_t` | [in] | 横坐标、第一操作数或 X 通道值；显示函数中单位为像素。 | 签名/实现语义 |
| `y` | `int32_t` | [in] | 纵坐标、第二操作数或 Y 通道值；显示函数中单位为像素。 | 签名/实现语义 |
| `width` | `int32_t` | [in] | 图形、位图或显示区域宽度，通常单位为像素。 | 签名/实现语义 |
| `height` | `int32_t` | [in] | 图形、位图或显示区域高度，通常单位为像素。 | 签名/实现语义 |
| `pixels` | `const uint16_t *` | [in] | RGB565 像素数组。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_st7789-c-TFT_ST7789_DrawMonoBitmap"></a>

### `TFT_ST7789_DrawMonoBitmap`

- 定义位置：[modules/signal_tft_st7789.c:L271](modules/signal_tft_st7789.c#L271)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ST7789 SPI 屏驱动和基础图元。
- 具体作用：按单色位图的前景/背景位在屏幕上绘制图形，并支持透明背景。
- 函数签名：

```c
tft_st7789_status_t TFT_ST7789_DrawMonoBitmap(tft_st7789_t *tft,int32_t x,int32_t y,uint8_t width,uint8_t height,const uint8_t *bitmap,size_t bitmap_size,uint16_t fg,uint16_t bg,bool transparent)
```

- 返回值（`tft_st7789_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_st7789_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `x` | `int32_t` | [in] | 横坐标、第一操作数或 X 通道值；显示函数中单位为像素。 | 签名/实现语义 |
| `y` | `int32_t` | [in] | 纵坐标、第二操作数或 Y 通道值；显示函数中单位为像素。 | 签名/实现语义 |
| `width` | `uint8_t` | [in] | 图形、位图或显示区域宽度，通常单位为像素。 | 签名/实现语义 |
| `height` | `uint8_t` | [in] | 图形、位图或显示区域高度，通常单位为像素。 | 签名/实现语义 |
| `bitmap` | `const uint8_t *` | [in] | 单色或彩色位图数据。 | 签名/实现语义 |
| `bitmap_size` | `size_t` | [in] | 位图缓冲区字节数，用于边界检查。 | 签名/实现语义 |
| `fg` | `uint16_t` | [in] | 前景 RGB565 颜色。 | 签名/实现语义 |
| `bg` | `uint16_t` | [in] | 背景 RGB565 颜色。 | 签名/实现语义 |
| `transparent` | `bool` | [in] | 是否启用透明背景绘制。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_st7789-c-SignalTFTST7789_GetModuleStatus"></a>

### `SignalTFTST7789_GetModuleStatus`

- 定义位置：[modules/signal_tft_st7789.c:L279](modules/signal_tft_st7789.c#L279)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ST7789 SPI 屏驱动和基础图元。
- 具体作用：公开接口（ST7789 SPI 屏驱动和基础图元）：返回模块成熟度和验证等级元数据。
- 函数签名：

```c
signal_module_status_t SignalTFTST7789_GetModuleStatus(void)
```

- 返回值（`signal_module_status_t`）：返回模块成熟度/验证等级元数据，供应用判断示例、冻结或硬件验证状态。（依据：类型/实现语义）
- 参数：无（`void`）。


<a id="file-modules-signal_tft_st7789_font-c"></a>

## `modules/signal_tft_st7789_font.c`

<a id="fn-modules-signal_tft_st7789_font-c-TFT_ST7789_GetFont"></a>

### `TFT_ST7789_GetFont`

- 定义位置：[modules/signal_tft_st7789_font.c:L33](modules/signal_tft_st7789_font.c#L33)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ST7789 多尺寸 ASCII 字库、数字和浮点文本。
- 具体作用：按字体枚举返回内置字模描述符。
- 函数签名：

```c
static const tft_st7789_font_descriptor_t *TFT_ST7789_GetFont( tft_st7789_font_t font)
```

- 返回值（`const tft_st7789_font_descriptor_t *`）：返回内部对象/缓冲区指针；调用者不得越界访问，若接口允许失败还需先检查是否为 `NULL`。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `font` | `tft_st7789_font_t` | [in] | 字体描述符，包含字模尺寸、字符范围和位图地址。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_st7789_font-c-TFT_ST7789_GetFontMetrics"></a>

### `TFT_ST7789_GetFontMetrics`

- 定义位置：[modules/signal_tft_st7789_font.c:L47](modules/signal_tft_st7789_font.c#L47)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ST7789 多尺寸 ASCII 字库、数字和浮点文本。
- 具体作用：读取指定字体的字符宽度和高度。
- 函数签名：

```c
tft_st7789_status_t TFT_ST7789_GetFontMetrics(tft_st7789_font_t font, uint8_t *width, uint8_t *height)
```

- 返回值（`tft_st7789_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `font` | `tft_st7789_font_t` | [in] | 字体描述符，包含字模尺寸、字符范围和位图地址。 | 签名/实现语义 |
| `width` | `uint8_t *` | [in/out] | 图形、位图或显示区域宽度，通常单位为像素。 | 签名/实现语义 |
| `height` | `uint8_t *` | [in/out] | 图形、位图或显示区域高度，通常单位为像素。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_st7789_font-c-TFT_ST7789_DrawChar"></a>

### `TFT_ST7789_DrawChar`

- 定义位置：[modules/signal_tft_st7789_font.c:L59](modules/signal_tft_st7789_font.c#L59)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ST7789 多尺寸 ASCII 字库、数字和浮点文本。
- 具体作用：从字库取出一个字符位图，并按前景色、背景色和透明设置绘制。
- 函数签名：

```c
tft_st7789_status_t TFT_ST7789_DrawChar(tft_st7789_t *tft, int32_t x, int32_t y, char character, tft_st7789_font_t font, uint16_t foreground, uint16_t background, bool transparent_background)
```

- 返回值（`tft_st7789_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_st7789_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `x` | `int32_t` | [in] | 横坐标、第一操作数或 X 通道值；显示函数中单位为像素。 | 签名/实现语义 |
| `y` | `int32_t` | [in] | 纵坐标、第二操作数或 Y 通道值；显示函数中单位为像素。 | 签名/实现语义 |
| `character` | `char` | [in] | 待绘制或转换的字符。 | 签名/实现语义 |
| `font` | `tft_st7789_font_t` | [in] | 字体描述符，包含字模尺寸、字符范围和位图地址。 | 签名/实现语义 |
| `foreground` | `uint16_t` | [in] | 文字/前景的 RGB565 颜色。 | 签名/实现语义 |
| `background` | `uint16_t` | [in] | 背景的 RGB565 颜色。 | 签名/实现语义 |
| `transparent_background` | `bool` | [in] | 是否跳过背景像素：true 为透明文字，false 会填背景色。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_st7789_font-c-TFT_ST7789_DrawString"></a>

### `TFT_ST7789_DrawString`

- 定义位置：[modules/signal_tft_st7789_font.c:L76](modules/signal_tft_st7789_font.c#L76)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ST7789 多尺寸 ASCII 字库、数字和浮点文本。
- 具体作用：逐字符绘制 NUL 结尾字符串，并按配置处理换行和透明背景。
- 函数签名：

```c
tft_st7789_status_t TFT_ST7789_DrawString(tft_st7789_t *tft, int32_t x, int32_t y, const char *text, tft_st7789_font_t font, uint16_t foreground, uint16_t background, bool transparent_background, bool wrap)
```

- 返回值（`tft_st7789_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_st7789_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `x` | `int32_t` | [in] | 横坐标、第一操作数或 X 通道值；显示函数中单位为像素。 | 签名/实现语义 |
| `y` | `int32_t` | [in] | 纵坐标、第二操作数或 Y 通道值；显示函数中单位为像素。 | 签名/实现语义 |
| `text` | `const char *` | [in] | 以 NUL 结尾的待显示/处理字符串。 | 签名/实现语义 |
| `font` | `tft_st7789_font_t` | [in] | 字体描述符，包含字模尺寸、字符范围和位图地址。 | 签名/实现语义 |
| `foreground` | `uint16_t` | [in] | 文字/前景的 RGB565 颜色。 | 签名/实现语义 |
| `background` | `uint16_t` | [in] | 背景的 RGB565 颜色。 | 签名/实现语义 |
| `transparent_background` | `bool` | [in] | 是否跳过背景像素：true 为透明文字，false 会填背景色。 | 签名/实现语义 |
| `wrap` | `bool` | [in] | 文字到达屏幕边缘后是否自动换行。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_st7789_font-c-TFT_ST7789_AppendUint32"></a>

### `TFT_ST7789_AppendUint32`

- 定义位置：[modules/signal_tft_st7789_font.c:L114](modules/signal_tft_st7789_font.c#L114)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ST7789 多尺寸 ASCII 字库、数字和浮点文本。
- 具体作用：把无符号整数转换为十进制字符并追加到文本缓冲区。
- 函数签名：

```c
static size_t TFT_ST7789_AppendUint32(char *buffer, size_t position, uint32_t value, uint8_t minimum_digits)
```

- 返回值（`size_t`）：返回当前缓冲区元素数或已追加的字符数；具体含义由函数作用决定。（依据：类型/函数名语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `buffer` | `char *` | [in/out] | 调用者提供的工作或输出缓冲区，生命周期必须覆盖本次操作。 | 签名/实现语义 |
| `position` | `size_t` | [in] | 数组、波表、像素或状态表中的索引位置。 | 签名/实现语义 |
| `value` | `uint32_t` | [in] | 要设置、换算或限制的输入值；量纲见函数名和相邻参数。 | 签名/实现语义 |
| `minimum_digits` | `uint8_t` | [in] | 整数显示的最少位数，不足时补前导字符。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_st7789_font-c-TFT_ST7789_DrawInt32"></a>

### `TFT_ST7789_DrawInt32`

- 定义位置：[modules/signal_tft_st7789_font.c:L130](modules/signal_tft_st7789_font.c#L130)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ST7789 多尺寸 ASCII 字库、数字和浮点文本。
- 具体作用：把有符号 32 位整数格式化后用指定字体绘制。
- 函数签名：

```c
tft_st7789_status_t TFT_ST7789_DrawInt32(tft_st7789_t *tft, int32_t x, int32_t y, int32_t value, tft_st7789_font_t font, uint16_t foreground, uint16_t background, bool transparent_background)
```

- 返回值（`tft_st7789_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_st7789_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `x` | `int32_t` | [in] | 横坐标、第一操作数或 X 通道值；显示函数中单位为像素。 | 签名/实现语义 |
| `y` | `int32_t` | [in] | 纵坐标、第二操作数或 Y 通道值；显示函数中单位为像素。 | 签名/实现语义 |
| `value` | `int32_t` | [in] | 要设置、换算或限制的输入值；量纲见函数名和相邻参数。 | 签名/实现语义 |
| `font` | `tft_st7789_font_t` | [in] | 字体描述符，包含字模尺寸、字符范围和位图地址。 | 签名/实现语义 |
| `foreground` | `uint16_t` | [in] | 文字/前景的 RGB565 颜色。 | 签名/实现语义 |
| `background` | `uint16_t` | [in] | 背景的 RGB565 颜色。 | 签名/实现语义 |
| `transparent_background` | `bool` | [in] | 是否跳过背景像素：true 为透明文字，false 会填背景色。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_st7789_font-c-TFT_ST7789_DrawFloat"></a>

### `TFT_ST7789_DrawFloat`

- 定义位置：[modules/signal_tft_st7789_font.c:L149](modules/signal_tft_st7789_font.c#L149)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ST7789 多尺寸 ASCII 字库、数字和浮点文本。
- 具体作用：按指定小数位格式化浮点数后用指定字体绘制。
- 函数签名：

```c
tft_st7789_status_t TFT_ST7789_DrawFloat(tft_st7789_t *tft, int32_t x, int32_t y, float value, uint8_t decimal_places, tft_st7789_font_t font, uint16_t foreground, uint16_t background, bool transparent_background)
```

- 返回值（`tft_st7789_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_st7789_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `x` | `int32_t` | [in] | 横坐标、第一操作数或 X 通道值；显示函数中单位为像素。 | 签名/实现语义 |
| `y` | `int32_t` | [in] | 纵坐标、第二操作数或 Y 通道值；显示函数中单位为像素。 | 签名/实现语义 |
| `value` | `float` | [in] | 要设置、换算或限制的输入值；量纲见函数名和相邻参数。 | 签名/实现语义 |
| `decimal_places` | `uint8_t` | [in] | 十进制小数位数。 | 签名/实现语义 |
| `font` | `tft_st7789_font_t` | [in] | 字体描述符，包含字模尺寸、字符范围和位图地址。 | 签名/实现语义 |
| `foreground` | `uint16_t` | [in] | 文字/前景的 RGB565 颜色。 | 签名/实现语义 |
| `background` | `uint16_t` | [in] | 背景的 RGB565 颜色。 | 签名/实现语义 |
| `transparent_background` | `bool` | [in] | 是否跳过背景像素：true 为透明文字，false 会填背景色。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_st7789_font-c-SignalTFTST7789Font_GetModuleStatus"></a>

### `SignalTFTST7789Font_GetModuleStatus`

- 定义位置：[modules/signal_tft_st7789_font.c:L191](modules/signal_tft_st7789_font.c#L191)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ST7789 多尺寸 ASCII 字库、数字和浮点文本。
- 具体作用：公开接口（ST7789 多尺寸 ASCII 字库、数字和浮点文本）：返回模块成熟度和验证等级元数据。
- 函数签名：

```c
signal_module_status_t SignalTFTST7789Font_GetModuleStatus(void)
```

- 返回值（`signal_module_status_t`）：返回模块成熟度/验证等级元数据，供应用判断示例、冻结或硬件验证状态。（依据：类型/实现语义）
- 参数：无（`void`）。


<a id="file-modules-signal_tft_st7789_mspm0g3507-c"></a>

## `modules/signal_tft_st7789_mspm0g3507.c`

<a id="fn-modules-signal_tft_st7789_mspm0g3507-c-write_spi"></a>

### `write_spi`

- 定义位置：[modules/signal_tft_st7789_mspm0g3507.c:L5](modules/signal_tft_st7789_mspm0g3507.c#L5)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ST7789 到 MSPM0 SPI/GPIO 的平台适配。
- 具体作用：内部辅助（ST7789 到 MSPM0 SPI/GPIO 的平台适配）：MSPM0 SPI 阻塞发送适配回调。
- 函数签名：

```c
static int write_spi(void *context, const uint8_t *data, size_t length)
```

- 返回值（`int`）：返回总线回调状态；0 通常表示发送成功，非 0 表示失败。（依据：实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `context` | `void *` | [in/out] | 驱动/算法上下文或回调用户数据，用于保存实例状态并避免依赖全局变量。 | 签名/实现语义 |
| `data` | `const uint8_t *` | [in] | 待处理的数据/总线字节缓冲区；是否原地修改由参数方向和函数说明决定。 | 签名/实现语义 |
| `length` | `size_t` | [in] | 数据、文本或波表的有效长度。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_st7789_mspm0g3507-c-set_dc"></a>

### `set_dc`

- 定义位置：[modules/signal_tft_st7789_mspm0g3507.c:L16](modules/signal_tft_st7789_mspm0g3507.c#L16)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ST7789 到 MSPM0 SPI/GPIO 的平台适配。
- 具体作用：设置 ST7789 的 D/C 引脚，选择命令阶段或数据阶段。
- 函数签名：

```c
static void set_dc(void *context, bool high)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `context` | `void *` | [in/out] | 驱动/算法上下文或回调用户数据，用于保存实例状态并避免依赖全局变量。 | 签名/实现语义 |
| `high` | `bool` | [in] | 高电平、上限或区间较大端点。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_st7789_mspm0g3507-c-set_bl"></a>

### `set_bl`

- 定义位置：[modules/signal_tft_st7789_mspm0g3507.c:L23](modules/signal_tft_st7789_mspm0g3507.c#L23)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ST7789 到 MSPM0 SPI/GPIO 的平台适配。
- 具体作用：设置 ST7789 背光控制引脚。
- 函数签名：

```c
static void set_bl(void *context, bool high)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `context` | `void *` | [in/out] | 驱动/算法上下文或回调用户数据，用于保存实例状态并避免依赖全局变量。 | 签名/实现语义 |
| `high` | `bool` | [in] | 高电平、上限或区间较大端点。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_st7789_mspm0g3507-c-set_reset"></a>

### `set_reset`

- 定义位置：[modules/signal_tft_st7789_mspm0g3507.c:L31](modules/signal_tft_st7789_mspm0g3507.c#L31)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ST7789 到 MSPM0 SPI/GPIO 的平台适配。
- 具体作用：设置 ST7789 硬件复位引脚。
- 函数签名：

```c
static void set_reset(void *context, bool high)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `context` | `void *` | [in/out] | 驱动/算法上下文或回调用户数据，用于保存实例状态并避免依赖全局变量。 | 签名/实现语义 |
| `high` | `bool` | [in] | 高电平、上限或区间较大端点。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_st7789_mspm0g3507-c-delay_ms"></a>

### `delay_ms`

- 定义位置：[modules/signal_tft_st7789_mspm0g3507.c:L39](modules/signal_tft_st7789_mspm0g3507.c#L39)
- 属性：文件内部 `static`；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ST7789 到 MSPM0 SPI/GPIO 的平台适配。
- 具体作用：调用平台延时回调等待指定毫秒数。
- 函数签名：

```c
static void delay_ms(void *context, uint32_t milliseconds)
```

- 返回值（`void`）：无返回值；结果通过硬件状态、模块对象或输出参数产生。（依据：签名/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `context` | `void *` | [in/out] | 驱动/算法上下文或回调用户数据，用于保存实例状态并避免依赖全局变量。 | 签名/实现语义 |
| `milliseconds` | `uint32_t` | [in] | 延时时间，单位 ms。 | 签名/实现语义 |

<a id="fn-modules-signal_tft_st7789_mspm0g3507-c-SignalTFTST7789_MSPM0_Init"></a>

### `SignalTFTST7789_MSPM0_Init`

- 定义位置：[modules/signal_tft_st7789_mspm0g3507.c:L45](modules/signal_tft_st7789_mspm0g3507.c#L45)
- 属性：公开接口；功能分类：TFT、字库、波形与界面绘制。
- 所属模块：ST7789 到 MSPM0 SPI/GPIO 的平台适配。
- 具体作用：公开接口（ST7789 到 MSPM0 SPI/GPIO 的平台适配）：初始化模块对象、硬件绑定和运行状态。
- 函数签名：

```c
tft_st7789_status_t SignalTFTST7789_MSPM0_Init( tft_st7789_t *tft, tft_st7789_rotation_t rotation, uint16_t x_offset, uint16_t y_offset)
```

- 返回值（`tft_st7789_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `tft` | `tft_st7789_t *` | [in/out] | TFT 驱动上下文，保存总线回调、屏幕尺寸、旋转方向和当前状态。 | 签名/实现语义 |
| `rotation` | `tft_st7789_rotation_t` | [in] | 屏幕旋转枚举，决定宽高和坐标方向。 | 签名/实现语义 |
| `x_offset` | `uint16_t` | [in] | 控制 `SignalTFTST7789_MSPM0_Init` 的 `x_offset` 输入量；类型为 `uint16_t`，有效范围由函数中的参数校验决定。 | 签名/实现语义 |
| `y_offset` | `uint16_t` | [in] | 控制 `SignalTFTST7789_MSPM0_Init` 的 `y_offset` 输入量；类型为 `uint16_t`，有效范围由函数中的参数校验决定。 | 签名/实现语义 |


<a id="file-modules-signal_vca820_gain_control-c"></a>

## `modules/signal_vca820_gain_control.c`

<a id="fn-modules-signal_vca820_gain_control-c-SignalVCA820_TargetVppToDACCode"></a>

### `SignalVCA820_TargetVppToDACCode`

- 定义位置：[modules/signal_vca820_gain_control.c:L5](modules/signal_vca820_gain_control.c#L5)
- 属性：公开接口；功能分类：模拟前端、比较器、增益与功耗。
- 所属模块：VCA820 控制电压、增益和 DAC 码预算。
- 具体作用：作为模块公开接口，读取DAC 数据/配置；具体输入、输出和量纲见下方参数及返回值说明。
- 函数签名：

```c
signal_algorithm_status_t SignalVCA820_TargetVppToDACCode( float target_vpp_v, const signal_vca820_gain_config_t *config, uint16_t *dac_code)
```

- 返回值（`signal_algorithm_status_t`）：返回状态码：成功值表示操作完成，其他值用于区分空指针、范围、容量、硬件或数值错误。（依据：类型/实现语义）
- 参数明细：

| 参数 | 声明类型 | 方向 | 具体意义 | 说明依据 |
|---|---|---|---|---|
| `target_vpp_v` | `float` | [in] | 期望输出峰峰值，单位 V。 | 签名/实现语义 |
| `config` | `const signal_vca820_gain_config_t *` | [in] | 模块配置结构体，包含初始化所需的采样率、时钟、容量或硬件参数；调用期间必须保持可读。 | 签名/实现语义 |
| `dac_code` | `uint16_t *` | [out] | DAC 原始输出码。 | 签名/实现语义 |

## 7. 赛场复用检查表

1. 先从第 4 节确认应用实际调用的公开 API，再到第 6 节核对参数、返回值及 `static` 依赖。
2. 复制纯算法模块时同时复制同名 `.c/.h`、公共状态类型以及 `signal_math*` 依赖。
3. 复制 ADC/DMA、DAC、Capture、TFT、键盘、OPA/GPAMP/COMP 等硬件模块时，同步复现第 2 节 SysConfig 实例名和事件/IRQ/引脚。
4. 核对 `signal_config.h` 中的 Fs、N、VREF、ADC/DAC 位数、DMA 块数和显示尺寸；这些量决定参数单位和数组容量。
5. `example05` 标为构建排除的旧 DAC/DDS 文件不能因为“目录里存在”就认为当前固件会编译执行。
6. 本手册是静态审计结果，不等同于示波器/逻辑分析仪/实板验证；赛前至少对采样率、相位符号、门限电压和引脚复用做一次硬件复核。

---

生成器：`tools/generate_per_project_function_docs.py`；本文件校验记录：114 个函数、275 个具名参数。
