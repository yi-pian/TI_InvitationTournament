# 24_A-05 Q4 静态功耗

复制 `static_power_measurement` README 的计算调用。Q4 先调用 AD9850 power-down，再把 DAC 写为 0，等待稳定后采集 `POWER_ADC`，最后调用 `SignalStaticPower_Calculate`。

自写顺序逐行解释：power-down 行去掉 DDS 动态负载；DAC 清零行关闭幅度控制输出；延时行等待电源进入静态；平均 ADC 行得到采样电阻电压；模块调用行换算电流和功率；状态行通知显示刷新。公式不在 main 重写。
