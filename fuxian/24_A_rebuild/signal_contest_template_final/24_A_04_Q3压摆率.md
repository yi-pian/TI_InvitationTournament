# 24_A-04 Q3 压摆率

复制稳健 Vpp 与压摆率模块 README 的配置和 Process 调用。SysConfig 仍使用 ADC0/PA25、DMA_CH0、TIMG0，只由 `SignalADC_SetSampleRate` 切到 2 MS/s。

自写代码逐行作用：第一行把 AD9850 切到方波测试频率；第二行等待模拟通道稳定；第三行启动 ADC 帧；随后把 ADC code 换成电压；模块调用分别返回上升/下降压摆率与边沿时间；最后只把结果存入显示变量。无效结果分支把测量状态置为失败，避免屏幕显示旧值。
