# 24_A-02 Q1 信号源与键盘

从 AD9850 README 复制配置结构体、`AD9850_Init`、`AD9850_SetFrequencyHz`；从 VCA820 README 复制目标 Vpp 到 DAC code 的调用；从矩阵键盘 README 复制 `SignalMatrixKeypad4x4_ReadNewSymbol` 最小闭环。

自写逻辑逐行含义：数字键一行把字符减 `'0'` 得到数值；下一行用“旧值乘 10 再加新数字”形成预输入；`*` 分支切换频率/Vpp 编辑项；`#` 分支检查范围并调用模块；最后清空预输入并置 `g_tft_dirty=true`。这些行只做 UI 组合，不重写 DDS、键盘扫描或 VCA 公式。
