# 24_A-06 ST7789 显示与验证

显示代码直接使用 ST7789 README 的 `SignalTFTST7789_MSPM0_Init`、FillScreen 和字体 DrawString/DrawInt32/DrawFloat，没有 ILI9341 映射。显示文字、行坐标、颜色和原 24_A 相同。

自写刷新逻辑逐行解释：上电 `FillScreen` 只执行一次；`App_TFTDrawStaticLayout` 画固定标题；主循环先检查 `g_tft_dirty`；为 false 立即返回；为 true 时按 `g_app_mode` 画对应数值；最后清 false。Q1 另外使用 `g_q1_input_dirty` 和 `g_q1_value_dirty`：数字键只置 `g_q1_input_dirty`，只清除并重画 `INPUT:` 右侧 136x16 字段；`*` 只清除并重画 `EDIT:` 右侧字段和输入字段；`#` 额外置 `g_q1_value_dirty`，只清除并重画频率/VPP 右侧字段。标签、单位、标题和其它行不传输。这样输入数字不会重画整页。现场验证顺序是 Generate SysConfig、Refresh、Clean/Build、依次按 A-D 检查四问。当前实板状态为 `BOARD NOT_RUN`。
