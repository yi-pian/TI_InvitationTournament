# MODULE CARD

MODULE: VCA820 Gain Control

功能：目标 Vpp -> VCA820 控制电压 -> DAC12 code。

输入：目标 Vpp 和题目校准参数；输出：12-bit DAC code。

SysConfig：不需要；DAC 外设仍由应用工程配置。

边界：纯算法、无动态内存、无 GPIO/Timer/ADC 访问。状态：PC_VERIFIED；BOARD NOT_RUN。
