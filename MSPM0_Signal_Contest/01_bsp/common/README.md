# Common contracts

`signal_status.h` separates runtime state from evidence maturity; `signal_types.h` carries pointer, length and sample-rate metadata between modules; `signal_math.h` contains tiny portable helpers. These headers own no hardware and allocate no memory.
