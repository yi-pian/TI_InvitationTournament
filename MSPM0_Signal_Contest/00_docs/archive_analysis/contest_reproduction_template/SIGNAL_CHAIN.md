# Signal Chain

```text
Input → Analog Frontend → Hardware Acquisition → Adapter
      → Selected Algorithms → Result → UART/UI
```

逐箭头注明类型、数组长度、单位、Fs 与所有权。禁止出现无说明的 raw→float 或 packed FFT→magnitude 循环。
